# UI_SPEC.md — 화면 명세 (실제 스크린샷 기반)

> 실제 서비스 화면 캡처 20장을 분석해 작성한 UI/UX 명세.
> 개인 사용 목적의 재현이며, PROJECT_SPEC.md의 기능 명세를 화면 단위로 구체화한다.
> [작성 상태] ✅ 기능 1 완결 (채널 설정 8탭 · 프로젝트 관리 · 상세 6탭 · 타임라인 편집기 · 성과 분석) / ✅ 기능 2 완결 (10탭 전체 + 분석 모달 2변형 + 파생 지표 알고리즘)

---

## 0. 공통 레이아웃

### 0.1 전체 구조
```
┌────────────────────────────────────────────────────┐
│ 상단 헤더 (고정, 흰 배경, 하단 보더)                    │
├──────────┬─────────────────────────────────────────┤
│ 좌측     │  콘텐츠 영역 (연한 회색 배경 #F8F9FB 계열)   │
│ 사이드바  │  ┌ 안내 배너 (닫기 가능)                   │
│ (고정)   │  ├ 페이지 타이틀 영역                       │
│          │  └ 메인 콘텐츠                             │
└──────────┴─────────────────────────────────────────┘
```

### 0.2 상단 헤더
- 좌측: 로고 (보라색 라운드 사각형 아이콘 + "reelbox.ai" 인디고 텍스트) → 자체 서비스명으로 교체
- 우측: 알림 벨 아이콘(빨간 뱃지에 미확인 수), 프로필 아바타(인디고 원형), 드롭다운 화살표

### 0.3 좌측 사이드바 (약 280px, 접기 버튼 `<` 존재)
**메인 메뉴** (섹션 라벨 소문자 회색)
- 대시보드
- 프로젝트 관리 ← 기능 1
- 스케줄 관리
- 아이디어 뱅크
- 성과 분석 (아코디언 ▼)
  - YouTube 분석
  - 파트너스 수익
  - 유튜브 데이터 분석 ← 기능 2

**하단 고정 메뉴**
- 채널 설정
- 구독 관리
- 이용 약관
- 이용 가이드 (아코디언: 가이드 페이지, AI 어시스턴트)
- 계정 카드: 아바타 + 표시명 + 이메일 + 온라인 녹색 점

**상태 표현**: 활성 메뉴는 연회색 라운드 배경. 아이콘 + 텍스트 조합.

### 0.4 공통 컴포넌트 패턴
- **주요 버튼**: 인디고(#6366F1 계열) 배경, 흰 텍스트, 라운드 8px
- **안내 배너**: 연한 인디고 배경, 필름 아이콘 + "이 기능이 처음이신가요? 영상으로 알아보기 >" + 우측 × 닫기
- **페이지 타이틀 영역**: 큰 제목 + ⓘ 물음표 아이콘 + `가이드` 버튼(점선 테두리 pill) + 부제(회색 설명문) + 우측 주요 액션 버튼
- **설정 안내 박스**: 💡 아이콘 + 연파랑 배경 "여기서 설정한 값은 채널 기본값으로 저장되며, 프로젝트 상세 화면에서 재정의할 수 있습니다."
- **폼 하단 액션바**: 좌측 `복제` `삭제(빨강 아웃라인)` / 우측 `취소` `저장(인디고)` — 보기 모드일 땐 우측이 `편집` 버튼

---

## 1. 프로젝트 관리 (목록 화면)

### 1.1 타이틀 영역
- 제목: "프로젝트 관리" + ? + 가이드 버튼
- 부제: "YouTube 비디오 프로젝트를 생성하고 관리합니다"
- 우측: `☐ 현재 페이지 전체 선택 (0/12)` 체크박스 + `+ 새 프로젝트` 버튼
  - 전체 선택 카운터는 (선택 수/현재 페이지 카드 수) 형식 → 일괄 작업(삭제 등) 전제

### 1.2 검색·필터 카드 (흰 배경 라운드 카드)
- 1행: 검색 입력(돋보기 아이콘, placeholder "프로젝트 검색...") + 드롭다운 3개
  - `모든 채널` / `모든 상태` / `모든 포맷` ← ⚠️ 세 번째 필터는 "생성 방식"이 아니라 **영상 포맷(숏폼/롱폼)**
- 2행: 정렬 드롭다운 `최신순`

### 1.3 프로젝트 카드 그리드 (3열, 반응형)
카드 구성 (위→아래):
1. 헤더 행: ☐ 체크박스(좌) / 채널명 pill 뱃지(연인디고) / 상태 뱃지(우, 예: `✓ 업로드 완료` 연녹색) / 필요 시 ⋮ 메뉴
2. 제목 (볼드, 1줄 말줄임)
3. 생성일 (예: "2026년 7월 18일")
4. 설명 (예: "제11화: 혈관 건강 및 고혈압 예방 편")
5. 진행률: "진행률" 라벨 + 우측 % 텍스트 + 인디고 진행 바
6. 파이프라인 단계 아이콘 5개 (📄스크립트 → 🖼️이미지 → 🎙️TTS → 🎞️편집 → 📹영상) — 완료 단계는 색상 표시
7. 검수 상태 박스 (연회색): "검수 상태" 라벨 + `검수 완료` 뱃지 + `☑ 검수 완료` 체크박스 (토글)
8. 우하단 `작업 계속` 버튼 (인디고, 소형) → 프로젝트 상세 편집으로 이동

카드 테두리: 인디고 얇은 보더. 페이지네이션 존재(전체 선택 문구의 "현재 페이지" 표현으로 확인).

### 1.4 새 프로젝트 생성 모달
필드 (위→아래):
1. `채널 선택` — 드롭다운 (등록된 채널 목록)
2. `프로젝트 제목 (필수입력)` — placeholder "예: 2024년 1월 콘텐츠"
3. `설명 (선택)` — textarea, placeholder "프로젝트에 대한 간단한 설명..."
4. `영상 포맷` — 카드형 라디오 2개
   - 숏폼 (9:16) — "최대 180초" / 선택 시 인디고 보더+연인디고 배경
   - 롱폼 (16:9) — "최대 1800초 (30분)"
   - 기본 선택값은 채널 설정의 비디오 포맷
5. 하단 우측: `취소` `생성`

---

## 2. 채널 설정 (= 파이프라인 기본값의 본체)

> ⚠️ 구조적 핵심: 스크립트/이미지/TTS/영상/업로드의 모든 기본값은 **채널 단위**로 정의된다.
> 프로젝트는 생성 시 채널 기본값을 상속하고, 프로젝트 상세에서 항목별 재정의(override)한다.
> 데이터 모델에서 ChannelSettings(기본) ↔ ProjectSettings(override, null이면 상속) 구조 필수.

### 2.0 화면 구조
- 타이틀: "채널별 설정 관리" + 부제 "각 채널별로 콘텐츠 생성 설정을 관리합니다" + 우측 `+ 새 채널 추가`
- 좌측: **채널 목록 패널** (고정폭 카드)
  - 헤더: "채널 목록" + `4/10개` 카운터 뱃지(보유/최대 슬롯) + "4개 표시"
  - 검색: "채널 검색..."
  - 필터 칩: `전체` `쇼핑` `일반`
  - 채널 아이템: ⠿ 드래그 핸들(순서 변경) + 채널명 + `YouTube` 뱃지 + `0/1` 카운터 + 채널 ID(말줄임) + 우측 녹색 상태 점
  - 선택된 아이템: 연인디고 배경
- 우측: **설정 패널** — 탭 8개: `일반` `스크립트` `이미지` `TTS/BGM` `영상 설정` `쇼핑` `업로드`
- 새 채널 추가 시: 이름 "새 채널 {현재일시}", ID "temp-{timestamp}"로 임시 생성 → YouTube 연동 시 자동 치환

### 2.1 [일반] 탭
| 항목 | UI | 비고 |
|---|---|---|
| 채널 이름 | 텍스트 입력 | "연결된 YouTube 채널명입니다. 필요시 수정할 수 있습니다." |
| 채널 ID | 텍스트 입력(읽기성) | "YouTube 계정에서 자동으로 가져온 정보입니다." 녹색 힌트 |
| 국가 (Country) | 드롭다운, 기본 한국(KR) | "국가를 설정하면 스크립트, TTS 음성, 썸네일 폰트, 업로드 언어 등 콘텐츠 생성 전체가 해당 국가에 맞춰 동작합니다." |
| 언어 (Language) | 드롭다운, 기본 한국어(ko) | "국가에 따라 자동 설정. 다국어 국가는 수동 변경 가능" |
| 비디오 포맷 | 카드 라디오: 숏폼(9:16, 세로 1080x1920, 최대 180초) / 롱폼(16:9, 가로 1920x1080, 최대 1800초) | "채널의 기본 비디오 포맷. 프로젝트 생성 시 개별 변경 가능" |
| 채널 활성화 | 체크박스 | |
| YouTube 채널 연동 | 섹션 | 상태: `● 연결됨 {일시}` 또는 `● 미연결` / "간편 연결 방식" 안내 박스 / 버튼: `YouTube 계정 간편 연결`(미연결 시) 또는 `YouTube 계정 재연결` + `연결 해제`(연결 시) / 연결 시 녹색 "✓ YouTube 채널 연결됨" 배너 |

→ 구현: Google OAuth 2.0 (youtube.upload + youtube.readonly scope). 연동 성공 시 채널명/ID 자동 입력.

### 2.2 [스크립트] 탭
| 항목 | UI | 값/옵션 |
|---|---|---|
| 기본 생성 모델 | 드롭다운 | 예: "클로드 소넷 4.5 – 속도와 품질 균형, 기본값으로 권장" (모델 선택형. 자체 구현 시 Anthropic API 모델 선택) |
| 시험 생성 | 버튼 | 현재 설정으로 샘플 대본 미리 생성 |
| 웹 검색 기본값 | 박스: `사용` 체크 + 허용 도메인 입력 | "최신 자료가 필요할 때 자동으로 검색 결과를 프롬프트에 포함합니다." 도메인 쉼표 구분, 최대 20개 (예: youtube.com, korea.kr), `검사` `초기화` 버튼 |
| 대본 생성 프롬프트 | 대형 textarea, 0/4000자 | placeholder에 구조화된 예시(타깃/형식/톤/전달 목표/구조 상세) |
| 프롬프트 템플릿 칩 | 클릭 시 프롬프트에 반영되는 프리셋 칩 15종 | 리스트형, 설명형, 비교형, 미신 파헤치기, 결과 보여주기, 경고형, 왜?형, 가이드형, 사고실험형, 랭킹형, 스토리텔링형, 케이스 스터디형, 미니 다큐형, 인터뷰/대담형, 예측/전망형 |
| 부스터 칩 (CTR/유지율/참여) | 칩 9종 | 오픈루프, 호기심갭, 패턴인터럽트, 감정트리거, 구독유도, 댓글유도, 엔드스크린, SEO최적화, 숏폼루프 |
| 기본 생성 설정 | | 영상 길이(초): 60초 등 |

안내문: "이 프롬프트는 대본 생성 시 기본값으로 사용되며, 개별 영상에서 수정할 수 있습니다." / "핵심 메시지, 타깃 시청자, 전달 톤 등을 100자 이상으로 구체적으로 작성할수록 더 나은 스크립트가 생성됩니다."

→ 구현: 템플릿/부스터 칩은 사전 정의된 프롬프트 조각(snippet)을 시스템 프롬프트에 합성하는 방식.

### 2.3 [이미지] 탭
| 항목 | UI | 값/옵션 |
|---|---|---|
| 채널 기본 이미지 생성 모델 | 그룹형 드롭다운 | 3개 그룹으로 구분: **[상] 크레딧 적고 품질 우수, 대량생성 적합**(고속 경량 모델 2종: 초고속형 ~0.8초, 디테일형 ~1.4초) / **[최상] 고품질 적합**(표준 모델 4종: 한글 텍스트 정확도·사실감 중심 Low/Medium 등급, 표준형/고속형) / **[프리미엄] 초고품질**(최상위 2종: DSLR급 인물+한글 다수 정확 렌더링, 최고 품질형). 각 옵션에 이모지 + 한 줄 특징 설명 |
| 이미지 스타일 프리셋 | 드롭다운 12종 | 직접 입력(프리셋 사용 안 함), 사실적 사진(기본), 텍스트 없는 사실적 사진, 애니메이션, 지브리 스타일, 고흐 유화, 수채화, 동양 수묵화, 픽셀 아트, 사이버펑크, 판타지 아트, 빈티지 |
| 기본 해상도 | 드롭다운 | 1344×768 (16:9)(기본) / 1920×1080 (16:9 HD) — 포맷이 숏폼이면 세로 해상도 세트로 전환 필요 |
| 채널 기본 프롬프트 | textarea | "스타일 프리셋 선택 시 기본 프롬프트는 비활성화됩니다. 직접 스타일을 입력하려면 드롭다운에서 '직접 입력'을 선택하세요." (프리셋 선택 시 disabled). **공식 도움말 보강**: 영상 전반의 분위기·스타일을 **한 문장, 150자 이내, 영어로만** 입력 (예: "cozy cinematic lighting"). 특정 장면 묘사를 쓰면 그 장면만 생성될 수 있으므로 전체 분위기만. **실행 시 각 장면 프롬프트 맨 앞에 자동으로 prepend**되어 모든 이미지에 공통 반영 |
| 시험 생성 | 버튼 + `한→영 번역` 버튼 | "현재 채널 기본 프롬프트, 이미지 스타일 프리셋, 해상도로 미리 확인합니다." / "한글 또는 영어로 직접 작성하거나, 위의 한→영 번역 버튼을 사용하세요." |
| 시험 생성 프롬프트 | textarea 0/500자 | placeholder "원하는 장면을 입력하세요 (한글/영어 모두 가능)" |

→ 구현: 이미지 모델 어댑터 인터페이스로 추상화(모델별 API 상이). 개인용이므로 실제 사용할 모델 1~2개만 연결하고 나머지는 드롭다운에 disabled 표시해도 됨.

### 2.4 [TTS/BGM] 탭
**BGM 설정**
- `BGM 설정 관리` 버튼 → 모달 (아래 2.4.1)
- 미지정 시: "현재 채널 기본 배경음원이 지정되어 있지 않습니다."

**TTS 시험 생성 & 미리듣기**
- `시험 생성` 버튼
- 시험 생성 문장 textarea: 기본 "안녕하세요! 채널 기본 TTS 설정으로 생성한 미리듣기입니다." 34/50자 (최대 50자)

**기본 생성 모델 (프로바이더)** — 그룹형 드롭다운
- 기본 TTS: Google Cloud TTS / OpenAI Audio TTS / NAVER Clova Voice
- 프리미엄 TTS: ElevenLabs / Fish Audio(내음성 복제가능) / Supertone(내음성 복제가능) / Qwen3 TTS(내음성 복제가능)
- "프로바이더에 따라 설정 가능한 옵션이 달라집니다. 채널 기본값은 프로젝트 상세에서도 재정의할 수 있습니다."

**프로바이더별 옵션 (동적 폼 전환)** — ⚠️ 프로바이더 선택에 따라 하위 필드가 바뀜 (공식 도움말로 파라미터 스키마 확정)

*Google Cloud TTS* — "다양한 언어와 음색, 안정적·맞춤 설정, 비즈니스용"
| 파라미터 | 범위/옵션 | 기본값 |
|---|---|---|
| 음성 선택 | ko-KR-Wavenet-A(여, 기본·균형·명료) / B(여, 부드럽고 따뜻) / C(남, 생동감·표현력) / D(남, 차분·전문적) | A |
| 오디오 인코딩 | MP3(범용) / WAV(무압축) / OGG Opus(고효율 압축, 스트리밍) | MP3 |
| 말하기 속도 | 0.25 ~ 4.0 | 1 |
| 음높이(피치) | -20.0 ~ 20.0 | 0 |
| 볼륨 게인(dB) | -96.0 ~ 16.0 | 0 |

*OpenAI Audio TTS* — "문맥 파악, 자연스러운 억양·감정, 장문에 강함"
| 파라미터 | 범위/옵션 | 기본값 |
|---|---|---|
| 음성 선택 | 도움말 6종: alloy(중립·부드러움)/echo(깊고 울림)/fable(따뜻·친근)/onyx(선명·강렬)/nova(밝고 활기)/shimmer(가볍고 맑음) — 스크린샷 기준 13종으로 확장됨(Ash, Ballad, Coral, Sage, Verse, Marin, Cedar 추가). 성별 라벨 병기 | Echo |
| 모델 | GPT-4o mini TTS(빠르고 자연) / tts-1(최고 속도) / tts-1-hd(고음질) | GPT-4o Mini TTS |
| 오디오 포맷 | MP3 / WAV / OGG | MP3 |
| 속도 | 0.25 ~ 4.0 — ⚠️ GPT-4o mini TTS는 숫자 속도 미지원 → 음성 지시문으로 대체 | 1 |
| 음성 지시문 | 자연어 최대 200자. 말투·속도·감정·분위기 지시 (예: "조금 더 빠르고, 밝고 경쾌하게 말해줘") | 빈값 |

*NAVER Clova Voice* — "한국어 특화 발음·억양"
| 파라미터 | 범위/옵션 | 기본값 |
|---|---|---|
| 스피커(음성) | 100여 종 | - |
| 속도 | 0 ~ 15 (클수록 빠름) | **5** |
| 피치 | -5 ~ 5 | 0 |
| 볼륨 | -5 ~ 5 | 0 |
| 감정 | 기본/슬픔/기쁨 등 — *일부 음성만 지원* | 기본 |
| 감정 강도 | 감정 지원 음성에서만 조절 | - |
| 음색 | -5 ~ 5 (원래 톤과 다른 효과) | 0 |
| 끝음 | -5 ~ 5 (말끝 높낮이: ↑명확·강조 / ↓차분·부드러움) | 0 |

→ 구현: 프로바이더별 파라미터 스키마(JSON Schema)를 정의하고 폼을 스키마 기반으로 렌더링. 프로바이더마다 기본값·범위가 완전히 다르므로(속도만 봐도 GCP 0.25~4/기본1 vs Clova 0~15/기본5) 공통 필드로 뭉개지 말 것.

**세그먼트 단위 생성** (공식 도움말): 음성은 **문장별로 생성**되며 문장 단위 수정·재생성 가능. 재생성 버튼 → 설정창에서 모델·음성 등 변경 후 새로 생성. ▷ 재생으로 미리듣기.

#### 2.4.1 BGM 설정 모달
- 헤더: 🎵 BGM 설정 + "채널 자동 생성 시 사용할 기본 배경음원을 선택하고 (영상 길이에 맞춰 자동으로 반복 재생됩니다) 세부 설정을 조정하세요." + × 닫기
- 상단 안내 바: "채널 기본값으로 저장됩니다. 프로젝트 상세 화면의 TTS/BGM 탭에서 개별 프로젝트별로 재정의할 수 있습니다." + 우측 `내 음원 업로드` 버튼 + "사용 중: 0/30, 남은 슬롯: 30개"
- 배경음원 선택: 탭 `시스템 음원 101` / `내 음원 0`
- 음원 카드 (2열 그리드): 곡명 + `기본 제공` 뱃지 + 설명("무료 BGM – …") + 길이(예: 82초) + "자동 반복 재생" + `▷ 미리듣기` 버튼("82초 미리보기") + 재생 진행바(0:00 / 1:22)
- 선택된 카드: 인디고 보더
- 우측 세부 설정 패널: "음원은 영상 길이에 맞춰 자동으로 반복 재생됩니다." / 재생 속도 슬라이더(느리게~빠르게, 표시 1.0x) / 볼륨(dB) 슬라이더(-22dB ~ +15dB, 표시 0.0dB) + "권장 볼륨: -22dB ~ +15dB (TTS와 자연스럽게 어우러지는 범위)"
- 푸터: "시스템 기본 음원은 자동 생성 파이프라인에서 모든 사용자에게 제공됩니다. 개별 프로젝트에서 재정의 시 채널 설정보다 우선합니다." + 버튼 `사용 안 함` `취소` `저장`

→ 구현: 시스템 음원은 로열티프리 음원 몇 개를 storage에 두는 것으로 대체. 내 음원 업로드(mp3, **최대 30개**) 기능 포함. (공식 도움말은 시스템 음원 "30여 가지", 스크린샷 카운터는 101 — 지속 추가되는 목록이므로 개수는 데이터 기반으로)

### 2.5 [영상 설정] 탭 — 내부 서브탭 4개: `영상 효과` `자막` `후킹멘트` `영상 길이`
(후킹멘트 탭은 비활성 상태로 관찰됨 — 조건부 활성으로 추정)

#### 2.5.1 영상 효과 서브탭
- 섹션 설명: "채널에서 생성되는 이미지 슬라이드 영상에 기본 적용되는 전환·줌·패닝 효과를 구성합니다. 프로젝트 상세 페이지에서 필요할 때 영상별로 오버라이드를 적용할 수 있습니다."
- 기본 영상 효과 드롭다운 14종: 없음, 페이드(기본), 컷, 슬라이드(좌), 슬라이드(우), 슬라이드(상), 슬라이드(하), 와이프(좌), 와이프(우), 와이프(상), 와이프(하), 대각선(좌상), 대각선(우하), 흑색 페이드, 백색 페이드
  - 선택 효과 설명문 표시 (예: 페이드 — "이미지가 서서히 어두워지며 다음 이미지로 전환됩니다. 가장 자연스럽고 부드러운 효과입니다.")
  - 주황색 주석: "* 전환 효과는 각 이미지의 끝부분에 적용되어 다음 이미지로 자연스럽게 연결됩니다."
- 전환 지속 시간(초): 슬라이더 + 숫자 입력, 범위 0.1~2.0(스크린샷) / 공식 도움말은 0.1~5초 — 버전 차이, 구현은 0.1~5초로 여유 있게
- `☐ 패닝 효과` — "장면을 좌우·상하로 천천히 이동시키며 움직임을 줍니다." 체크 시 방향·속도 선택 노출. 방향 `랜덤`이면 이미지마다 다른 방향
- `☐ 줌 효과` — "이미지의 주요 피사체를 강조하기 위해 확대·축소 동작을 더합니다." **줌 강도 권장 최대 1.5, 2.0 초과 시 과도** (공식 도움말)
- 💡 패닝+줌 동시 사용 = **Ken Burns 효과** (정지 이미지에 생동감)
- `☑ 자동 효과 조정` (기본 체크, 연파랑 박스) — 선택한 기본 전환 효과의 **그룹 내 순환**으로 단조로움 완화. 공식 예시: 페이드 → 크로스페이드 → 디졸브 순환 / 슬라이드(좌) → (우) → (상) → (하) 순환 / 줌인 → 줌아웃 순환. (⚠️ 크로스페이드·디졸브는 드롭다운 14종에 없지만 순환 그룹 멤버로 존재 — 효과 그룹 테이블 별도 정의 필요)

→ 구현: FFmpeg xfade 필터(fade, slide*, wipe*, diagonal 등 매핑) + zoompan 필터(패닝/줌).

#### 2.5.2 자막 서브탭
- `☑ 자막 사용` 체크박스
- **텍스트 스타일**: 폰트 드롭다운(나눔고딕 등) / 글자 크기(8~120, 기본 24) / 글자 굵기 드롭다운(굵게 — "이 폰트는 굵기 변경을 지원하지 않습니다" 조건부 주석) / 최대 줄 길이(10~80, 기본 40)
- 글자 색상: 컬러 스와치 + HEX 입력 (#FFFFFF)
- **배경 스타일**: 배경 색상(#000000) / 배경 투명도 슬라이더(투명~불투명, 기본 50%)
- **테두리 & 위치**: 테두리 두께(0~20, 기본 2) / 테두리 색상(#000000)
- 세로 위치 Y (0~1080): 슬라이더+숫자(기본 540) — "자막 윗변 기준 · 0=상단, 540=중앙 (1080은 화면 밖)"
- 가로 위치 X (0~1920): 슬라이더+숫자(기본 960) — "0=왼쪽, 960=중앙, 1920=오른쪽"
- **애니메이션 효과**: 페이드 인(0~5초, 기본 0.2) / 페이드 아웃(0~5초, 기본 0.2)
- **우측 실시간 미리보기**: "미리보기 텍스트 (줄바꿈: Enter)" textarea + 검정 캔버스(캔버스 영역 1920×1080 표기)에 현재 스타일·위치로 자막 렌더 + 좌표 표시 (960, 540) + "여기서 입력한 줄바꿈은 미리보기 참고용입니다. 실제 자막은 '최대 줄 길이' 설정에 따라 자동 줄바꿈됩니다."

→ 구현: 미리보기는 HTML Canvas로 렌더. 실제 번인은 FFmpeg drawtext/ASS 자막 매핑.
**공식 도움말 보강**: 글자 크기 쇼츠 권장 40~55 / 배경 투명도 50%가 가독성·노출 균형 권장(자막 배경 미사용 시 '투명') / 테두리 두께 0=없음, 밝은 배경엔 검정·어두운 배경엔 흰 테두리 권장 / 페이드 인·아웃 0~2초(도움말) vs 0~5초(스크린샷) — 구현은 0~5초 / **줄 간격 배수** 필드 존재(권장 1.3~1.6, 스크린샷 미노출)
**⚠️ 좌표계는 포맷 종속**: 도움말(숏폼 기준)은 세로 위치 0~1920(중앙 960)·가로 0~1080(중앙 540), 스크린샷(롱폼 기준)은 세로 0~1080(중앙 540)·가로 0~1920(중앙 960). 즉 캔버스 해상도(1080×1920 vs 1920×1080)에 따라 슬라이더 범위·중앙값이 전환됨 → 좌표 범위를 하드코딩하지 말고 포맷의 width/height에서 파생. 쇼츠는 하단 검정 영역 배치 권장, 너무 낮으면 쇼츠 제목 표시 영역과 겹침 주의

#### 2.5.3 후킹멘트 서브탭 (공식 도움말 기반 — 스크린샷에선 비활성 상태였음)
- `후킹멘트 표시` 활성화 토글: ON이면 스크립트에서 생성된 후킹멘트를 영상 시작 부분에 오버레이
- 위치 설정 (슬라이더): 가로 0~1080(중앙 540) / 세로 0~1920(중앙 960) — **상단 검정 영역(세로 0~300) 배치 권장** (숏폼 기준)
- 스타일: 글자 크기 20~80(권장 40~55) + 글자 색상 피커 / 테두리 두께 0~5 + 테두리 색상 피커
- 표시 시간: 슬라이더 — **0초 = 영상 전체에 표시**
- 후킹멘트 입력란(스크립트에서 자동 입력, 수정 가능) + 우측 실시간 미리보기
- ※ 서브탭이 비활성이었던 이유 추정: 후킹멘트 텍스트가 비어 있거나 표시 OFF 상태일 때 조건부 비활성

#### 2.5.4 영상 길이 서브탭 (공식 도움말 기반)
- 목표 영상 길이: 30~90초. 이 값에 맞춰 영상 길이·음성 재생 속도·이미지 수·전환 타이밍이 자동 조정됨
- `재생속도 자동 조절`: TTS 총 길이와 목표 길이가 다를 때 음성 속도를 **0.5x~2x 범위에서 자동 조절**해 맞춤
- `짧은 영상 늘리기`: 생성 영상이 목표보다 짧으면 재생을 느리게 해 목표 시간에 맞춤. 과도한 감속은 음성 부자연 경고. **'재생속도 자동 조절' 활성 시에만 사용 가능** (종속 옵션)
- ✔️ `채널 기본값으로` 버튼: 채널 설정값으로 리셋 (영상 설정 탭 공통)

### 2.6 [쇼핑] 탭 — 쿠팡 파트너스 연동
- 우측 상단 `프로필링크` 버튼
- 쿠팡 채널 ID (Tracking Code) 입력 — "쿠팡 파트너스 대시보드 > 채널 관리에서 생성한 8~20자의 영문/숫자 조합 ID를 입력하세요." + 미입력 시 빨간 검증 문구
- 영상당 생성할 링크 수: 숫자 입력 (1~10, 기본 3) — "자동 생성 시 최신 영상당 추가할 상품 링크 수"
- Access Key / Secret Key 입력
- `☐ 쇼핑 제휴 자동 추천 활성화` + 안내: "쿠팡 채널 ID와 API 키가 저장되어 있어야 자동 추천이 동작합니다." "자동으로 추가된 링크는 상품 링크 관리에서 언제든지 수정/삭제할 수 있어요."
- 보안 안내 박스: "저장된 값은 보안상의 이유로 표시되지 않습니다. 새 값을 입력하면 즉시 덮어씁니다."

→ 구현 우선순위 낮음(개인 필요 시). 키는 서버측 암호화 저장, 마스킹 표시 패턴만 동일하게.

### 2.7 [업로드] 탭
| 항목 | UI | 값 |
|---|---|---|
| 기본 카테고리 | 드롭다운 | YouTube 표준 카테고리 16종: 영화 및 애니메이션, 자동차 및 차량, 음악, 애완동물 및 동물, 스포츠, 여행 및 이벤트, 게임, 사람 및 블로그, 코미디, 엔터테인먼트, 뉴스 및 정치, 노하우 및 스타일, 교육(기본), 과학 및 기술, 비영리 및 사회운동 |
| 태그 (쉼표로 구분) | 입력, 0/200자 | placeholder "예: 교육, 키워드1, 키워드2". **병합 순서: 채널 기본 태그가 먼저, 프로젝트 태그가 뒤에 이어붙음. 동일 태그는 한 번만 전송(dedup)** |
| 태그 자동 생성 | 체크박스 + `자동 태그 수` 숫자(기본 10) | AI가 스크립트 기반 태그 생성. **자동 태그는 기본 태그 뒤에 이어붙음. 권장 10개 내외, 최대 20개** |
| 제목 템플릿 | 입력 | 기본 `{{projectTitle}}`. 변수: {{projectTitle}}, {{channelName}}, {{date}}. 예: `{{projectTitle}} | {{channelName}}`. **프로젝트 업로드 탭 제목이 비어 있을 때만 적용**(입력 시 그 값이 우선) — 4.6 제목 캐스케이드와 일치 |
| 설명 (Description) | textarea | 인사말·채널 소개 등 모든 영상 공통 문구. **최종 설명 = 채널 기본 설명(앞) + 프로젝트 업로드 탭 입력(뒤 이어붙음), 줄바꿈·공백 유지** |
| 설명 자동 생성 | 체크박스 + 추가 지시 입력(0/100자) | AI가 스크립트 기반 **약 200자 내외** 요약 생성 → **기본 설명 다음에 줄바꿈 2회 후 이어붙음**. 지시 입력란에 스타일 지정 가능(예: "간결하면서도 이해하기 쉽게 요약해주세요", "구독과 좋아요로 이어지게 유도해 주세요"). 비워도 무방 |
| 공개 설정 | 드롭다운 | 공개(누구나) / 미등록(검색 불가, URL 접속 가능) / 비공개(본인만, 기본) |
| 스크립트 중복 검사 | 체크박스 (도움말에서 확인, 스크린샷 미노출) | ON 시 **최근 생성된 30개 프로젝트의 제목을 참고해 중복되지 않는 스크립트를 요청**. AI 성능상 100% 보장 안 됨 안내 → 구현: 스크립트 생성 프롬프트에 최근 30개 제목 목록을 "피해야 할 주제"로 주입 |

---

## 3. 구현 노트 (UI 재현 관점)

1. **채널 설정 → 프로젝트 상속 구조가 최우선 설계 포인트.** settings JSON을 채널/프로젝트 2계층으로 두고, 조회 시 merge(project ?? channel)로 해석.
2. **동적 폼**: TTS 프로바이더, 이미지 모델, 스타일 프리셋 선택에 따라 하위 필드 구성이 바뀌는 패턴이 반복됨 → 프로바이더별 스키마 정의 + 조건부 렌더링 공통 컴포넌트로.
3. **시험 생성(preview) 패턴이 모든 생성 탭에 존재** (스크립트/이미지/TTS): 채널 설정만으로 소량 샘플을 즉시 생성·확인하는 UX. 재현 필수.
4. **그룹형 드롭다운** (이미지 모델의 [상]/[최상]/[프리미엄], TTS의 기본/프리미엄): optgroup + 옵션별 한 줄 설명 텍스트.
5. **슬라이더+숫자 입력 페어**가 표준 패턴 (전환 시간, 자막 위치, BGM 볼륨 등).
6. 개인용이므로: 구독 관리/크레딧/슬롯 제한(채널 4/10개, BGM 0/30 등) UI는 생략하거나 상수로 대체 가능. 표시만 유지하고 싶으면 설정값으로.

---

## 4. 프로젝트 상세 편집 화면

### 4.0 화면 골격
- **헤더**: ← 뒤로가기 + 프로젝트 제목(대형 볼드) + `검수 완료 {일시}` 연녹색 뱃지 + 우측 `게시됨` 상태 텍스트. 아래줄: ? 아이콘 + `가이드` 버튼 + 프로젝트 설명(부제)
- **전체 진행률**: "전체 진행률" 라벨 + 우측 % + 인디고 풀폭 진행 바
- **파이프라인 단계 아이콘 6개** (가로 등간격): `스크립트` `이미지` `TTS/BGM` `영상` `썸네일` `업로드` — 완료 단계는 연녹색 사각 아이콘 배경 (⚠️ 단계는 6개: 썸네일 포함)
- **검수 관리 패널** (접이식 카드): ▼ 토글 + "검수 관리" + `검수 완료` 뱃지 + "클릭하여 펼치기"
- **탭 바**: `스크립트 ●` `이미지 ●` `TTS/BGM ●` `영상 ●` `썸네일 ●` `업로드 ●` — 각 탭명 옆 상태 점(완료=녹색). 활성 탭 인디고 + 하단 보더

### 4.1 [스크립트] 탭 — 스크립트 관리
- 헤더: "스크립트 관리" + 우측 `⟳ 전체 스크립트 재생성` 버튼(인디고)
- 카드 상단: "생성일: {일시}"
- **섹션 카드 4종** — 각 섹션은 연파랑 헤더 바(파란 라벨) + 우측 아이콘 3개(✏️ 편집 / ⟳ 재생성 / ⧉ 복사):
  1. `제목` — 단일 입력. 편집 모드 진입 시 아래에 `저장`(인디고)/`취소` 버튼 표시
  2. `후킹멘트` — 단일 입력 (영상 시작 부분에 표시되는 문구)
  3. `대본` — 대형 textarea (문단 단위 스크롤)
  4. `이미지 프롬프트 (N개)` — 번호 뱃지(#1~#N) + 항목별 textarea + 항목별 🗑️ 삭제 + 항목 하단 파란 안내문 "나노바나나 시리즈(표준/고속/프로)는 한글 프롬프트를 지원합니다." (선택 이미지 모델에 따른 조건부 안내)
     - 리스트 하단: 숫자 입력 + `프롬프트 추가` 버튼 + `(12/900)` 카운터 → 최대 900개
     - 편집 모드 하단: `저장` `취소`
- 카드 우하단: `✓ 생성 완료` 상태 텍스트
- **개념**: 대본에서 장면(씬)별 이미지 프롬프트가 파생되며, 각 프롬프트가 이미지 1장 = 타임라인 이미지 클립 1개에 대응

#### 4.1.1 새 스크립트 생성 폼 (공식 도움말 기반)
| 항목 | UI/제약 | 비고 |
|---|---|---|
| 주제 | textarea, **최소 100자 ~ 최대 2000자** (100자 미만이면 생성 버튼 비활성) | 이 내용 기반으로 제목+후킹멘트+대본이 함께 생성됨. "제목, 타깃, 형식, 톤, 전달 목표 등 구체적으로 작성할수록 결과가 좋아집니다." |
| 채널 기본 프롬프트 포함 | 체크박스 | ON: 채널 설정의 대본 생성 프롬프트를 합성해 일관된 톤 유지 / OFF: 주제 입력만으로 생성 |
| 영상 길이 조절 | 슬라이더 **30초~90초** | (채널 기본값 60초와 연동. 숏폼 기준) |
| 이미지 프롬프트 수 | 숫자 **1~30개** | 생성 시 만들 장면 수. (생성 후 수동 추가는 최대 900개까지 별도 카운터) |
| 사용할 LLM 모델 | 드롭다운 | 모델별 크레딧 단가 상이 → 자체 구현 시 Anthropic 모델 선택 + 예상 비용 표시 |
| 웹 검색 사용 | 체크박스 + 도메인 입력 | 쉼표 구분 **최대 5개**. 빈칸이면 공개 도메인 광범위 탐색. 로그인/유료 페이지 제외. 검색 1회당 추가 비용 → Anthropic web search tool로 재현 가능 |
| 트위터(X) 실시간 검색 | 체크박스 + 상세 설정 | 인기 게시물 검색. 좋아요/조회수/특정 계정 포함·제외 필터. **활성화 시 도메인 필터·시간 범위 필터는 비적용** → 개인용 재현 범위 외(X API 유료), 인터페이스만 |

#### 4.1.2 섹션별 편집 규칙 (공식 도움말 기반)
- **제목**: 핵심 키워드 포함 + 호기심 자극 + 간결. 우측 아이콘으로 수정/재생성
- **후킹멘트**: 영상 시작 3초 안에 관심을 끄는 문구. ✏️ 직접 수정 / ⟳ 재생성 — 재생성 시 **프롬프트를 입력하면 AI가 반영**해 새 문구 생성 (재생성 버튼에 지시문 입력 팝오버 필요)
- **대본**: 발화·자막의 원문. 직접 수정 또는 AI 재생성
- **이미지 프롬프트**: 스크립트 생성 시 자동 동반 생성. ⚠️ **영어로만 입력하도록 강제** ("이미지 생성 모델은 영어 기반") — 단, 일부 모델(나노바나나 계열)은 한글 프롬프트 지원 안내가 별도 표시됨. 한→영 번역 버튼과 연계

### 4.2 [이미지] 탭 — 이미지 관리
- 헤더: "이미지 관리" + 우측 `⟳ 전체 재생성`(인디고) + `⬆ 빈 카드 추가` 버튼
- `☐ 전체 선택 (0/12)` 체크박스
- **이미지 카드 그리드 (4열)**:
  - 썸네일 (16:9) + 좌상단 ☐ 체크박스 + 우상단 `#번호` 뱃지 + 좌하단 시간 구간 뱃지(예: `0s - 4s`, `4s - 8s` — 씬별 타임코드)
  - 호버 시 `원본 보기` 툴팁
  - 하단 액션 아이콘 5개: 👁️ 원본 보기 / ⬆ 업로드(교체) / ⟳ 재생성 / ✨ (AI 편집·개선) / 🗑️ 삭제
  - 프롬프트 요약 1줄(말줄임) + "드래그앤드롭으로 이미지 교체 가능"
- **이미지 전체 재생성 모달**:
  - 노란 경고 배너: "⚠ 기존 이미지가 모두 삭제되고 새로 생성됩니다."
  - 좌측 **장면별 프롬프트 조정**: `기본값 복원` 링크 / 안내 "같은 프롬프트로 재생성하면 거의 동일한 이미지가 만들어집니다. 새 장면을 원하면 한두 문장이라도 수정해 주세요." / `장면 1..N` 각각: `번역` 버튼 + textarea + 글자수(예: 536/500자 — 초과 표시 허용) + "디테일을 바꿀수록 다른 이미지가 생성됩니다." / 하단 주석 "* 채널 기본 프롬프트는 실행 시 자동으로 맨 앞에 붙습니다."
  - 우측 **이번 실행에 사용할 모델**: "채널 기본: {모델명}" 표시 + 모델 드롭다운 / **스타일 프리셋**: "채널 기본: {프리셋}" + `기본값 복원` + 드롭다운 / 채널 기본 프롬프트(프리셋 선택 시 비활성 안내) / 이미지 해상도 드롭다운
  - 파란 안내 박스: "재생성할 이미지 · 총 12개 장면 — 채널 기본 프롬프트와 해상도가 기본값으로 적용되며, 이 모달에서 조정한 값은 이번 실행에만 우선 적용됩니다. 동일한 옵션은 개별 이미지 재생성 모달에서도 수정할 수 있습니다."
  - 불릿: 기존 이미지 모두 삭제 후 새로 생성 / 동일 프롬프트라도 다른 이미지 생성 / 재생성 시간은 이미지 수에 따라 다름
  - 하단: `취소` `⟳ 재생성 시작`
  - → **오버라이드 계층 3단 확인**: 채널 기본값 < 프로젝트 설정 < "이번 실행" 일회성 설정
- **개별 이미지 재생성** (공식 도움말 기반): 재생성 버튼 → 프롬프트 입력 시 AI가 반영해 새 이미지 생성. "동일한 프롬프트로 재생성하면 거의 같은 결과물" 안내(시드 고정 또는 유사 동작) → 새 결과를 원하면 한두 문장 수정 유도. 드래그 앤 드롭으로 로컬 이미지 직접 교체 가능
- **⚠️ 버전 차이 기록**: 공식 도움말의 모델 라인업(Swift Mode 균형형 / Cinema Mode 프리미엄 / Precision Mode 디테일형 / Nano Banana / Nano Banana Pro / Standard Mode / Studio Mode — Standard·Studio는 "한글 글자 깨질 수 있음" 명시)은 스크린샷의 라인업(Z-Image Turbo, FLUX.2, 오픈AI Low/Medium/High, Nano Banana 계열)과 다름. 해상도도 도움말은 "1024×1024 고정", 스크린샷은 1344×768/1920×1080 선택형 → **모델·해상도 목록은 수시로 교체되는 요소이므로 하드코딩하지 말고 DB/설정 기반 모델 레지스트리(표시명, 설명, 지원 해상도, 한글 지원 여부, 단가)로 설계할 것**

### 4.3 [TTS/BGM] 탭 — TTS/BGM 관리
- 헤더: "TTS/BGM 관리" + 우측 `🎙 음성 재생성`(인디고) / 아래 `🎵 BGM 설정 관리` 버튼
- **BGM 설정 카드**: `프로젝트 전용` pill 뱃지 + 곡명 + "1분 32초 · 자동 반복 재생" + 볼륨 0.0 dB + 재생 속도 1.0x + "이 프로젝트에서는 채널 기본값 대신 위 설정이 적용됩니다." + 우상단 "최근 업데이트: {일시}"
- 하단 캡션: "현재 적용: 프로젝트 전용 배경음원 – {곡명}"
- `☐ 전체 선택 (0/72)` — TTS는 **문장 단위 세그먼트**로 관리 (예: 72개)
- **세그먼트 카드 리스트**: `#번호` + 시간 구간(`0s - 5s`) + ✓ + `OpenAI` 프로바이더 뱃지 + 칩 3개(`gpt-4o-mini-tts` `echo` `1x`) + 문장 텍스트 + 버튼 `▷ 재생` `⟳ 재생성` `🗑 삭제`
- **TTS 재생성 모달** (전체 재생성):
  - 기본 생성 모델 드롭다운 + "프로바이더에 따라 지원 옵션이 다릅니다. 프로젝트별 기본값을 자유롭게 조정하세요. (채널 언어: ko 기준 7개 프로바이더 지원)"
  - 음성 선택 + ▷ 미리듣기 / 모델 / 오디오 포맷 / 음성 지시문(자연어, 0/200)
  - 불릿: 기존 음성을 모두 삭제하고 새로 생성 / 동일한 텍스트로 다른 음성이 생성됨
  - `☐ 세그먼트별 개별 음성 설정` (점선 박스 옵션 — 세그먼트마다 다른 목소리 지정 모드)
  - `취소` `▷ 생성 시작`
- **프로젝트 BGM 설정 모달**: 채널의 BGM 모달과 동일 구조이되 제목 "프로젝트 BGM 설정", 상단 안내 "프로젝트 전용 설정 — 저장하면 채널 기본값보다 우선 적용됩니다.", 푸터 "설정된 배경음원은 자동 생성 파이프라인에서 이 프로젝트에만 적용됩니다." + `사용 안 함` `취소` `저장`

### 4.4 [영상] 탭
- 상단 카드: 프로젝트 제목 + "영상 ID: {uuid 앞부분}..." + 우측 `🎬 타임라인 편집` 버튼(인디고)
- 결과 카드: `✓ 영상 생성 완료` + HTML5 비디오 플레이어(컨트롤: 재생, 되감기, 음소거, 타임코드 0:00/5:01, 배속 1x, 전체화면) + 하단 중앙 `⬇ 다운로드` 버튼(녹색)

### 4.5 [썸네일] 탭 (공식 도움말 기반 — 스크린샷 미수신)
- **배경 선택** 3가지 소스: ① 직접 이미지 업로드 ② 단일 색상 선택 ③ **현재 프로젝트에서 생성된 이미지 중 선택** — 우측 미리보기에 즉시 반영
- **텍스트 오버레이**: 체크박스로 on/off. 활성화 시 아래 설정 노출 + 미리보기 실시간 갱신
- **썸네일 텍스트 (캔버스 인터랙티브 편집)**:
  - 문구 입력 + 미리보기 캔버스에서 **드래그로 위치 이동**
  - 텍스트 박스 **모서리 핸들로 크기 조절**
  - **마우스 휠로 글자 크기 미세 조절**
  - Enter로 수동 줄바꿈
- **폰트 설정**: 크기 슬라이더(휠 확대/축소와 병행), 스타일(폰트), 글자 색상 / 테두리 적용 가능 — 텍스트·테두리 **각각 색상 지정**
- 최종 산출물: 포맷 해상도에 맞는 썸네일 이미지 파일 (숏폼 1080×1920 세로 or 롱폼 1280×720 표준 — 구현 시 YouTube 권장 1280×720 기준)
- **⚠️ YouTube 커스텀 썸네일 전제 조건**: 채널이 YouTube Studio에서 **중급 기능(전화번호 인증)** 을 활성화해야 커스텀 썸네일 업로드 가능. 미인증 시 영상 프레임이 썸네일로 자동 사용됨. UI에 인증 안내(studio.youtube.com → 설정 → 채널 → 기능 사용 자격 요건 → 중급 기능 → 전화번호 인증 6단계) + 인증 상태 확인 배너 포함
- → 구현: Fabric.js 또는 Konva 같은 캔버스 라이브러리로 텍스트 오버레이 편집기 구현. 업로드 시 YouTube Data API `thumbnails.set` 호출 (실패 시 미인증 안내)

### 4.6 [업로드] 탭 (공식 도움말 기반 — 스크린샷 미수신)
- **업로드 준비 상태 체크리스트**: 업로드 가능 조건을 시각적으로 표시 — ① 채널 자격 증명(OAuth) 연결됨 ② 필수 산출물 완비(**스크립트, 영상, 썸네일**). 미충족 시 업로드 버튼 비활성
- **제목** — ⚠️ 3단 우선순위 캐스케이드:
  1. 업로드 설정의 제목 입력값 (최우선)
  2. 비어 있으면 → 프로젝트 제목
  3. 프로젝트 제목도 없으면 → 채널 기본 설정의 제목 템플릿({{projectTitle}} 등) 렌더 결과
- **공개 설정**: 공개(누구나) / 미등록(검색 불가, URL 접속 가능) / 비공개(본인만) — YouTube privacyStatus public/unlisted/private 매핑
- **설명** — 병합 규칙: 사용자 입력값이 그대로 반영되되, **입력을 모두 지워도 채널 기본 설명 + 자동 설명(기능 활성 시)은 항상 포함되어 업로드됨**
- **태그** — 병합 규칙: 최종 태그 = **채널 기본 태그 + 자동 생성 태그(기능 활성 시) + 사용자 추가 태그**. 프로젝트 화면에서는 기본/자동 태그를 삭제할 수 없음. 태그 없이 올리려면 채널 설정 업로드 탭에서 기본 태그 비우기 + 태그 자동 생성 비활성화
- **예약 업로드 사용**: 토글 활성 시 날짜·시간 지정 → 해당 시각 자동 업로드 (YouTube API publishAt + privacyStatus=private 조합으로 구현)
- → 구현 노트: 제목 캐스케이드·설명/태그 병합은 **업로드 실행 시점에 서버에서 해석**하는 순수 함수로 구현하고, UI에는 "최종 업로드될 제목/설명/태그" 미리보기를 표시해 사용자 혼란 방지 (원 서비스는 이 미리보기가 없어 도움말로 설명하는 방식 — 개선 포인트)

---

## 5. 타임라인 편집기 (전체 화면, 다크 테마)

> ⚠️ 별도 라우트의 풀스크린 영상 편집기. 라이트 테마인 나머지 화면과 달리 **다크 테마**.
> 규모가 크므로 구현은 Phase를 분리할 것 (아래 §5.6 참고).

### 5.0 상단 툴바
- 1행: ← 뒤로 + 🎬 + 프로젝트 제목 + ? + `가이드` + 버튼 그룹: `📄 스크립트` `🖼 이미지` `🎙 TTS`(좌측 탭 전환/재생성 진입) | `유효성 검사` `품질 분석 ①`(뱃지=이슈 수) `라이브러리`(조건부 비활성) | `⟳ 동기화` `✨ AI 자동 편집` `🔊 자동 효과음` `⦿ 렌더링`(인디고 강조)
- 2행(재생 컨트롤 바): |◀ ▶ ▶| + 속도 슬라이더(1x) + 타임코드 `01:38:04 / 05:01:24` + 줌 -○+ (2%) + 우측: `ⓘ Preview Mode – 애니메이션/전환효과는 렌더링 후 확인` 안내 pill + `스냅` + ? + `⛶ 미리보기 최대화`

### 5.1 좌측 콘텐츠 영역 — 탭 4개: `스크립트` `자막` `미리보기` `완성본`
- **스크립트 탭**: 제목 / 후킹멘트(placeholder "영상 시작 부분에 표시되는 문구") / 본문(글자수 표시, 예: 1995자) / 이미지 프롬프트(12/900개) — 각 필드에 ⧉ 복사·⟳ 아이콘, 하단 `스크립트 저장` 버튼. (상세 편집 탭과 동일 데이터의 편집 뷰)
- **자막 탭**: `전체 선택` + 우측 `찾기` → `바꾸기` 입력 + `모두 바꾸기` / 자막 세그먼트 카드 리스트: `#번호` + `01:13.5 – 01:17.6` 시간 + 2줄 텍스트(편집 가능) + ↑ 이동 + × 삭제 / 하단 "총 70개 자막" / 현재 재생 중 세그먼트에 `재생중` 표시 + 하이라이트
- **미리보기 탭**: 현재 재생 위치의 합성 프리뷰(이미지+자막 오버레이). 우상단에 좌측탭 공통 타임코드 표시
- **완성본 탭**: 렌더링된 결과 비디오 플레이어

### 5.2 우측 속성(Properties) 패널 — 선택 컨텍스트 기반
- 미선택: "속성 — 클립을 선택하세요" + 안내 박스("클립을 클릭하여 선택하면 해당 클립의 속성을 편집할 수 있습니다. Ctrl+클릭으로 여러 클립을 선택할 수 있습니다.")
- 미선택 시 하단에 **전역 정보/설정** 노출:
  - **타임라인 통계**: 트랙 6개 / 총 클립 155개 / 길이 301.8초 / 칩: `이미지 13` `TTS 67` `BGM 5` `자막 70`
  - **스냅 설정**: ☑ 스냅 활성화 + 스냅 간격 슬라이더 100ms (10ms~1000ms)
  - **줌 레벨**: 현재 줌 % + 슬라이더(10%~1000%) + 프리셋 버튼 `50%` `100%` `200%` `400%`
  - **단축키** 안내 (Space 재생/일시정지 등)

#### 자막 클립 선택 시 — 서브탭: `기본 정보` `자막` `스타일` `효과`
- **기본 정보**: 클립 ID(sub_10...), 트랙(Subtitles), 시간(시작 00:33.232 / 끝 00:36.736 / 지속 3.504초(3504ms)), "시간 편집 (MM:SS.mmm 형식, Alt+방향키로 100ms 조절)" + 시작/종료 시간 입력
- **자막**: 자막 텍스트 textarea + "글자를 드래그하여 선택 후 아래 색상을 클릭하면 적용됩니다. 텍스트를 수정하면 색상이 초기화됩니다."(부분 색상 적용 기능) / **자막 재구성**: 기준 글자수 슬라이더(15자) + `자막 재구성 (R)` 버튼 — "입력한 글자수에 맞춰 의미가 통하는 단위로 분할합니다. 정확한 글자수가 아닌, 해당 글자수 내외의 자연스러운 끊김 기준입니다."
- **스타일**: `☑ 모든 자막에 스타일 적용`(체크 시 모든 자막 클립에 동시 적용) / 폰트 설정(폰트 드롭다운, 글자 크기 슬라이더 77px, 글자 색상 스와치+팔레트 8색+커스텀, 글자 굵기 — 미지원 폰트 주석) / 배경 설정(배경 색상, 배경 투명도 0.5) / 위치 설정(px 단위, 가로/세로 슬라이더+수치+`C` 중앙 정렬 버튼) / 테두리 설정(두께 2px, 색상, "테두리가 두꺼우면 자동 줄바꿈으로 2줄 이상일 때 윗줄·아랫줄 테두리가 서로 겹쳐 보일 수 있습니다...") / 레이아웃 설정(최대 줄 길이 30자 — "영상 폭 초과하는 자막은 자동으로 줄바꿈됩니다")

#### 이미지 클립 선택 시 — 서브탭: `기본 정보` `변환` `이미지 효과` `전환` `키프레임` `특수효과` `마스크`
- **기본 정보**: 클립 ID(img_...), 트랙(Image), 시간(시작/끝/지속 19.500초) + 시간 입력
- **변환**: `☐ 모든 이미지에 변환 적용` / 위치(-1~2 정규화 좌표) X 0.5, Y 0.5 + `C` 버튼 / 크기(100%) 스케일 1 / 회전(0deg): 각도 슬라이더+수치+프리셋 `0` `90` `180` `-90` + 주석 "베이스 트랙은 회전이 렌더링에서 적용되지 않습니다. 회전이 필요하면 오버레이 트랙에 배치하거나, 회전 키프레임을 사용하세요." / 투명도(100%) 불투명도 1 / 좌우 반전 (`반전 없음` 토글 버튼) / `기본값으로 리셋`
- **이미지 효과**: `☐ 모든 이미지에 효과 적용`(파란 박스, "체크 시 아래 효과 변경이 모든 이미지 클립에 동시 적용됩니다") / `☑ 패닝 효과` + 방향 드롭다운(`랜덤` 등) + "가로로 긴 이미지는 위/아래 방향 패닝을 사용할 수 없습니다" + 속도 드롭다운(`보통` 등) / `☑ 줌 효과` + 타입 드롭다운(`줌 인` 등) + 강도 슬라이더(1.3x) / **색보정**: 프리셋 버튼 그리드 20종 — 없음(기본), 시네마틱, 네오 느와르, 블루 스틸, 골든 아워, 레트로 필름, 세피아, 페이디드, 폴라로이드, 비비드, HDR 느낌, 팝 컬러, 따뜻한 톤, 차가운 톤, 황금 톤, B&W 클래식, B&W 하이콘트라스트, 필름 누아르, 소프트 B&W, 몽환적 / 슬라이더 4종: 밝기·대비·채도·색온도(각 기본 0) / 안내 "ⓘ 색온도는 미리보기와 최종 영상 색감이 조금 다를 수 있어요." / `기본값으로 리셋`
- **전환**: `☐ 모든 이미지에 전환 적용` / 전환 효과 그룹형 드롭다운 — 기본: 없음·페이드·컷 / 슬라이드: 좌·우·상·하 / 와이프: 좌·우·상·하 / 대각선: 좌상·우하 / 페이드: 흑색 페이드·백색 페이드 (채널 설정의 14종과 동일 세트) / 선택 효과 설명 박스(💡) / 주황 주석 "* 현재 클립에 전환 효과를 적용하면, 다음 이미지가 해당 효과로 나타납니다." / 지속 시간 슬라이더 100ms~2000ms(기본 1000ms, "1000ms (1.0초)" 표기) / `현재 설정: 페이드 / 1000ms` 요약 박스 / `기본값으로 리셋`
- **키프레임**: 상단 "현재 시점: 88.64s" + "클립 길이: 19.5s" + `모두 초기화` / 속성 4종 각각 값 + `[+]` 키프레임 추가 버튼: 위치 X(0.50), 위치 Y(0.50), 스케일(1.00x), 회전(0deg) / "* 타임라인에서 재생 위치를 이동한 후 [+] 버튼으로 키프레임을 추가하세요. 속성당 최대 8개까지 지원됩니다." / **키프레임 프리셋**: 강도 `약`/`강` 토글 + 대상 "이 클립에 적용" + 카테고리별 버튼 — 줌: `펀치 인` `팝 인` / 패닝: `팬 라이트` `팬 레프트` / 리듬/감성: `틸트 스윙` / 임팩트: `셰이크` `슬라이드 인` / "* 프리셋 적용 시 기존 키프레임은 덮어쓰기됩니다. 줌/패닝 효과는 자동으로 비활성화됩니다." "* 여러 클립을 Ctrl+클릭 또는 Ctrl+A로 선택하면 선택된 모든 클립에 일괄 적용됩니다."
- **마스크**: 초기 상태 `마스크 추가` 버튼(파랑) + "클립의 특정 영역만 보이게 만드는 잘라내기 영역을 추가합니다. 마스크는 클립과 함께 줌/이동/회전됩니다." / 추가 후: 모양 `사각형`/`타원` 토글 + 슬라이더: 위치 X 0.50 / 위치 Y 0.50 / 너비 0.50 / 높이 0.50 / 회전 0° / 페더링(경계 부드러움) 0px / 둥근 모서리 0% + `☐ 반전 (마스크 바깥만 보이기)` + `마스크 제거`(빨강 풀폭)
- (특수효과 탭은 미캡처 — 존재만 명세)

#### TTS 클립 선택 시 — 서브탭: `기본 정보` `오디오`
- **기본 정보**: 클립 ID(tts_...), 트랙(TTS), 시간(시작 00:10.000 / 끝 00:15.208 / 지속 5.208초) + 시간 입력
- **오디오**: 오디오 정보(`TTS` 뱃지 + 해당 문장 텍스트 표시) / `☐ 모든 TTS에 설정 적용`(녹색 박스) / **볼륨 설정**: `☐ 음소거` + 볼륨 슬라이더(1.00 = 100%) / **재생 속도** "(클립 길이가 함께 조정됩니다)": 슬라이더 1.00x + "기본 속도" / 안내 박스 "연결된 자막이 있습니다. 속도 변경 시 자막 클립 길이도 함께 조정됩니다." — ⚠️ TTS↔자막 클립이 연동 관계임을 시사

#### 비디오 클립 선택 시 — 서브탭: `기본 정보` `변환` `비디오 효과` `전환` `키프레임` `특수효과` `비디오 옵션` `마스크`
- **기본 정보**: 클립 ID + 트랙(Video 1) + 시간 편집 + **AI 영상 분석 카드**(보라 테마): "퍼온 영상으로 쇼츠를 만들 때 사용하는 기능입니다. 직접 촬영하거나 AI로 생성한 영상에는 불필요할 수 있어요." / "영상 음성·시각·viral 패턴을 종합 분석해 편집 포인트(후킹/컷/패턴)를 추출합니다. 첫 분석에 30~90초 소요." / 비용 안내(크레딧) / `✨ AI 영상 분석 시작` 버튼 → (재현 시 우선순위 낮음, 자리만 유지)
- **변환 / 전환 / 키프레임 / 마스크**: 이미지 클립과 동일 구조 (전환 설명문만 "이 클립이 서서히 투명해지며 사라집니다."로 클립 타입별 상이)
- **비디오 효과**: 이미지 효과의 색보정 파트와 동일(프리셋 20종 + 밝기/대비/채도/색온도) — 패닝/줌 섹션은 없음
- **비디오 옵션**: 재생 속도 "(클립 길이가 함께 조정됩니다)" 슬라이더 1.00x / 좌우 반전 `반전 없음` 토글 + "비디오를 좌우로 뒤집습니다 (미러 효과)"

#### 하단 상태바 (전체 정보)
`6 트랙` | `155 클립` | `1 선택됨` | `05:01:24` | `30 FPS` | `2%` | `중형 화면` — 렌더링 기준 30FPS 확인. URL 라우트: `/dashboard/projects/{uuid}` (편집기)

### 5.3 우측 끝 품질 분석 패널
- 헤더: "품질 분석 ①" + `모두 무시` `재분석`
- 이슈 카드(파란 보더): "ⓘ 이미지 12개에 전환효과가 없습니다" + "클립 간 연결이 잘려 보일 수 있습니다 (클릭하여 12개 클립 선택)" + "전환효과는 장면 전환을 부드럽게 합니다. fade(0.5~1초)가 가장 무난합니다" + 액션 버튼 `fade 효과 추가 (800ms)`(원클릭 자동 수정) + `무시`
- → 규칙 기반 린트(전환 누락, 오디오 공백 등) + 원클릭 수정 액션 패턴

### 5.4 하단 타임라인
- 트랜스포트 바: |◀ ▶ ▶| + 타임코드 `01:38.13 / 05:01.80` + ↶↷ 실행취소/재실행 + ✂ 분할 + ⧉ 복사/붙여넣기/복제 + 🗑 삭제 + 트랙 뷰 토글 3종 + 우측 줌 %
- 시간 눈금자 (00:00:00 ~ 10:30 이상, 30초 단위) + 빨간 플레이헤드(현재 위치 수직선)
- **트랙 6개** (위→아래): 
  1. `Subtitles` (T 아이콘) — 노란 클립 ~70개
  2. `Video 1` — 보라 클립 (인트로/오버레이 영상)
  3. `Image` — 파란 클립 (Scene 1, Scene 3 00:28.50, 00:26.12 … 이름+길이 표시)
  4. `TTS` — 초록 클립 ~67개
  5. `비디오 오디오` — 주황 (영상 소스 오디오)
  6. `BGM` — 주황 파형 클립 (곡명 + 01:32.00, 반복 배치 4개)
- 각 트랙 좌측 헤더: ⠿ 드래그 + 아이콘 + 트랙명 + ▲▼ 순서 + `+` / 하단열: 👁 표시 토글, 🔒 잠금, » 접기
- 각 트랙 우측: `+` 클립 추가, 스케일(⚖), 🗑 트랙 삭제
- 최하단: `+ 트랙 추가` / 상태바 "1 clip(s) selected · Zoom: 2% · Duration: 05:01.80"
- 선택된 클립은 흰 테두리 하이라이트. 클립은 드래그 이동/트림 가능(표준 NLE 인터랙션)

### 5.5 편집 규칙·동작 (공식 도움말 기반)
- **타임라인은 영상 자동 생성과 별개**: 채널 기본설정 기준으로 생성되며, 스크립트+이미지+TTS까지 완료되면 자동 영상 생성 없이 바로 편집 진입 가능
- **렌더링 필수**: 편집 내용은 렌더링해야 최종 영상에 반영. 편집 후 재수정 시에도 재렌더링 필요
- **자동 실시간 저장**: 편집 내용은 편집창 내에서 실시간 자동 저장 (우측 상단 저장 상태 표시 — 스크린샷의 "저장 안됨" 인디케이터와 일치). 단, 저장≠렌더링
- **트랙 기능**: 추가/삭제(트랙 삭제 시 내부 클립 전체 삭제), 잠금, 음소거, **갭 정리(빈 공간 제거)**, **트랙 목표 시간 맞추기**(원하는 재생 시간 입력 → 트랙 내 클립 길이 비례 조절)
- **클립 인터랙션**: 클릭 선택 → 우측 속성 패널 / 드래그 이동(**같은 트랙 내에서만**) / 양끝 조절바(↔) 드래그로 트림 / 재생바 위치 + 가위 버튼으로 분할 / Delete 또는 휴지통으로 삭제 / **우클릭 컨텍스트 메뉴**(복사/붙여넣기 — 붙여넣기는 재생바 위치에 삽입)
- **편집 화면 재생 속도**: 편집 프리뷰 전용, 실제 영상에는 무영향 (영상 속도는 클립별 옵션에서)
- **유효성 검사**: 렌더링 전 검증. 예: 최대 길이 90초 초과 시 렌더링 버튼 비활성 → 문제 해결 후 `검증 실행` → "타임라인이 유효합니다" 표시 시 렌더링 활성화
- **단축키 전체 목록**: 재생/일시정지 Space, 처음 Home, 끝 End / 복사 Ctrl+C, 붙여넣기 Ctrl+V, 잘라내기 Ctrl+X, 복제 Ctrl+D, 삭제 Delete / 클립 분할 C 또는 Ctrl+B / 실행 취소 Ctrl+Z, 다시 실행 Ctrl+Y 또는 Ctrl+Shift+Z / 타임라인 확대·축소 Ctrl+마우스 휠 / 다중 선택 Ctrl+클릭 / 컨텍스트 메뉴 우클릭

### 5.6 데이터 모델 시사점
```
Timeline { projectId, duration, tracks[] }
Track { id, type(SUBTITLE|VIDEO|IMAGE|TTS|AUDIO|BGM), name, order, visible, locked }
Clip { id, trackId, startMs, endMs, payload(타입별: 텍스트/이미지경로/오디오경로/스타일/변환값), effects[] }
```
- 스크립트 저장 → 자막·TTS·이미지 클립 자동 생성(동기화 버튼) → 타임라인에서 미세 조정 → 렌더링(FFmpeg) 순서
- "동기화" 버튼: 스크립트/에셋 변경분을 타임라인 클립에 반영하는 재생성 로직

### 5.7 구현 범위 권고 (개인용)
- **Phase A (필수)**: 자동 배치된 타임라인의 "읽기 전용 뷰" + 클립 시간/텍스트 수정 + 자막 스타일 전역 편집 + 렌더링. (트랙 자유 편집 없이도 파이프라인 완성 가능)
- **Phase B (선택)**: 클립 드래그/트림/분할, 스냅, 실행취소, 품질 분석 린트 + 원클릭 수정
- **Phase C (고급)**: 키프레임, 마스크, 특수효과, AI 자동 편집, 자동 효과음
- 라이브러리 후보: 타임라인 UI는 자체 구현(Canvas/DOM) 또는 오픈소스 타임라인 컴포넌트 활용. 렌더링은 서버측 FFmpeg 단일 경로 유지(프리뷰는 근사, 최종은 렌더 — 실제 서비스도 "Preview Mode – 전환효과는 렌더링 후 확인" 방식으로 프리뷰≠최종을 허용함).

---

## 6. 성과 분석 > YouTube 분석 (분석 & 모니터링)

> 기능 2(리서치 도구)와 별개의 메뉴. **내 채널에 실제 업로드된 영상의 성과**를 모니터링하는 화면.

### 6.1 헤더
- 제목 "분석 & 모니터링" + 부제 "실제 업로드된 영상의 성과와 트렌드를 확인하세요."
- 우측 컨트롤: `기간` 드롭다운("최근 30일 (Last 30 Days)" 등) + `채널` 드롭다운 + `⟳ 새로고침` 버튼(인디고) + ? + `가이드`

### 6.2 KPI 카드 3개 (가로 배치)
| 카드 | 아이콘 | 값 | 증감 |
|---|---|---|---|
| 총 조회수 (Views) | 👁 | 숫자 | 우하단 `↗ 0.0%` (녹색, 기간 대비 증감률) |
| 총 시청 시간 (Watch Time) | ▶ | 숫자 | 동일 |
| 구독자 (Subscribers) | 👥 | 숫자 | 동일 |

### 6.3 채널 영상 목록 (테이블)
- 헤더: "채널 영상 목록" + "모든 영상의 성과를 확인하고 정렬할 수 있습니다." + 우측 "전체 12개 영상"
- 정렬 칩 4종: `인기순 (Views) ↓`(활성) / `최신순 (Latest)` / `시청시간순 (Watch Time)` / `좋아요순 (Likes)`
- 검색: "영상 제목 검색..."
- 테이블 컬럼: `#` / `제목`(썸네일 + 제목 + "업로드: {날짜}" — 썸네일 없으면 플레이스홀더 아이콘) / `조회수 (VIEWS)` / `시청 시간 (WATCH TIME)`("0 분" 단위) / `좋아요 (LIKES)` / `업로드일 (PUBLISHED)` — 각 수치 컬럼 헤더에 정렬 화살표(⇅) / 우측 `자세히 보기` 링크
- → 구현: YouTube Analytics API(내 채널 OAuth 필요, Data API와 별도 scope: yt-analytics.readonly). 조회수·좋아요는 Data API videos.list로도 대체 가능하나 시청 시간은 Analytics API 필수

---

## 7. 유튜브 데이터 분석 (기능 2) — 사용 흐름 문서 기반 요소 명세

> 공식 "상황별 추천 사용 흐름" 6종에서 역추출한 각 메뉴의 UI 요소·기능. 스크린샷 수신 시 세부 레이아웃 보강 예정.

### 7.0 기능 2 전용 레이아웃 (스크린샷 확정)
- **서브 앱 구조**: 사이드바의 "유튜브 데이터 분석" 진입 시 자체 헤더 + 자체 탭 바를 가진 독립 화면
- 헤더: 🔍 아이콘 + "유튜브 데이터 분석" + 부제 "키워드 탐색, 경쟁 채널 분석, SEO 최적화, 자동화 등 채널 성장 종합 도구" + 우측 `활용 가이드`(녹색 점선 pill) `가이드`(점선 pill) + 히스토리(🕘) + 알림(🔔) 아이콘
- **탭 바 10개** (아이콘+텍스트): `홈` `소스 발굴` `쇼핑 소스 발굴` `탐색·분석` `채널 분석` `한·영 비교` `제목 변경 이력` `영상 SEO` `떡상 영상` `저장됨` — 활성 탭 인디고 하단 보더
- 상단 상태 바: `나의 니치:` 칩(예: 쇼츠·밈) + 우측 `크레딧 {잔액}` (주황 pill)
- 환영 배너(닫기 가능): "유튜브 데이터 분석에 오신 것을 환영합니다 — ... 상단 탭에서 원하는 기능을 선택하거나, 아래 AI 챗봇에게 자연어로 질문하세요." (AI 챗봇 진입 존재 시사)
- **자체 산출 지표 고지 배너** (결과 화면 공통, 노란색): "VPH(시간당 조회수)/V·S 비율/성능 등급은 자체 산출한 분석이며, YouTube 공식 지표가 아닙니다." (영문 병기) — 재현 시에도 동일 고지 유지 권장

### 7.1 흐름 문서에서 확정된 메뉴별 요소

**홈 — "오늘 뭘 만들지 막힐 때 여기서 시작"** (공식 문서로 상세 확정)
- **상단 프로필 헤더**: `나의 니치` 칩 목록(설정한 니치 카테고리 — AI 아이디어 품질에 직접 영향) + `크레딧 잔액`(현재 잔액 + 각 분석의 소모 예상 크레딧) → 개인용 재현 시 크레딧은 API 비용 추정치 표시로 대체
- **오늘의 AI 아이디어** (메인 영역): 니치 기반으로 **Claude가 매일 새 영상 주제·훅 후보 자동 생성** (최근 트렌드 요소 + 채널 특성 혼합). 매일 진입 시 자동 갱신
  - 아이디어 카드 구성: 주제 + **`훅:` 라인**(첫 3초용 문구가 바로 보임) + `[저장]` 버튼(→저장됨, 대본 생성 씨앗)
  - `다시 생성` 버튼: 1일 1회 무료 + 크레딧으로 추가 생성
  - **직접 입력 모드**: 토픽·타겟 청중·카테고리를 지정해 생성
- **우측 위젯 3종**:
  1. `떡상 빠른 진입` — 한국 서브카테고리 칩 모음. 클릭 시 떡상 영상 탭으로 이동 + 해당 니치 자동 필터 적용 (딥링크 + 필터 프리셋 전달)
  2. `니치 인기` — 내 니치 카테고리의 "지금 뜨는 영상" 미리보기
  3. `최근 저장` — 저장됨의 최근 아이디어 3~5개 미니 뷰 (주간 리뷰 진입점)
- **니치 설정 카드**: 니치가 0개인 신규 사용자에게만 표시, 1개 이상 설정 시 자동 소멸. 니치는 채널 설정에서 상시 수정 가능 (→ 데이터 모델에 UserNiche/ChannelNiche 엔티티 필요)
- 권장 루틴(공식): 매일 1분 — 홈 진입 → 아이디어 훑기 → 1~2개 저장 → 저장됨에서 대본 생성 → 주 1편 기획 자동 확보
- → 구현: 일일 아이디어는 cron(또는 첫 진입 시 lazy 생성) + Anthropic API. 니치·최근 트렌드(캐시된 인기 영상 제목들)를 프롬프트 컨텍스트로 주입
- **스크린샷 확정 보강**: 아이디어는 **1회 생성당 5개 시드**. 카드 헤더에 날짜+니치 표시, 모드 토글 `자동 (니치 기반)` / `직접 입력`, 우측 `오늘의 아이디어 생성`→생성 후 `⟳ 재생성` 버튼. 빈 상태 문구 "아직 오늘의 아이디어가 없습니다..."
  - **아이디어 카드 구조** (5필드 + 액션): 제목(밈체 허용) + 우측 `강추 {점수}` 뱃지(예: 강추 85 — 추천 점수) / `왜 좋은가:` 파란 하이라이트 라인(트렌드 근거) / `후킹:` 첫 3초 문구 / `차별화:` / `키워드:` / 우하단 `🔍 분석` `🔖 저장` 버튼
  - 하단 각주: "# AI 생성. 자체 산출 — YouTube 데이터/추천 알고리즘과 무관." (책임 고지)
  - 우측 위젯 실측: `빠른 떡상 발굴`(칩 8종: 주식·코인/자기계발·생산성/테크 리뷰/AI·생성형 도구/부동산/먹방·혼밥/연애·솔로지옥/브이로그·일상 + `전체 보기 →`) / `니치 인기 영상`(썸네일+제목+채널·조회수 리스트 5개 + `더보기 →`) / `최근 저장`(+`모두 보기 →`, 빈 상태 "저장된 아이디어가 없습니다.")

**탐색·분석 — "절대 기준 시장조사"** (공식 문서로 상세 확정. 서브 모드 2개)
- **탐색(browse) 모드 — "지금 뜨는 영상"**: 국가 + 카테고리 + 기간 내 조회수 상위 **최대 100개**
  - **API 매핑 규칙**: `기간 24h + 쿼리 없음` = **YouTube 공식 인기 차트**(videos.list chart=mostPopular, 정확도 최고) / 그 외 조합 = search.list 결과를 조회수 순 정렬 — 두 경로의 결과가 다름을 UI에 암시(주의사항에 명시됨)
  - 필터: 쇼츠/롱폼 비디오폼, 성능 등급(폭발적/상승세), 최소 조회수(1만+ 등)
  - 권장 5단계: 카테고리 선택(예: 26 노하우/스타일) → 기간 `7d`(24h는 짧고 30d는 넓음) → 성능 등급 폭발적+상승세 → 최소 조회수 1만+ (노이즈 제거) → 상위 10개의 훅·썸네일·주제 패턴 메모
  - 기간 옵션에 1시간/6시간 단위 존재 (쇼츠 바이럴 포착용, 롱폼엔 표본 부족으로 부적합 — 안내 필요)
- **분석(analyze) 모드 — "이 키워드의 시장성"**: 키워드 → 검색량·경쟁도·추천 점수 산출 (파생 지표)
  - **공식 공개된 산식: 추천 점수 = 검색량 점수 × (1 − 경쟁도)** — "검색 많은데 경쟁 덜한" 기회 키워드일수록 높음
  - **경쟁도** = 상위 영상 조회수 분포 기반 파생 지표. **70%+ 는 이미 포화** 기준선
  - **복수 키워드(bulk) 모드**: 한 번에 최대 10개 비교
  - `추천 키워드` 버튼: 같은 주제의 연관 키워드 자동 제안
  - 활용 흐름: 후보 5~10개 개별 분석 → 점수 비교 → 상위 키워드를 떡상 영상 키워드 모드로 재검색해 실제 성공 사례 확인
- **자동 키워드 확장** (browse/analyze 공통, 버튼 없이 자동 적용): 입력 키워드에서 **관련 검색어 3개 자동 생성 → 병렬 검색 → 결과 병합**. 영상 표본 최대 200개로 확대되어 검색량·경쟁도 신뢰성 상승 + 파생 채널 풀 확장. 결과가 너무 좁으면 키워드를 짧게 입력 유도(예: "재테크 신혼부부" → "재테크")
- **한국형 서브카테고리 칩**: 부동산/먹방/연애 등 한국 니치 1-click 진입. **YouTube 공식 카테고리 매핑이 부정확하므로 칩은 "키워드 검색 + 제목 매칭 필터" 조합으로 정확도 보강** — 칩별 (검색 키워드 세트, 제목 정규식/포함어) 매핑 테이블 필요
- → 구현 노트: 검색량 점수는 YouTube가 검색량을 직접 제공하지 않으므로 프록시(검색 결과 수·상위 영상 조회수 합·최신성)로 산출. 자동 키워드 확장은 Anthropic API로 연관어 3개 생성 → 병렬 search.list(캐싱 필수 — 확장 검색은 쿼터 4배 소모). 카테고리 ID는 YouTube 표준(26=노하우/스타일, 27=교육) 사용
- **스크린샷 확정 보강 (폼)**: 모드 토글 `탐색`/`분석` + 우측 크레딧 뱃지(탐색 50 크레딧). 체크박스 2종: `☑ 한국어만 (KR 전용)`(한국 검색 시 외국어 영상 제외) / `☐ 외국 영상 자동 번역`(제목 한국어 자동 변환). 폼 행: 국가 / 카테고리 / 비디오폼 / 기간 / 검색어(선택) / `탐색` 버튼
  - **카테고리 드롭다운에 평균 수익 병기**: "음악 · 평균 ₩2.0M / 게임 · 평균 ₩491K / 인물·블로그 · 평균 ₩104K / 엔터테인먼트 · 평균 ₩1.7M / 뉴스·정치 · 평균 ₩519K / 노하우·스타일 · 평균 ₩830K / 교육 / 과학기술 · 평균 ₩192K" — 카테고리별 평균 수익 추정치를 옵션 라벨에 표시
  - **비디오폼의 정확한 정의**: 전체(쇼츠+롱폼) / **쇼츠 = 180초 이하 + #shorts** / **롱폼 = 180초 초과 또는 시그널 없음**
  - 한국형 서브카테고리 칩 **17종 전체**: 주식·코인 / 자기계발·생산성 / 테크 리뷰 / AI·생성형 도구 / 부동산 / 먹방·혼밥 / 연애·솔로지옥 / 브이로그·일상 / 쇼츠·밈 / K-pop·아이돌 / 게임·리뷰 / 키즈·교육 / 뷰티·패션 / 여행·맛집 / 자동차 / 이슈·정치 시사 (+주석 "YouTube 공식 분류 외 한국 사용 패턴 시드")
  - 빈 상태: 안내문 + **추천 시작 예시 칩**(KR·24h·전체 / KR·게임·7d / US·24h / KR·노하우/스타일 — 클릭 시 폼 프리셋)
- **스크린샷 확정 보강 (결과 화면)**: "86개 결과 · 50 크레딧 차감됨" + `결과 저장` 버튼 / 자체 산출 고지 배너(§7.0)
  - **성능 등급 필터 칩 — 5단계 확정**: `전체 {n}` / `🔥 폭발적` / `상승세` / `안정 성장` / `장기 영상` / `정체` (각 칩에 건수 표시. "폭발적/상승세 = 지금 뜨는 영상")
  - 최소 조회수 필터: 전체 / 1천+ / **★1만+ (추천 별표)** / 10만+ / 100만+ / `☐ 채널당 1개만` 체크박스(채널 중복 제거)
  - **영상 카드 구성**: 썸네일 / 제목 / 채널명 / 뱃지 행: 성능 등급+**VPH**(예: "폭발적 · 시간당 1.4만회") + **추정 수익**(예: "추정 약 6.1만원") / 버튼 행 5종: `분석` `드릴다운` `자막 복사` `대본 패턴`(보라 강조) `번역` / 메타: 조회·좋아요·댓글 수 + "{n}시간 전" / 하단 풀폭 `저장` 버튼
  - **하단 "이 결과의 핵심 토픽"**: **TF-IDF 기반** 토픽 칩(예: 건강 ×23, 시니어건강 ×43...) + "클릭 시 분석 모드로 재검색" (탐색→분석 연결 동선)
  - → 구현: VPH = 조회수/경과시간, 추정 수익 = 조회수 × 카테고리별 RPM 추정치(카테고리 평균 수익 표시와 동일 테이블), 성능 등급 = VPH·경과일·조회수 조합 규칙, 핵심 토픽 = 결과 제목 코퍼스 TF-IDF 상위 n개

**떡상 영상 — "상대 기준, 재현 공식 발굴"** (공식 문서로 알고리즘 완전 확정 ⭐)
- 정의: **"자기 채널 평균(중앙값) 대비 N배 조회수"** — 핵심 지표명 `median_ratio`. 작은 채널의 기습 대박까지 포착
- **3개 모드**:
  1. `영상 단위(키워드)` ⭐ 기본값 — 키워드로 영상 검색 → 각 영상을 **자기 채널의 최근 업로드 50개 기준 중앙값(baseline)** 과 비교(기간과 무관하게 안정). **최대 100개 unique 채널**의 영상 통합 정렬. mostPopular 제약 없어 소형 채널 폭증까지 포착. 구체 주제에 적합
  2. `채널 단위(카테고리)` — 국가 + 카테고리 + 선택적 시드 키워드로 **상위 50개 채널 자동 선정** → 각 채널의 기간 내 떡상 통합 정렬. 시드 없이 카테고리만이면 공식 인기 차트 기반 상위 50채널. 니치의 대표 채널 파악용
  3. `채널 ID` — UCxxx 입력 → 그 채널의 최근 영상 중 떡상만. 경쟁자 1명 역설계용 (채널 분석 탭에서 ID 복사)
- **💎 숨겨진 보석 모드** (키워드·카테고리 모드 지원): 검색 행 우측 체크박스 ON + **구독자 상한 선택: 1만↓ / 5만↓ / 10만↓(기본) / 50만↓ / 100만↓**
  - 상한 초과 채널의 영상 전부 제외 + **구독자 비공개 채널도 제외**(대형 채널 오노출 방지)
  - **서버 선필터링** — 결과 전체가 조건 만족. 결과 내 `신생 강자` 배지(구독 1만 미만)는 별개의 **강조용 배지**로 구분
  - 활용: 내 채널 체급(예: 구독 5천이면 1만↓ 또는 5만↓)과 비슷한 채널의 재현 가능한 공식만 남김
- **threshold(배수) 설정**: 1.5배(느슨, 트렌드 훑기) / **2배(추천 기본값)** / 3배(강력 떡상) / 5배(메가 바이럴)
  - 결과 부족 시 완화 순서: **① 기간 확장(7d→30d) 먼저 ② threshold 완화(2→1.5)** ③ 키워드·필터 점검
- **자동 키워드 확장**: 관련 검색어 3개 자동 생성·병렬 검색·병합 (예: "다이어트" → "다이어트 식단/건강/간단") → 유니크 영상·채널 2~3배 확대. 자동 적용, 의도 밖 확장 섞일 수 있으니 결과 제목 훑기 안내
- **`[패턴 분석]` 버튼** (결과 상단): 상위 떡상 영상들의 **공통 훅·업로드 시간대·길이·주제를 AI가 자동 추출** — 개별 카드의 `[대본 패턴]`(훅 분석→내 스타일 대본 생성)과 구분
- **노이즈 필터 3종** (자동 적용):
  1. `median_ratio > 50배` outlier 자동 차단 (채널이 그 주제와 무관한 우연 케이스)
  2. 채널 표본 10개 미만이면 해당 채널 영상 skip (baseline 신뢰도 확보)
  3. view_count 1000 미만 제외 (스팸/테스트)
- 제목 키워드 매칭 필터: **AND 적용 + 공백 정규화**("해외 반응"↔"해외반응" 동일 처리)
- → 구현 노트: PROJECT_SPEC의 초기 알고리즘(최근 30개 평균)을 **최근 50개 median**으로 교체. 파이프라인: search.list(+확장 3쿼리) → 채널별 uploads 최근 50개 조회수 수집(캐시 필수) → median 산출 → ratio = 영상 조회수/median → threshold·보석 모드·노이즈 필터 적용 → ratio 내림차순. 채널당 API 비용이 크므로 채널 median은 24h+ 캐시
- **스크린샷 확정 보강**: 헤더 "⚡ 떡상 영상 발굴" + **모드 토글 3개**: `채널 단위 (카테고리)` / `영상 단위 (작은 채널 발굴)`(기본 활성) / `# 채널 ID` + 우측 **180 크레딧**
  - 설명문: "키워드로 영상을 직접 검색해서 각 영상이 자기 채널 평균 대비 몇 배 떡상했는지 분석합니다. **작은 채널의 폭증 영상도 발굴 가능**"(녹색 강조)
  - 폼 1행: 국가 드롭다운 / 키워드 입력 / threshold 드롭다운("**평균 2배 이상 (기본)**" 라벨) / 비디오폼(전체 쇼츠+롱폼) / 기간(최근 30일) / `☐ 💎 숨겨진 보석` 체크박스(주황 테두리) / `🔍 영상 떡상 발굴` 버튼
  - 보석 체크 시 구독자 상한 드롭다운 노출: 구독자 1만↓/5만↓/**10만↓(기본 ✓)**/50만↓/100만↓
  - 하단 신뢰도 주석: "※ 신뢰도: **채널 표본 5개 미만이면 결과에서 제외** · 영상 조회수 1천 미만은 노이즈로 제외" — ⚠️ 공식 문서(10개 미만)와 스크린샷(5개 미만)이 상이 → 임계값은 설정 상수로 두고 기본 5 채택
  - 빈 상태: "키워드를 입력하고 [영상 떡상 발굴]을 누르세요. 키워드에 맞는 폭증 영상을 추려드려요. 작은 채널의 떡상도 함께 발견됩니다."

**채널 분석 — "채널 깊이 있게 해부"** (공식 문서로 상세 확정)
- 진입: 채널명 검색 → 상세 패널 → **5개 도구** 실행
  1. `인기 영상 TOP 10` — 대표작의 썸네일·제목 패턴 파악
  2. `전수 스캔 (Library Scan)` — 채널 전체 영상을 **median 기반 분석** → **"꾸준한 인기 영상(에버그린)" + "장기 떡상"** 자동 분류. 채널 규모에 따라 수 분 소요, **결과는 30분간 세션 보존**(다른 탭 갔다 와도 자동 표시)
  3. `골든 업로드 시간대` — 과거 성과로 **요일×시간대 히트맵** 생성 → 업로드 최적 시점 제안
  4. `썸네일 DNA` — 전수 스캔의 떡상 썸네일들을 **AI(비전)가 분석**해 공통 색·레이아웃·텍스트 패턴 정리 (원 서비스 18 크레딧)
  5. `채널 성장 궤적` — **매일 자동 수집된 구독자 히스토리 차트 (즐겨찾기 채널만)**
- 필터: **`신생 강자`** — 팬덤이 아닌 콘텐츠 자체로 뜬 영상만 추출
- 권장 흐름: 벤치마크 채널 검색 → 신생 강자 필터 → 전수 스캔 → 장기 떡상 1위를 영상 SEO로 원인 분석 → 썸네일 DNA로 디자인 패턴 → 골든 시간대를 내 채널에 적용
- **⭐ 즐겨찾기 시스템** (기능 2의 백그라운드 데이터 축): 채널을 즐겨찾기(별표)하면 **매일 새벽 cron으로 구독자 수 + 영상 제목 변경을 자동 수집** → 성장 궤적 차트와 제목 변경 이력 탭의 데이터 소스가 됨
- → 구현 노트: 전수 스캔 = uploads 플레이리스트 전체 페이지네이션 수집(playlistItems.list, 50개/page) + videos.list 배치 → 채널 median 조회수 산출 → 분류: 에버그린(경과일 대비 지속 조회 유입 = 최근성 낮은데 조회수 상위), 장기 떡상(median 대비 N배 이상). 스캔 결과 30분 캐시. 썸네일 DNA는 Anthropic API 비전(썸네일 이미지 배치 입력 → 색상 팔레트/레이아웃/텍스트 배치 공통점 JSON). 히트맵은 각 영상의 publishedAt(요일×시간) × 조회수 가중 집계. 일일 수집 cron: FavoriteChannel 대상 channels.list(구독자) + 최근 영상 제목 스냅샷 → TitleSnapshot/SubscriberSnapshot 저장
- **스크린샷 확정 보강**:
  - **즐겨찾기 채널 그룹** (상단 카드, 3 크레딧): 그룹 생성(`+ 새 그룹`) → 그룹 칩(이름 + 🗑 삭제) + "'{그룹명}' 멤버 (n개)" + 채널 검색 결과에서 `그룹 추가` 버튼으로 편입 + **`그룹 영상 일괄 수집`** 버튼 — 즐겨찾기가 "그룹" 단위로 관리됨 (FavoriteGroup > FavoriteChannel 2계층)
  - **채널 검색** (10 크레딧): 국가 드롭다운 + "채널명 또는 핸들 (예: @veritasium)" 입력. 결과는 좌측 리스트(아바타·채널명·`그룹 추가`·구독/영상 수·설명 2줄) + **채널 크기 필터 칩**: `전체` / `🏆 신생 강자 · 구독 1만↓` / `🌱 성장 중 · 구독 1만~10만` / `👑 메가 채널 · 구독 10만+` (주석 "신생 강자 = 따라할 만한 패턴") + `결과 저장`
  - **우측 채널 상세**: 지표 4종(구독자/총 조회수/영상 수/평균 조회수) + 우상단 `떡상 보기`(떡상 영상 탭 딥링크) `저장`
  - **Channel Grade 카드** (노란색): 등급 + 점수 예 "C+ (62/100)" + `자체 산출` 표기 + 원본 구독자 병기 — 채널 종합 등급 산정 로직 존재(구독·조회·업로드 빈도 등 조합 추정)
  - `주간 업로드` 카드: "7.14회 / 주"
  - 성장 추이 빈 상태 안내 2종: "성장 추이 데이터가 아직 없습니다. 즐겨찾기에 추가하면 매일 자동 누적됩니다." / "시계열 데이터 부족 (즐겨찾기 채널이 매일 cron으로 자동 수집)..."
  - 도구 카드 실측(크레딧): `전수 스캔 시작`(주황, **5 크레딧**) / `시간대 분석 (3 크레딧)` — "황금 업로드 시간대 (KST)" / `DNA 분석 (18 크레딧)` — **"먼저 위쪽 [전수 스캔]을 실행하면 떡상 썸네일을 분석할 수 있습니다"** (전수 스캔 선행 종속성)
  - **인기 영상 TOP 10 실측**: 기간·비디오폼 드롭다운 + 성능 등급 칩(건수 포함) + 리스트 항목: 순위/썸네일/제목/조회수/뱃지("장기 영상 · 시간당 169회 · **구독자의 2.4배**" — 구독자 대비 배수 지표!) + 추정 수익 + `자막` `대본 패턴` `번역` 버튼

**한·영 비교 — "해외 인기 주제의 한국화 가능성 탐색"** (공식 문서로 상세 확정)
- 레이아웃: **한국 vs 외국 1개 국가**의 같은 카테고리 인기 영상을 **좌우 그리드**로 비교
- **핵심 개념 `한국 갭 🏷️` 배지**: 외국 영상 카드에 표시 = 한국에 비슷한 주제의 채널이 없거나 약함 → 한국화 시도의 청신호
- **입력 조합 3종** (검색 경로가 달라짐):
  1. 카테고리만 + 키워드 비움 — **양국 공식 인기 차트 기반** 수집, 기간 필터(7d/30d/90d/365d) 내 영상만 통과. 안정적 결과
  2. 카테고리 + 키워드 — 키워드로 좁혀 비교. **자동 키워드 확장 적용**(탐색·분석과 동일 메커니즘)으로 양국 풀 다양화
  3. 키워드만 — 주제로만 비교. **해외 측은 키워드가 현지어로 자동 번역되어 검색됨**
- 번역 UI: 카드 상단 `[🇰🇷 외국 영상 제목 자동 번역]` **전체 토글** + 개별 카드 `[번역]` 버튼
- 국가 선택 가이드(안내 문구화): 미국=대세 주제 / 일본=문화 유사 흐름 / 베트남·인도네시아=신흥
- 권장 흐름: 카테고리+국가 선택 → (필요시 키워드) → 비교 시작 → 한국 갭 배지 영상 우선 확인 → 번역 → **법·문화 장벽 체크**(일본 개그코드, 미국 정치, 중국 금칙어 등) → 떡상 영상 키워드 모드로 한국 시장 규모 재확인
- ⚠️ 실패 사유 3종 경고(UI 안내): ① 문화 장벽(코미디 코드) ② 인구 규모 차이로 단순 번역 안 먹힘 ③ 저작권·초상권(외국 방송 클립)
- → 구현 노트: **한국 갭 판정 로직** — 외국 영상의 제목·주제를 한국어 키워드로 번역 → 한국 지역 search.list → 결과 수·상위 채널 규모·조회수 분포가 임계값 미만이면 갭 배지 부여. 번역은 Anthropic API 일괄 처리(카드 렌더 시 lazy + 캐시). 양국 인기 차트는 videos.list(chart=mostPopular, regionCode=KR/{선택국})
- **스크린샷 확정 보강**: 페이지 부제 "해외 트렌드를 한국화 기획으로" + 노란 안내 박스 "이 도구로 발견할 수 있는 것" 5불릿(주제/키워드 직접 입력, 외국에서만 떡상한 주제, 같은 주제의 후킹·제목 차이, 시장 규모 비교, 외국어 제목 번역)
  - 폼 (**100 크레딧**): 비교 대상 국가 **10개국**(미국/일본/영국/독일/프랑스/브라질/인도/인도네시아/베트남/태국, 국기 표시) / 카테고리 9종(전체/음악/게임/인물·블로그/엔터테인먼트/뉴스·정치/노하우·스타일/교육/과학기술) / 기간 4종(24시간/**7일 기본**/30일/90일) / 주제·키워드(선택, "한글 입력 시 외국 검색용으로 자동 영어 번역" placeholder) / `☐ 외국 영상 제목 자동 번역` / `🌐 한국 vs 외국 비교 시작` 버튼
  - **실행 중 상태**: 버튼 `비교 중...` + 파란 배너 "두 시장을 비교 분석하고 있어요 (**약 15-25초**) — 새로고침하지 마세요. 같은 조건으로 다시 검색하면 즉시 표시됩니다. ※ 다른 페이지로 이동해도 결과는 보존됩니다." + **스켈레톤 카드 8개**
  - **결과 상단 요약**: 상태 배너(예: "한국 영상을 가져오지 못했습니다. 외국 트렌드만 표시됩니다.") + 우측 **`외국이 더 활발 (N.배)`** 시장 활성도 뱃지 / 좌우 시장 요약 카드: 국가별 "상위 n편 평균 조회수 + m개 채널 (평균 x편/채널)" / "※ 한국 갭 후보 자동 식별 결과 n개" 카운트 문구
  - 좌우 그리드 헤더: "🇰🇷 한국 (KR) (n개)" / "🇺🇸 미국 (n개)". 카드에 `자동번역` 뱃지 + 번역 제목 + 공통 카드 요소(성능 등급·VPH·추정 수익·버튼 5종·메타)

**소스 영상 상세 모달 (5탭 변형)** — 한·영 비교·탐색 등에서 카드 `분석`/`드릴다운` 클릭 시 (스크린샷 확정)
> ⚠️ 분석 모달은 **2가지 변형** 존재: ① 9탭 풀버전(소스 발굴 문서 기준) ② **"소스 영상 상세" 5탭 경량판** — 탭: `자막` `SEO` `댓글` `모멘텀` `종합 분석`
- **좌측 패널**: YouTube 임베드 플레이어 + 원제목 + **한글 번역 제목(파란 링크 스타일)** + 조회수/좋아요/길이/**좋아요율 %(평균 대비 표기)** + 채널명 아코디언(`황금시간 분석` `라이브러리 스캔` 바로가기 칩 + `+ YouTube 채널 보기`) + `YouTube에서 열기` 버튼
- **자막 탭**: 이 변형에서는 자막을 직접 표시하지 않고 **"YouTube에서 자막 복사하는 방법" 3단계 가이드**(① [YouTube에서 영상 열기] 새 탭 ② 설명란 "더보기" ③ "스크립트 표시" 클릭) + 안내 스크린샷 이미지 — 카드 버튼 [자막 복사]와 동일한 안내형 접근 (9탭 풀버전의 직접 표시와 컨텍스트별 차이)
- **SEO 탭** (진단 체계 완전 확정): 상단 **총점 84/100** + 요약 카드 3(설명 길이 463자 / 태그 개수 15개 / 태그 글자수 220자) + 실제 태그 칩 목록 + `전체 태그 복사 →`
  - **카테고리별 점수 5항목 (각 20점, 진행 바)**: 제목 키워드 18/20(적정 길이 34자) / 설명 키워드 16/20(463자·타임스탬프 없음) / 태그 키워드 20/20(태그 15개) / 태그 활용도 15/20(태그 220/500자 44%) / 제목·설명 일치 15/20(공통 토큰 3/4개 75%)
  - **베스트 프랙티스 체크리스트** (✓/✗): thumbnail_resolution(최대 해상도 1280px) / timestamps(타임스탬프 포함 여부) / cta_in_description(구독·좋아요·댓글 CTA) / tag_count(권장 5개 이상) / description_length(권장 250자 이상)
  - 각주: "SEO 점수는 영상 메타데이터를 자체 산출한 분석이며, YouTube 공식 지표가 아닙니다."
- **댓글 탭** (분석 체계 완전 확정): KPI 4(분석 댓글 수/유니크 작성자/댓글 총 좋아요/긍정 비율) + **감정 분포 바**(긍정/중립/부정 %, 표본 수) + 정확도 고지("키워드 기반 자동 분류라 반어·풍자 정확도 낮을 수 있어요. 참고용 지표")
  - **🔥 핵심 인사이트**: 각 분류 카테고리의 좋아요 1위 댓글 + 활용 제안(예: "놀람 모먼트 1위 → 바이럴 가능 후킹 포인트" / "가장 인기있는 질문 → FAQ 영상 후보")
  - **댓글 의도 분류 칩**(건수): 전체/공감/놀람/수요/질문/요청/칭찬/비판/기타 + 수요 요약 문구
  - 시청자 관심 토픽(댓글 빈도 상위 단어 칩) + 인기 댓글 리스트(좋아요 순): 분류 뱃지 + 원문 + **🇰🇷 한국어 자동 번역 병기**
- **모멘텀 탭**: ⚠️ 조건부 — "정확한 24h 성장·가속도 분석은 **즐겨찾기 채널의 게시 7일 이내 영상**에서만 가능. 현재는 게시 경과시간 기준 단순 lifecycle 추정만 표시" + `영상 나이 4.4일` 카드 + 각주 "Reach Momentum과 생애주기는 자체 산출 파생 메트릭 (스냅샷 + 게시 경과시간 기반)" — 일일 스냅샷 데이터의 소비처
- **종합 분석 탭**: KPI 카드 4(조회수 / **시간당 조회수 736/h** / **성과 등급 "steady"** — 등급의 내부 영문 코드 노출 / 예상 수익(KRW) ₩23,407) + 좋아요·댓글 수 + `쇼츠` 뱃지 + 챕터 섹션(없으면 "챕터 타임스탬프가 없거나 인식되지 않았습니다") + **같은 채널 인기 영상 (20개)** 리스트(제목+조회수) + **유사 컨셉 영상 (40개)** — 사용된 검색어 병기(예: "compilation funniest farts")
- → 구현 노트: 모달 변형은 진입 컨텍스트 prop으로 분기(경량판 5탭 vs 풀버전 9탭). SEO 5항목×20점 + 체크리스트 5종은 순수 규칙 기반으로 그대로 재현 가능. 성과 등급 코드(steady 등) ↔ 한글 라벨(정체 등) 매핑 테이블. 모멘텀은 즐겨찾기 스냅샷 유무로 정밀/추정 모드 분기

**영상 SEO — "영상 1개 정밀 해부"** (공식 문서로 상세 확정)
- 입력: YouTube 영상 URL 붙여넣기 → 제목/설명/태그/챕터/트래픽 소스 상세 분석
- 제공 기능 5종:
  1. `SEO 진단` — **제목 길이·태그 수·설명 풍부도·챕터 유무·해시태그 사용을 점수화** + 낮은 항목별 "어떻게 올리는지" 체크리스트 역추적
  2. `타겟 키워드 평가` — **사용자가 제시한 키워드**가 제목/설명/태그에 얼마나 최적 배치됐는지 평가 (키워드 입력 필드 존재)
  3. `댓글 감정 분석` — 긍정/중립/부정 비율 + 대표 키워드 클러스터 + **"자주 나오는 질문" 클러스터**(후속 영상 주제 소스)
  4. `유사 영상 드릴다운` — 같은 채널 다른 영상 / 다른 채널의 유사 주제
  5. `자동 번역` — 외국 영상의 메타 한글 변환
- 권장 활용(안내 문구화): 내 영상 먼저 분석해 약점 파악 → 경쟁 대박 영상 분석 → 차이점 보완
- → 구현 노트: SEO 진단은 규칙 기반 점수(제목 30~60자, 태그 수, 설명 길이·링크·타임스탬프 유무, 챕터 파싱, 해시태그 수) + 항목별 개선 가이드 텍스트. 댓글 감정·질문 클러스터는 commentThreads 상위 100개 → Anthropic API 분류·클러스터링. 챕터는 설명란의 타임스탬프 패턴 파싱
- **스크린샷 확정 보강**: 폼(**3 크레딧**): URL 입력(placeholder "YouTube 영상 URL 또는 11자 ID (예: dQw4w9WgXcQ)") + **타깃 키워드(선택)** 입력 + `분석`. 안내 "타깃 키워드를 입력하면 **키워드 적합성 위주로**, 비우면 영상 전반을 분석합니다." 빈 상태 문구에 "7가지 항목별 SEO 점수와 개선 제안"
  - 결과 헤더: 제목 + 채널·포맷·길이 + 메타 요약(조회/좋아요/댓글/설명 글자수/태그 개수·글자수) + 우측 대형 점수 "**54** /100 SEO 점수" + `SEO 결과 저장` 버튼 + 공통 버튼(자막/대본 패턴/번역) + 타깃 키워드 컨텍스트 뱃지
  - **점수 카드가 타깃 키워드 모드에서 변형**: 제목+키워드 12/20(키워드 포함, 제목 79자) / 설명+키워드 14/20(타임스탬프 있음·키워드 없음) / 태그+키워드 6/20(타깃 키워드 미포함) / 태그 사용량 5/20(44/500자 9%) / 제목-설명 일치도 17/20(공통 토큰 83%) — 5탭 모달의 일반 모드(제목 키워드/설명 키워드/태그 키워드/태그 활용도/제목·설명 일치)와 항목명·기준이 타깃 키워드 유무로 분기. 낮은 점수 바는 주황색 경고 색상
  - **`비슷한 영상 발굴 ("{추출 검색어}")` 버튼** — 영상에서 추출한 검색 문구로 유사 영상 발굴 딥링크 (풀폭 연보라 바)
  - 베스트 프랙티스(✓/✗ 한글 라벨 5종: 썸네일 HD 해상도/설명에 타임스탬프/댓글·구독 CTA/태그 5개 이상/설명 250자 이상) + **`개선 제안` 카드**: 낮은 항목 기반 구체 제안 자동 생성(예: "타깃 키워드 '시니어건강'을 포함한 태그를 추가하세요." "태그 총 글자 수를 350자 이상으로 늘려 태그 슬롯을 활용하세요.")
  - **댓글 인사이트 카드**: `댓글 분석 (3 크레딧)` 버튼(추가 과금형 lazy 실행) + **"※ 같은 영상 재분석은 무료입니다."** — (videoId 단위) 재분석 무료 = 결과 영구 캐시 정책의 UI 표현

**영상 카드 공통 버튼 4종** — 탐색·떡상·한·영 비교 등 **모든 탭의 영상 카드에 공통 부착**
1. `[드릴다운]` — 영상 1개를 상세 패널로 펼침. 챕터·같은 채널 다른 영상·유사 영상 자동 수집
2. `[자막 복사]` — ⚠️ **자막을 직접 추출하지 않음**. YouTube 공식 스크립트(대본 표시) 기능으로 이동하도록 안내: 클릭 시 "어떻게 복사하는지" **4단계 가이드 팝업** + `[YouTube에서 영상 열기]` 바로가기. 쇼츠·자막 없는 영상은 YouTube 자체 제약으로 추출 불가 안내 → 정책 제약을 안내형 UX로 우회하는 패턴, 재현 시에도 동일 접근 권장
3. `[대본 패턴]` — 해당 영상의 **훅 구조·흐름을 분석해 내 스타일로 새 대본 자동 생성** (분석→생성 직결 버튼, 기능 1 연계 지점)
4. `[번역]` — 외국 영상 제목·설명 한글 토글

**제목 변경 이력 — "경쟁자의 A/B test 엿보기"** (공식 문서로 상세 확정)
- 대상: **즐겨찾기 채널**의 영상 제목이 언제·어떻게 바뀌었는지 + 변경 후 조회수 추이 자동 추적 (채널 분석의 일일 cron이 데이터 소스)
- **효과 분류 4종** (판정 기준 공개됨):
  - `가속 (+30%↑)` — 변경 후 24시간 조회 증가량이 **변경 전 24시간 대비 +30% 이상** → 새 제목이 더 좋았다는 강한 신호
  - `감속 (−30%↓)` — 오히려 느려짐 → 원래 제목이 더 좋았음
  - `변화 없음` — ±30% 이내 → 제목이 주요 변수 아님
  - `데이터 부족` — 변경 전·후 24시간 스냅샷 중 한쪽이라도 부족해 판단 불가
- 권장 흐름: 채널 분석에서 벤치마크 채널 즐겨찾기 → **1~2주 대기(매일 cron 수집)** → "가속" 케이스 집중 관찰 → 구 제목 vs 새 제목 차이 학습 (패턴 예: 숫자 추가 / 의문형→명령형 / 이름 제거 / 과장어 추가)
- → 구현 노트: **일일 cron이 제목만이 아니라 영상별 조회수도 스냅샷해야 함** (효과 판정에 변경 전후 24h 조회 증가량 비교 필요) → 스냅샷 모델: `VideoDailySnapshot { videoId, title, viewCount, capturedAt }`. 제목 변경 감지 = 직전 스냅샷과 title diff. 효과 = (변경 후 24h Δviews) / (변경 전 24h Δviews) − 1, ±30% 임계값. 데이터 부족 = 전후 스냅샷 미비 시

**소스 발굴 — "해외 영상으로 한국 쇼츠 만들기"** (공식 문서로 상세 확정)
- 개요: **컨셉 한 줄 입력** → 해외 YouTube + TikTok + Instagram Reels에서 관련 영상 수집 + **컨셉 일치도(매치 점수) 0~100점 자동 산정**. 한국에 없는 소재(외국인 반응, 해외 바이럴, K-콘텐츠 리액션) 발굴 목적
- **비용 모델**(원 서비스 크레딧 — 재현 시 API 비용 추정으로 대체): YouTube 검색 200 / TikTok +150 / Instagram +150 / 3개 모두 500. **동일 컨셉 24시간 내 재검색은 무료(캐시 결과)** → 캐시 키 = (컨셉, 옵션) 해시, TTL 24h
- **고급 옵션** (검색창 옆 보라색 `[옵션]` 버튼):
  - 지역 다중 선택 — 그룹핑: 영어권 / 유럽 / 아시아·태평양 / 라틴 / 중동·아프리카. **선택 국가의 현지 언어로 검색어 자동 번역·검색** (비영어권 영상 포착)
  - 언어 강제(영어/일본어/스페인어 등) / 길이(전체 / 쇼츠 ≤3분 / 롱폼 짧은 4~20분 / 롱폼 긴 20분+) / 게시일(30일/90일/1년/3년/5년+/10년+ — 고전 영상 발굴 가능) / 최소 조회수(1만+/10만+/100만+)
  - 정렬: 조회수 / 매칭도 / 최신 / **쇼츠 적합도**
  - `한국 콘텐츠 제외` (기본 ON) — 한국 채널·한국어 영상 자동 배제
  - 플랫폼 자유 조합 토글 (TikTok만, IG만도 가능)
- **결과 필터 프리셋 6종** (검색 후 결과 상단 칩, 클릭 즉시 적용·추가 비용 없음 = 클라이언트 필터):
  1. 플랫폼(멀티 검색 시, 칩에 영상 수 표시) 2. 언어(결과에 실재하는 언어만 노출) 3. 매치 점수(전체/30+관련/60+정합/80+최고) 4. 조회수(전체/1만+/10만+/100만+/1000만+) 5. 업로드(전체/1주/1개월/3개월/1년) 6. 길이(쇼츠 ≤3분 = YouTube Shorts 공식 한도 / 롱폼 >3분)
- **일괄 액션** (하단 고정 바): `전체 선택` / `선택 N개 저장`(→저장됨) / `선택 N개 분석`(영상별 자막·아이디어 자동 생성, YT 42 / TT·IG 15 크레딧) / **`CSV 내보내기`** — 플랫폼·링크·썸네일·제목·채널·조회수·좋아요·길이·매치 점수·매치 이유·매칭 키워드 등 **19개 컬럼, Excel 한글 호환**(BOM 포함 UTF-8)
- **플랫폼별 차이**:
  - 미리보기: YouTube는 분석 모달에서 인앱 재생 / TikTok·Instagram은 썸네일 + 외부 링크("TikTok에서 보기 ↗")만
  - 텍스트 분석(자막·댓글)은 YouTube 전용, TT/IG는 메타 정보 위주
  - 분석 모달 탭: **YouTube 9탭 전부** / **TT·IG는 5탭**(캡션·쇼츠 아이디어·댓글·스크립트 패턴·저장). SEO 진단·모멘텀·유사 영상은 YouTube 전용
- **실행 방식**: `발굴 시작` 후 **페이지를 이동해도 백그라운드 진행**, 5분 내 결과(최대 ~1000개) → 작업 큐 + 알림/뱃지 패턴
- 결과 부족 시 안내 4종: ① 컨셉을 더 구체적으로(또는 더 일반적으로) ② 한국 콘텐츠 제외 OFF ③ 게시일 필터 해제 ④ 플랫폼 추가
- 권장 5단계: 구체적 컨셉("한국 음식 반응"→"외국인 김치 첫 반응") → 옵션(지역 4~6개+플랫폼+한국 제외 ON) → 발굴 시작 → 필터로 압축(60+ → 10만+ → 1년) → 분석 모달에서 훅 추출
- → 구현 노트: **매치 점수는 자체 스코어링** — 컨셉↔제목/설명/태그 임베딩 유사도 또는 LLM 채점으로 재현. "매치 이유·매칭 키워드" 컬럼이 있으므로 점수와 함께 근거 텍스트도 생성·저장. TikTok/IG는 어댑터 인터페이스만(기존 방침 유지), YouTube만 1차 구현

**쇼핑 소스 발굴 — "해외 상품 영상으로 한국 쇼핑 쇼츠 만들기"** (공식 문서로 상세 확정)
- 개요: 상품 컨셉 한 줄 → **YouTube + Douyin(중국) + Kuaishou(중국) + TikTok + Instagram**에서 언박싱·리뷰·하울·실사용 영상 수집 + 매치 점수 0~100. 소스 발굴과의 차이: 소스 발굴은 "반응·바이럴 클립", 쇼핑은 **"팔 만한 상품 영상"** 목적 — 중국 쇼핑 플랫폼 2종 추가 + 분석 모달이 상품 정보·구매처·가격표 썸네일 특화
- 비용: YouTube 200(기본) + 플랫폼당 +150, 5개 전부 800. 24시간 캐시 동일. **잔액 부족 시 해당 플랫폼만 자동 비활성화되고 나머지는 정상 검색** (부분 실행 허용 패턴)
- 플랫폼 특성: YouTube(정형 리뷰·언박싱·비교, 자막·댓글 분석 가능) / Douyin(중국 내수 开箱测评·试用, 신상·가성비템) / Kuaishou(라이브커머스·생활밀착 꿀템) / TikTok(글로벌 하울) / IG Reels(셀럽 제품 소개)
- **고급 옵션** (소스 발굴 대비 차이점):
  - 지역·언어 — **지역에서 CN을 빼면 Douyin·Kuaishou도 함께 비활성화** (플랫폼↔지역 종속 규칙)
  - `상품 카테고리·쇼핑 시그널` — 언박싱/리뷰/하울 등 쇼핑 의도 신호로 필터
  - `가격대 힌트` — 저가/중가/고가 맥락
  - `한국어 포함` (기본 **ON**) — 소스 발굴의 "한국 콘텐츠 제외 기본 ON"과 정반대 기본값 (쇼핑은 한국어 리뷰도 유용)
  - 길이·게시일·최소 조회수·정렬은 소스 발굴과 동일
- **분석 모달 쇼핑 모드** (`[분석]` 버튼): ① 상품 정보(상품명·브랜드·가격대·카테고리 **자동 추출**) ② 쇼핑 후킹멘트(구매욕 자극 한국어 내레이션 초안) ③ CTA·구매처 힌트(구매처 검색 키워드) ④ 가격표 썸네일 컨셉 ⑤ 영상 설명의 상품 링크 원본 표시(seller 채널)
- **플랫폼별 데이터 특이사항** (UI에 안내 필요):
  - Douyin: **조회수 비공개** → "조회수: 비공개" 표시, 좋아요를 인기 지표로 안내
  - Kuaishou: 조회수 정상
  - Douyin·Kuaishou: **댓글 수집 미지원** → 분석 모달에 댓글 탭 자체가 없음
  - 자막 탭: 비-YouTube 영상은 **캡션 원문 + 한국어 번역 동시 표시**
  - 검색 결과 중복 자동 제거, 조회수 0/비공개는 오류 아님 안내
- → 구현 노트: Douyin·Kuaishou는 공식 API가 없어 TikTok/IG와 동일하게 **어댑터 인터페이스만 정의**(1차 범위 외). 상품 정보 자동 추출은 영상 제목·설명·캡션을 LLM으로 구조화 파싱(상품명/브랜드/가격대/카테고리 JSON)하는 방식으로 YouTube 대상 재현 가능. 쿠팡 파트너스(채널 설정 쇼핑 탭)와 연계하면 구매 링크 자동 생성 퍼널 완성
- **스크린샷 확정 보강 (소스 발굴·쇼핑 공통 폼 패턴)**:
  - 소스 발굴(보라 테마): placeholder "채널 컨셉 입력 (예: 한국 진돗개 키우는 외국인)" + `옵션 ▲` 토글 + **`발굴 시작 (200=200크레딧)` — 버튼에 예상 비용 동적 표시**. `☐ 제목 자동 번역` 체크박스. 지역은 7그룹 칩 전체 노출(영어권 8국/서유럽 9국/북유럽 5국/동유럽 8국/아시아 8국/라틴 5국/중동·아프리카 5국) + `전체 선택`/`전체 해제`. 언어 칩 15종. `한국 콘텐츠 제외` 토글(기본 ON) + 플랫폼 칩 `YouTube` `TikTok +150크레딧` `Instagram +150크레딧` + "추가 차감: {n} 크레딧 (잔액 부족 시 해당 플랫폼은 자동 비활성화 — YouTube 검색은 정상 진행)"
  - 쇼핑 소스 발굴(**주황 테마로 구분**): placeholder "상품 컨셉 입력 (예: 닌텐도 스위치 2 언박싱, 스킨케어 하울)" + `발굴 시작 (200+300=500크레딧)`(합산식 표시). 지역 16국(한국·중국·일본·미국 기본 선택), 언어 6종(한국어·중국어·영어·일본어 기본 선택). 정렬 기본 **컨셉 매칭도**. `가격대: 자동 (AI 판단)` 드롭다운. 상품 카테고리 칩 9종(영문: electronics/beauty/fashion/home/food/toys/sports/books/other), 쇼핑 시그널 칩 6종(unboxing/review/haul/compare/tutorial/gifting). `한국어 영상 제외`(기본 OFF) 토글 + **`☐ 썸네일 상품 노출 요구 (LLM 재랭킹 가중치)`** 체크박스 — LLM 재랭킹 단계 존재 확인. 플랫폼 칩에 Douyin·Kuaishou 포함 + "합계: 500 크레딧"

**영상 분석 모달 — "영상 1개를 정밀하게 해부"** (공식 문서로 9탭 완전 확정)
- 진입: 모든 리서치 탭(탐색·떡상·소스 발굴·한·영 비교)에서 영상 카드 클릭 또는 `[드릴다운]`
- **좌측 패널**: 영상 플레이어(YouTube 인앱 재생 / TT·IG 썸네일+외부 링크) / 제목(원문과 한글 번역이 다르면 **둘 다 표시**) / 메타(조회수·좋아요·길이·게시일·**참여율**) / 채널 정보(채널명·국가·구독자·채널 타입) / **매치 점수**(소스 발굴 진입 시만: 0~100 + **점수 이유**) / 우상단 `저장` `외부 열기`
- **우측 9개 탭**:
  1. `자막` — YouTube 공식 자막을 **타임코드와 함께** 표시. 외국 영상 한글 번역 토글. 쇼츠·자막 없는 영상은 추출 불가 안내 (※ 영상 카드의 [자막 복사]는 YouTube로 안내하는 별도 경로 — 모달 자막 탭은 직접 표시)
  2. `핵심 모먼트` — 자막에서 임팩트 있는 **5개 순간**: 타임스탬프 + 원문 + 한국어 번역 + **한국 시청자 관점 요약**
  3. `쇼츠 아이디어` — **한국 쇼츠 3개 자동 기획**: 캐치한 한국어 제목 / 길이 / 사용할 원본 구간 / 훅 포인트 / **300~500자 한국어 내레이션 초안**
  4. `썸네일 컨셉` — 클릭 유도 썸네일 **2개**: 한국어 텍스트 + **영문 이미지 프롬프트**(AI 이미지 생성에 바로 사용 — 기능 1 이미지 파이프라인과 포맷 호환)
  5. `댓글 분석` — 긍정·중립·부정 비율 + 키워드 클러스터 + "자주 나오는 질문"(후속 주제 발굴)
  6. `유사 영상` — 같은 채널 + 다른 채널 같은 주제. **카드 클릭 시 분석 모달 재진입**(모달 중첩/교체 내비게이션)
  7. `SEO 진단` — 제목 길이·태그 수·설명·챕터·해시태그 점수화 (영상 SEO 탭과 동일 엔진)
  8. `챕터` — 구간별 제목 + **챕터 점프 재생** + 챕터별 자막 미리보기
  9. `대본 패턴` — 훅 구조·흐름 분석 → **주제만 한국식으로 바꾼 한국어 대본 초안** 자동 생성
- **비용 모델** (원 서비스 크레딧, 탭별 lazy 과금 구조 — 재현 시 탭 최초 클릭 시 생성+캐시):
  - YouTube: 쇼츠 아이디어(드릴다운) 42 / 댓글 3 / 대본 패턴 14 / SEO 3 / **챕터·유사 영상 무료**
  - TikTok: 쇼츠 아이디어 15 / 댓글 8 / 캡션 표시·번역 무료 / 대본 패턴 미지원
  - Instagram: 쇼츠 아이디어 15 / 댓글 2 / 대본 패턴 미지원. 캡션 없으면 자막 탭에서 안내 후 외부 링크
  - 일괄 분석은 영상별 단가 합산 안내
- 플랫폼별 탭 가용성: YouTube 9탭 전부 / TT·IG는 자막(캡션)·쇼츠 아이디어·댓글·저장 (+스크립트 패턴, 소스 발굴 문서 기준) — SEO 진단·모멘텀·유사 영상·대본 패턴은 YouTube 전용
- **저장 연동**: 분석 결과는 영상 저장 시 **함께 보관**되어 저장됨 탭에서 재열람 가능 → SavedItem에 분석 결과 JSON 포함 필수
- ⚠️ 품질 경고(UI 안내): 자막 없는 영상은 핵심 모먼트·쇼츠 아이디어 품질 저하 → 자막 있는 영상 위주 분석 권장
- 권장 5단계: 매치 점수 높은 영상 1~5개 → 핵심 모먼트로 "한국에 통할 부분" 식별 → 쇼츠 아이디어 1개 선택 → 썸네일 컨셉의 영문 프롬프트로 시안 → 대본 패턴 초안을 저장됨/대본 생성으로 연결
- → 구현 노트: 탭 2·3·4·9는 자막(+메타)을 컨텍스트로 한 Anthropic API 생성물 — 각각 출력 스키마(JSON) 정의. 자막은 YouTube 자막 트랙 API(captions)로 취득 가능한 범위에서, 참여율 = (좋아요+댓글)/조회수. 탭별 결과는 (videoId, tabType) 키로 영구 캐시해 재과금 방지

**저장됨 (Swipe Board)** — 스크린샷 확정
- 헤더: "저장된 아이디어 (Swipe Board)" + 우측: **타입 필터 드롭다운**(전체/…) + **태그/제목 검색** 입력 + 카운트("1개")
- **저장 카드 구성**: 좌상단 **타입 뱃지**(예: `인기 영상` — 저장 출처 컨텍스트: 인기 영상/아이디어/채널 등) + 우상단 🗑 삭제 / 제목(**스냅샷 당시 값 보존**) / 채널명 · 조회수(당시 스냅샷) / **`+ 태그` 버튼**(사용자 태그 추가 → 태그 검색과 연동) / 저장일 + `원본 ↗` 외부 링크 / 하단 버튼 2개: `🔍 분석`(분석 모달 재진입 — 저장된 분석 결과 재열람) + `📝 메모`(사용자 메모 작성)
- 문서 확정 사항과 결합: 아이디어 타입 카드에는 `[대본 생성]` 버튼 → 기능 1 프로젝트 생성 직결 (홈/사용 흐름 문서 기준)
- → 데이터 모델: `SavedItem { type(VIDEO|IDEA|CHANNEL), snapshotJson(제목·채널·지표 당시값), analysisJson(분석 결과), userTags[], memo, sourceUrl, savedAt }`

### 7.2 공식 사용 흐름 6종 (UX 시나리오 — 재현 시 온보딩/가이드 문서로 활용)
1. **주제가 없는 신규 채널**: 홈 AI 아이디어 → 탐색·분석(분석 모드)로 검색량·경쟁도 → 떡상 영상(키워드 모드)로 작은 채널 성공 공식 관찰 → 3개+ 케이스 공통 패턴(썸네일 색감/첫 3초 훅/제목 숫자) 추출 → 저장됨
2. **성과 정체 채널**: 채널 분석(내 채널 전수 스캔) → 에버그린·장기 떡상으로 내 강점/공식 역추적 + 썸네일 DNA → 떡상 영상으로 경쟁자 공식 수집 → 교차 분석
3. **해외 주제 한국화**: 한·영 비교(카테고리+국가) → `한국 갭` 배지 주목 → 번역 → 한국화 판단 → 영상 SEO로 원본 심층
4. **경쟁 채널 벤치마킹**: 채널 분석(경쟁 채널 TOP 10) → 폭발적/상승세 + 신생 강자 필터 → 영상 분석→영상 SEO(태그/챕터/댓글 감정) → 제목 변경 이력으로 A/B 패턴
5. **주간 쇼츠 속성 기획**: 탐색 모드(24h/7d + 쇼츠 + 폭발적/상승세 + 조회수 1만+) → 상위 5개 훅 분석 → 저장 → 대본 생성
6. **해외 바이럴 발굴**: 소스 발굴(컨셉 + 멀티플랫폼) → 필터 프리셋으로 압축 → 분석 모달(자막·댓글·아이디어) → 저장 → 쇼츠 대본 씨앗

### 7.3 구현 시사점 (PROJECT_SPEC 개정에 반영 예정)
- **"성능 등급"(폭발적/상승세/에버그린/신생 강자)은 파생 지표 분류 체계** — 조회수 증가율, 업로드 경과일, 채널 평균 대비 배수, 구독자 대비 조회수 등을 조합한 등급 산정 로직 필요
- **자체 스코어(0~100)** 가 소스 발굴 결과에 존재 (필터 "점수 60+") — 키워드 시장성 스코어와 통합 설계 가능
- 소스 발굴의 비동기 대량 수집(~1000개/5분)은 작업 큐 + 진행률 UI 패턴 (기능 1 렌더링과 동일 인프라 재사용)
- TikTok/Instagram은 기존 방침대로 어댑터 인터페이스만 (YouTube 우선)
- 모든 리서치 결과의 종착지는 저장됨 → `[대본 생성]` → 기능 1 프로젝트 생성이라는 **단일 퍼널 구조**
