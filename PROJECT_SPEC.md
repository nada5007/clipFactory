# YouTube 콘텐츠 스튜디오 — 프로젝트 명세서 (PROJECT_SPEC.md)

> reelbox.ai의 핵심 기능 2가지를 로컬 개발 환경(VS Code + Claude Code)에서 직접 구현하는 프로젝트.
> 이 문서는 Claude Code가 참조하는 최상위 요구사항 문서이며, 세부 구현 지침은 CLAUDE.md를 참조한다.

---

## 0. 프로젝트 개요

| 항목 | 내용 |
|---|---|
| 프로젝트명 | youtube-content-studio (가칭) |
| 목표 | ① YouTube 비디오 프로젝트 생성·관리 시스템 ② YouTube 데이터 분석 대시보드 |
| 개발 도구 | VS Code + Claude Code |
| 사용자 | 단일 사용자(본인) 우선. 멀티유저는 추후 확장 |
| 배포 | 1차: 로컬 실행 (localhost) / 2차: 개인 서버 또는 Vercel 배포 |

### 0.1 권장 기술 스택

```
Frontend  : Next.js 14+ (App Router) + TypeScript + Tailwind CSS + shadcn/ui
Backend   : Next.js API Routes (또는 대안: Spring Boot 3 + React 분리형)
DB        : SQLite (개발) → PostgreSQL (운영), ORM: Prisma
작업 큐    : BullMQ + Redis (영상 생성 등 장시간 작업용) — 1차에서는 단순 비동기 처리로 대체 가능
영상 처리  : FFmpeg (로컬 설치 필수)
외부 API  : Anthropic API(스크립트), TTS API(ElevenLabs 또는 Google Cloud TTS),
            이미지 생성 API(택 1), YouTube Data API v3
```

> Java/Spring 경험이 있다면 Backend를 Spring Boot(WebFlux 또는 MVC + SSE)로 분리해도 좋다.
> 단, 1인 개발 + Claude Code 활용 효율을 고려하면 Next.js 단일 코드베이스를 권장한다.

### 0.2 필요한 API 키 (.env)

```
ANTHROPIC_API_KEY=        # 스크립트/아이디어 생성
ELEVENLABS_API_KEY=       # TTS (또는 GOOGLE_TTS_KEY)
IMAGE_API_KEY=            # 이미지 생성 (예: 별도 이미지 생성 API)
YOUTUBE_API_KEY=          # Data API v3 (분석용, 읽기)
GOOGLE_OAUTH_CLIENT_ID=   # YouTube 업로드용 OAuth 2.0
GOOGLE_OAUTH_CLIENT_SECRET=
GEMINI_API_KEY=           # 스크립트 재생성 - Google Gemini 모델 사용 시
XAI_API_KEY=              # 스크립트 재생성 - xAI Grok 모델 사용 시
DATABASE_URL=
```

**프로젝트 상세 편집 화면 (1.3) — 스크립트 탭 LLM 멀티 프로바이더 지원 (2026-07-27 수정, 사용자 요청)**: 스크립트 필드별 재생성 시 사용할 LLM을 Anthropic(Claude)/OpenAI(GPT)/xAI(Grok)/Google(Gemini) 중에서 선택 가능해야 한다. GPT는 기존 `IMAGE_API_KEY`(OpenAI 계정 공용)를 재사용하고, Gemini/xAI는 위 신규 키를 채널 설정 > API 키 관리 화면에서 등록한다. API 키가 없는 프로바이더의 모델은 선택 목록에 표시하되 비활성화 처리한다.

---

## 1. 기능 1 — YouTube 비디오 프로젝트 생성 및 관리

### 1.1 데이터 모델

```prisma
model Channel {
  id            String    @id @default(cuid())
  name          String
  youtubeChannelId String?           // 연동된 실제 YouTube 채널
  defaultSettings Json                // 스크립트/TTS/영상/업로드 기본값
  projects      Project[]
  createdAt     DateTime  @default(now())
}

model Project {
  id            String    @id @default(cuid())
  title         String
  description   String?
  channelId     String
  channel       Channel   @relation(fields: [channelId], references: [id])
  status        ProjectStatus @default(DRAFT)
  // DRAFT | SCRIPTING | IMAGING | TTS | EDITING | RENDERED | UPLOADED | FAILED
  reviewStatus  ReviewStatus  @default(PENDING)   // PENDING | REVIEWED
  creationType  CreationType  @default(MANUAL)    // MANUAL | AI_AUTO
  progress      Int       @default(0)             // 0~100
  settings      Json                              // 채널 기본값 override
  script        Script?
  images        ImageAsset[]
  audio         AudioAsset?
  video         VideoAsset?
  thumbnail     ThumbnailAsset?
  uploadConfig  UploadConfig?
  createdAt     DateTime  @default(now())
  updatedAt     DateTime  @updatedAt
}
```

(Script, ImageAsset, AudioAsset, VideoAsset, ThumbnailAsset, UploadConfig 모델은
각각 원본 텍스트/파일 경로/생성 파라미터/상태 필드를 갖는다 — 구현 시 Claude Code가 상세 설계)

### 1.2 프로젝트 관리 화면 (목록)

**요구사항**

1. **프로젝트 생성**
   - 우측 상단 `+ 새 프로젝트` 버튼 → 모달 오픈
   - 필수 입력: 채널 선택(드롭다운), 제목
   - 선택 입력: 설명, 생성 방식(수동/AI 자동)
   - 생성 시 채널의 `defaultSettings`가 프로젝트 `settings`로 복사됨 (이후 프로젝트에서 수정하면 프로젝트 값이 최종 적용)

2. **검색 및 필터**
   - 검색: 프로젝트 제목 부분 일치 (디바운스 300ms)
   - 필터 드롭다운 3종: 채널 / 상태(status) / 생성 방식(creationType)
   - 정렬: 최신순(기본), 오래된순, 제목순, 진행률순
   - 검색·필터·정렬 상태는 URL 쿼리스트링에 반영 (새로고침 유지)

3. **프로젝트 카드 리스트**
   - 카드에 표시: 제목, 채널명, 진행률 바(%), 상태 뱃지, 검수 상태, 생성일
   - 카드 우측 상단 `⁝` 메뉴: 프로젝트 수정(제목/설명 인라인 수정 모달), 복제, 삭제(확인 다이얼로그)
   - **검수 완료 체크박스**: 클릭 시 reviewStatus를 PENDING ↔ REVIEWED 토글. 목록에서 검수 대기/완료를 시각적으로 구분 (뱃지 색상)
   - **작업 계속 버튼**: 프로젝트 상세 편집 화면으로 이동

### 1.3 프로젝트 상세 편집 화면

좌측 사이드바 탭 6개 + 우측 미리보기 영역 구조.

| 탭 | 기능 | 1차 범위 |
|---|---|---|
| 스크립트 설정 | 주제 입력 → Anthropic API로 쇼츠 대본 생성. 톤/길이/언어 옵션. 수동 편집 가능 | ✅ 필수 |
| 이미지 생성 | 대본 장면별 이미지 프롬프트 자동 생성 → 이미지 API 호출. 장면별 재생성/교체 | ✅ 필수 |
| TTS/BGM 설정 | 대본 → TTS 음성 파일 생성 (한국어 자연 음성). BGM 파일 선택 + 볼륨 조절 | ✅ 필수 |
| 영상 설정 | FFmpeg로 이미지+음성+자막(SRT 자동 생성)+BGM 합성. 해상도(1080x1920), 자막 스타일 | ✅ 필수 |
| 썸네일 설정 | 이미지 선택 또는 생성 + 텍스트 오버레이 | ⏩ 2차 |
| 업로드 설정 | 제목/설명/태그, 공개 설정, 예약 업로드(datetime), YouTube OAuth 업로드 | ✅ 필수 (예약은 2차) |

**파이프라인 규칙**
- 각 단계는 독립 실행 가능하되, 이전 단계 산출물이 없으면 안내 표시
- 단계 완료 시 `progress` 자동 갱신 (스크립트 20% → 이미지 40% → TTS 60% → 영상 80% → 업로드 100%)
- 장시간 작업(이미지 일괄 생성, 렌더링)은 백그라운드 작업 + 진행률 SSE(Server-Sent Events)로 클라이언트에 스트리밍

**스크립트 탭 — UI 전체 확장 요구사항 (2026-07-27 수정, 참조 사이트 캡처 근거)**: 사용자가 참조 사이트(원본 서비스)의 프로젝트 상세 화면 캡처를 제공했고, 개별 프로젝트 관리 화면 구성을 참조 사이트와 동일하게 맞추도록 요청함. 화면 전체(탭 바, 전체 진행률 바, 검수 관리 패널 등)와 파이프라인 단계별 탭들을 순차적으로 캡처 제공하며 확장할 예정이며, 이번 1차 작업 범위는 **스크립트 탭만**으로 한정한다(사용자 지시: "일단 스크립트 탭에 대해서만 구현 진행. 나머지 탭은 사용자가 참조 사이트의 캡쳐를 차례차례 제공하겠음"). 나머지 탭(이미지/TTS·BGM/영상/썸네일/업로드)과 페이지 공통 헤더(진행률 바, 6단계 아이콘, 검수 관리 패널, 탭 바 자체)는 이후 캡처 제공 시 별도로 진행한다.

스크립트 탭 상세 요구사항:
- 제목/후킹멘트/대본 3개 카드 각각에 편집(연필)/재생성(새로고침)/복사 아이콘 3종을 붙인다. 편집은 기존 인라인 편집을 유지하고, 복사는 클립보드 복사, 재생성은 필드별 재생성 모달을 연다.
- 재생성 모달: 커스텀 프롬프트(선택, 최대 1000자, 비워두면 기본 설정으로 재생성) + 재생성에 사용할 LLM 모델 드롭다운(프로바이더별 그룹, 위 0.2 항목 참조) + 취소/재생성 버튼.
- 이미지 프롬프트 카드: 기존 읽기 전용 목록을 항목별 textarea(직접 수정)+삭제 아이콘으로 바꾸고, 하단에 개수 입력 + "프롬프트 추가" 버튼(빈 항목 추가) + 카운터(현재/900)를 추가한다. 카드 헤더에도 복사/재생성(전체 프롬프트 재생성, 동일 모달 패턴) 아이콘을 붙인다.

**탭 바 도입 + 이미지 탭 전체 확장 (2026-07-27 수정, 참조 사이트 캡처 + 참조 영상 근거)**: 사용자가 참조 사이트의 이미지 관리 화면 캡처 7장과 참조 영상 2개(https://youtu.be/v-_8IICJfSs, https://youtu.be/sRqDs_RCBr8)를 제공하며 이미지 탭 전체 확장을 요청함. 사용자 확인(AskUserQuestion): 이번 라운드에 "이미지 변환"(다중 이미지 합성/배경 교체)까지 포함해 전체 한 번에 진행, 이미지 생성 모델은 품질 티어까지 세분화.

1. **탭 바**: `project-detail-client.tsx`를 세로 패널 나열 → 가로 탭 바(스크립트 관리/이미지/TTS·BGM/영상/썸네일/업로드) 클릭 전환 구조로 변경. 각 탭에 완료 여부 점(dot) 표시(프로젝트 progress 임계값 기반의 근사치 — 정확한 하위 리소스 존재 여부 조회는 이번 범위에서 제외). 전체 진행률 바, 검수 관리 패널은 이번 라운드 범위 밖(다음 캡처에서 진행).
2. **이미지 생성 모델**: OpenAI(gpt-image-1, quality: Low/Medium/High 3단계) + Google Nano Banana(Gemini 이미지 모델, 표준형/Pro 2단계) 총 5개 옵션. Gemini는 기존 GEMINI_API_KEY, OpenAI는 기존 IMAGE_API_KEY를 재사용.
3. **이미지 카드별 액션 5종**: 원본보기(새 탭에서 원본 파일 열기), 이미지교체(로컬 파일 업로드로 교체), 이미지 재생성(해당 장면 프롬프트+모델로 즉시 재생성/덮어쓰기), 이미지 변환(아래 4번), 삭제.
4. **이미지 변환**: 소스 이미지 최대 5개(대상 카드 포함) + 배경 프리셋 6종(고급 스튜디오/미니멀 화이트/자연 속/카페 분위기/한국 거실/해변 배경) + 변환 프롬프트 + 모델(OpenAI/NanoBanana) + 해상도(1K/2K/4K) + 비율(16:9/4:3/1:1/3:4/9:16) + 변환 강도(0~100, 실제로는 프롬프트 지시문 강도로 근사 반영 — Gemini/OpenAI 이미지 API에 강도 파라미터가 없어 프롬프트 엔지니어링으로 대체). "미리보기"로 결과를 먼저 확인한 뒤 "적용"해야 대상 카드에 실제 반영됨(자동 저장 아님).
5. **전체 재생성 모달 고도화**: 기존 "전체 재생성" 버튼을 장면별 프롬프트 임시 조정 + 모델 선택이 가능한 모달로 확장. 프롬프트 조정은 이번 실행에만 적용되고 스크립트에 저장된 원본 이미지 프롬프트는 바꾸지 않는다.
6. **빈 카드 추가**: 이미지 없이 빈 슬롯만 추가(이후 업로드 또는 재생성으로 채움). 스키마 변경: `ImageAsset.filePath`를 nullable로, `quality` 필드 추가.
7. **범위 제외 사항(의도적 축소)**: 전체 선택 체크박스 + 일괄 작업 버튼(참조 캡처에 목적지 동작이 안 보여 이번 범위 제외), 온보딩 안내 배너, 스타일 프리셋 12종 전체(핵심 배경 프리셋 6종만 우선 구현).

**이미지 변환 팝업 기능 보강 (2026-07-28 수정, 참조 캡처 근거)**:
1. 소스 이미지를 "프로젝트 내 기존 이미지 선택"이 아니라 **로컬 드라이브 업로드** 방식으로 변경. 기본값은 대상 카드 이미지 1개이며, 이를 포함해 제거(X) 가능하고, "+ 추가"로 로컬 파일을 최대 5개까지 업로드한다.
2. 배경 프리셋에 한국어/English 언어 토글 추가 — 토글에 따라 프리셋 라벨과 실제 프롬프트 문구가 해당 언어로 전환된다.
3. 변환 프롬프트 라벨을 "변환 프롬프트 (한글/영어 모두 가능)"으로, 나노바나나 모델의 한글 프롬프트 지원 안내 문구를 추가.
4. 변환 비율(16:9/4:3/1:1/3:4/9:16) 선택 유지, UI를 드롭다운 대신 버튼(칩) 형태로 변경.
5. Nano Banana 계열에 "Nano Banana 2 (고속형)" 모델 추가(Pro급 품질을 더 빠르고 저렴하게). 각 모델에 "최대 입력 이미지" 개수(표준형 3개/Pro 5개/고속형 5개)를 두고, 모델 선택 시 설명+최대 입력 이미지 안내 문구를 표시. 소스 이미지가 모델의 최대 개수를 초과하면 자동으로 잘라낸다.
6. OpenAI 모델 관련 오류("Billing hard limit has been reached")는 사용자 OpenAI 계정의 결제 한도 문제이며 코드 결함이 아님 — 별도 조치 없음.

**TTS 탭 전체 확장 (2026-07-28 수정, 참조 캡처 근거)**: 사용자가 참조 사이트의 TTS/BGM 관리 화면과 "TTS 재생성" 모달 캡처를 제공하며 TTS 기능 보강을 요청함. 사용자 확인(AskUserQuestion): 이번 라운드는 **TTS만** 진행하고 BGM 설정(BGM 카드, 곡 선택/업로드, 볼륨·재생속도)은 다음 라운드로 미룬다. BGM 라운드 착수 시 사용자가 지정한 소스(유튜브 "브금대통령" 채널, https://www.youtube.com/channel/UC7oH_BYk5rSP0JF7IDn-jRQ)에서 음원을 받아 쓰는 방법을 강구하되, 실제 다운로드 자동화 전에 유튜브 이용약관·저작권 확인이 필요함.

1. **문장 분리 규칙**: 이미 `.`, `!`, `?`(및 전각 `。！？`) 기준으로 분리되어 있어(`src/lib/text.ts`) 이번 요청과 이미 일치 — 변경 불필요.
2. **멀티 프로바이더**: OpenAI Audio TTS 신규 추가(모델 tts-1/tts-1-hd/gpt-4o-mini-tts, 음성 12종, 오디오 포맷 mp3/wav/ogg, gpt-4o-mini-tts 전용 음성 지시문 최대 200자 — 이 모델은 숫자 속도 조절 미지원). ElevenLabs는 모델/음성 목록을 하드코딩하지 않고 ElevenLabs 실제 API(`GET /v1/models`, `GET /v1/voices`)에서 매 요청 시 동적으로 가져온다(계정별 보이스 라이브러리가 다르므로). OpenAI는 기존 `IMAGE_API_KEY`(OpenAI 계정 공용) 재사용, ElevenLabs는 기존 `ELEVENLABS_API_KEY` 재사용.
3. **Google Cloud TTS / NAVER Clova Voice 조사 결과**(구현은 하지 않음, 안내만): Google Cloud TTS는 Standard 음성 월 400만자·WaveNet/Neural2 월 100만자 무료 티어가 있으나 GCP 결제 계정(신용카드) 등록이 선행되어야 API 활성화 가능. NAVER Clova Voice(NAVER Cloud Platform)는 Application당 기본료가 별도 청구되고 합산 기준 월 100만자까지 기본 사용량 포함, 마찬가지로 결제수단 등록이 선행 조건 — 두 서비스 모두 "완전 무료"는 아니고 사용량 기준 과금이며 결제 계정 개설이 필요함. 추후 어댑터 확장 시 참고.
4. **세그먼트별 개별 재생성/삭제**: 기존에는 전체 삭제 후 일괄 재생성만 가능했던 것을 각 세그먼트 카드에 개별 재생성/삭제 버튼을 추가. 개별 재생성/삭제 시 이후 세그먼트들의 `startMs/endMs`를 실제 오디오 길이 기준으로 재계산해 순서를 맞춘다(타이밍이 어긋나면 영상 합성 단계에서 문제가 되므로).
5. **TTS 재생성 모달**: 프로바이더 선택에 따라 하위 옵션이 달라지는 동적 폼(OpenAI: 음성/모델/오디오 포맷/음성 지시문, ElevenLabs: 모델/음성/안정성·유사도 부스트·스타일·속도 슬라이더) + "세그먼트별 개별 음성 설정" 체크박스로 특정 세그먼트에만 다른 프로바이더/설정을 일괄 적용하는 기능.
6. **스키마 변경**: `AudioSegment`에 `settings Json?` 추가(오디오 포맷/지시문/안정성 등 프로바이더별 파라미터 보관).

**BGM 설정 (2026-07-29 수정, 사용자 지정 소스 근거)**: 사용자가 유튜브 "브금대통령[BGM President]" 채널(https://www.youtube.com/channel/UC7oH_BYk5rSP0JF7IDn-jRQ)의 라이센스를 직접 확인함 — 유튜브/인스타그램 영상에 배경음악으로 사용 시 무료(출처 표시 조건부). 조사 결과 해당 채널 영상 설명란에 자체 다운로드 사이트(bgmpresident.com)가 안내되어 있으나 실제 구조 확인 불가하여, 사용자의 기존 프로젝트(`/Users/eric/vsc-workspace/AnimalShortform/src/processing/bgm_processor.py`)에 이미 구현되어 있던 방식(yt-dlp로 채널 영상 목록 조회 후 오디오만 mp3로 추출, 제목 키워드 기반 카테고리 자동 분류)을 그대로 포팅하기로 확정.

1. **BGM 라이브러리**: `BgmTrack` 모델 신규(유튜브 videoId/제목/카테고리/재생시간/파일경로/출처). "브금대통령 채널 동기화" 버튼으로 신규 트랙만 골라 yt-dlp 다운로드. 프로젝트에 종속되지 않는 전역 라이브러리라 기존 Job 모델(projectId 필수)에 태우지 않고, maxVideos를 적당히 제한(기본 20개)한 동기 처리 + 로딩 상태 UI로 구현. 카테고리는 제목 키워드 매칭(귀여운/웅장한/신나는/잔잔한/긴장감/기타)으로 자동 분류. MP3 직접 업로드도 지원.
2. **채널 기본값 / 프로젝트 전용 오버라이드**: BGM 설정(트랙 선택+볼륨(dB)+재생속도+반복재생)을 `Channel.defaultSettings.bgm`(채널 기본값)과 `Project.settings.bgm`(프로젝트 전용, 있으면 채널 기본값 대신 적용)로 저장 — 기존 두 필드가 이미 자유 JSON이라 스키마 변경 없이 애플리케이션 레벨에서 확장.
3. **범위 제외(의도적 축소)**: 실제 영상 렌더링(ffmpeg)에 BGM을 믹싱하는 것은 이번 라운드 범위 밖 — "영상" 탭 확장 시 별도 진행. 채널별 동기화 자동 스케줄링(다른 프로젝트는 주기적 cron)도 이번엔 수동 버튼으로만 제공.
4. **주의**: yt-dlp로 유튜브에서 오디오를 추출하는 것은 유튜브 자체 이용약관상 회색지대(공식 다운로드 기능이 아닌 방식의 다운로드는 유튜브 ToS 위반 소지)이며, 음원 저작자(브금대통령)의 저작권 라이센스와는 별개 사안임을 사용자에게 고지함 — 사용자가 본인 프로젝트에서 이미 사용 중인 방식을 확인 후 진행을 확정함.

**영상 탭 + 타임라인 편집기 Phase A (2026-07-29 수정, 참조 캡처 9장 근거)**: 사용자가 영상 탭 진입 화면(미생성/생성 완료)과 타임라인 편집기 초기 화면·스크립트 탭 캡처를 제공함. UI_SPEC.md §5.7이 이미 Phase A/B/C 구현 단계를 권고하고 있어, 사용자 확인(AskUserQuestion)으로 이번 라운드는 **Phase A만** 진행: "자동 배치된 타임라인의 읽기 전용 뷰 + 클립 시간/텍스트 수정(스크립트 탭) + 렌더링". 클립 드래그/트림/분할/실행취소/스냅 상호작용, 키프레임/마스크/색보정/전환효과, AI 자동 편집·자동 효과음은 Phase B/C로 명확히 다음 라운드에 미룬다.

1. **영상 탭 개편**: 기존 "🎬 영상 렌더링" 버튼을 영상 탭에서 제거하고, 렌더링 트리거는 타임라인 편집기 툴바로 이동. 영상 탭은 미생성 시 "타임라인 편집기에서 영상을 생성해주세요" 안내 + `타임라인 편집` 버튼, 생성 완료 시 플레이어+다운로드+`타임라인 편집` 버튼(재진입)만 담당.
2. **신규 라우트**: `/projects/:id/timeline` — 전체화면 다크 테마, 사이드바/헤더 없이 단독 렌더링(`AppShell`이 경로 접미사로 분기).
3. **데이터 모델**: Phase A는 읽기 전용이라 별도 Timeline/Track/Clip 테이블을 새로 만들지 않고, 기존 Script.imagePrompts/ImageAsset/AudioSegment/BGM 설정으로부터 6개 트랙(Subtitles/Video 1/Image/TTS/비디오 오디오/BGM)을 그때그때 계산해 보여준다(`src/lib/timeline.ts`). Video 1·비디오 오디오 트랙은 원본 영상 업로드 기능이 없어 항상 빈 트랙으로 표시.
4. **좌측 4탭**: 스크립트(기존 스크립트 탭 재생성/복사 컴포넌트 재사용, 완전 구현) / 자막·미리보기는 "다음 라운드 지원 예정" 플레이스홀더 / 완성본(기존 영상 플레이어 재사용).
5. **우측 속성 패널**: 클립 미선택 기본 상태만 구현 — 타임라인 통계(트랙 6/총 클립/길이/트랙별 칩 개수), 줌 레벨(실제로 하단 타임라인 가로 스케일에 반영), 스냅 설정 UI(시각적 표시만, Phase B에서 실제 드래그와 연동), 단축키 안내.
6. **품질 분석**: 참조 캡처의 자막 줄 길이 린트 1종만 구현(`analyzeSubtitleLineLength`, 권장 14자/줄) — "N개의 줄 길이가 M자를 초과합니다" 카드 + 무시 버튼(로컬 상태로 dismiss).
7. **유효성 검사**: 총 길이 1800초 초과 여부 + 콘텐츠 존재 여부만 검사(`validateTimeline`), "타임라인이 유효합니다" 또는 이슈 목록 표시.
8. **하단 6트랙**: 시간 눈금자 + 트랙별 클립을 막대로 시각화(읽기 전용 — 드래그/트림 없음). 재생 트랜스포트 바는 이번 라운드에서 비활성(시각 요소만).

**타임라인 편집기 — 좌우 패널 리사이즈 (2026-07-29 추가 수정)**: 좌측 콘텐츠 영역(스크립트 등)과 우측 속성/품질 분석 패널 사이 너비를 마우스 드래그로 조절할 수 있게 한다.

**타임라인 편집기 Phase B (2026-07-29 수정, UI_SPEC.md §5.7 권고)**: "phase b 진행하자" 요청에 따라 클립 드래그/트림/분할, 스냅, 실행취소, 품질 분석 원클릭 수정을 진행한다. 사용자 확인(AskUserQuestion)으로 Phase A의 "매번 재계산" 방식 대신 UI_SPEC §5.6 그대로 **정식 Timeline/TimelineTrack/TimelineClip 테이블**을 신설해 실제 편집 상태를 저장한다(가벼운 오버라이드 JSON 방식 대신).

1. **스키마**: `Timeline`(프로젝트당 1개) → `TimelineTrack`(타입별 6개) → `TimelineClip`(startMs/endMs/payload Json) 3단 관계형 테이블 신설.
2. **동기화 로직**: 최초 진입 시 자동으로, "동기화" 버튼 클릭 시 수동으로 Script/ImageAsset/AudioSegment/BGM 설정 기준 클립을 만든다. `payload.sourceId`로 원본 레코드와 매칭해 기존 클립의 수동 편집(시간)은 보존하고, 텍스트 등 내용만 갱신 — 원본이 사라진 클립은 정리, 새로 생긴 원본은 클립 추가. BGM 트랙은 반복 배치 특성상 매번 전체 재생성.
3. **클립 선택 + 기본 정보 속성 패널**: 클릭으로 클립 선택 → 우측 패널에 시작/끝 시간(수정 가능), 자막·TTS 클립은 텍스트 편집 포함.
4. **드래그/트림**: 같은 트랙 내에서만 드래그 이동, 양끝 핸들로 트림. 스냅 활성화 시 설정한 간격(ms)으로 스냅. 이웃 클립과 겹치지 않도록 경계 클램프.
5. **분할**: 재생헤드 위치(타임코드 눈금자 클릭으로 이동) 기준으로 선택된 클립을 둘로 분할.
6. **삭제**: 선택된 클립 삭제.
7. **실행 취소/다시 실행**: 클라이언트 히스토리 스택(변경 전 스냅샷) + 일괄 복원 API로 구현(서버측 이벤트 소싱 없이).
8. **품질 분석 원클릭 수정**: 자막 줄 길이 초과 클립들에 대해 글자수 기준으로 줄바꿈만 자동 삽입(클립 개수·타이밍은 유지 — 완전한 자막 재분할은 이후 라운드).
9. **범위 제외**: 키프레임/마스크/색보정/전환효과/특수효과, AI 자동 편집, 자동 효과음, 컨텍스트 메뉴(우클릭 복사/붙여넣기)는 Phase C로 유지.

**타임라인 편집기 Phase C (2026-07-29 요청, reelbox.ai 참조 캡처 다수 근거)**: 사용자가 reelbox.ai 실제 편집기 캡처(재생 툴바, TTS 호흡구간 팝업, BGM/이미지/비디오/TTS 추가 모달, 트랙 좌측 메뉴, 목표 길이 입력 등)를 근거로 다음을 요청. 규모가 커서 하위 그룹으로 나누어 순차 진행(사용자 확인 대기 중, 아직 미착수):

1. **재생 관련 툴바**: 처음으로/재생·일시정지(Space)/끝으로 — 자막·비디오·TTS·이미지·BGM·비디오오디오가 있으면 모두 합쳐 타임라인 기준으로 미리보기 화면에 합성 재생. 전체 시간 + 현재 재생바 시간 표시. 실행취소(Ctrl+Z)·다시실행(Ctrl+Y, 이미 구현됨). 가위 도구(단축키 S) — 클릭으로 클립 분할(현재는 재생헤드+버튼 방식만 있음). 복사(Ctrl+C)/복제(Ctrl+D)/붙여넣기(Ctrl+V)/잘라내기(Ctrl+X)/삭제(Del). 트랙별 클립 1개 선택 후 "전체 갭 제거"(해당 트랙 전체, TTS/자막 대상), 멀티 셀렉트 후 "선택 클립 사이 갭만 제거"(TTS/자막/이미지/BGM/비디오 대상). TTS 호흡구간 추가(클립 사이 지정 간격만큼 벌리기, 0.1~0.5초 프리셋). 목표 길이 맞추기(트랙 전체를 원하는 길이로 비례 조정, §5.5에 이미 명세된 "트랙 목표 시간 맞추기"와 동일 개념).
2. **타임라인 스케일 조절**: 재생 속도 슬라이더 0.1x~4x(편집 프리뷰 전용, §5.5 기존 명세). 줌 슬라이더 1%~100%(현재 구현은 10~1000% 범위 — reelbox.ai 실측 캡처 기준 1~100%로 재조정 필요).
3. **버튼 색상 버그**: 비활성/normal 상태 버튼 배경이 흰색에 가까워 버튼 내부 텍스트가 안 보이는 대비 문제 수정(다크 테마 버튼 스타일 점검).
4. **클립 타입별 속성 패널 세분화**: TTS/자막/이미지/BGM/비디오+비디오오디오 별로 서로 다른 속성 패널 표시(BGM과 비디오오디오는 동일 구성 공유) — §5.2에 이미 상세 명세된 서브탭 구조(자막: 기본정보/자막/스타일/효과, TTS: 기본정보/오디오 등)를 실제 구현.
5. **트랙 추가 기능**: 하단 `+ 트랙 추가` — 캡처의 좌측 트랙 메뉴(비디오/이미지/TTS(음성)/BGM(최대 2개)/효과음/자막) 참고.
6. **클립 추가 기능**: 기존 트랙에 클립 추가 — 타입별 모달(비디오: 직접 업로드/AI 생성/아바타 영상/무료 영상/릴박스 라이브러리, 이미지: 직접 업로드/AI 생성/이미지 변환/무료 이미지/릴박스 라이브러리, TTS: 직접 업로드/AI 생성(자막도 함께 생성 옵션), BGM: 직접 업로드/내 BGM/기본 BGM 검색) — 단, "무료 영상/이미지 검색", "릴박스 라이브러리", "아바타 영상"은 reelbox.ai 자체 콘텐츠 라이브러리 기능이라 본 프로젝트에는 없음. **직접 업로드 + (이미 구현된) AI 생성/BGM 라이브러리 경로만** 재현하고 나머지는 버튼을 만들지 않는다.
7. **멀티 셀렉트 후 삭제**: 여러 클립을 Ctrl+클릭 등으로 다중 선택 후 한 번에 삭제.

**범위 조정 (2026-07-29, 사용자 확인)**: 6번 항목의 "무료 영상/이미지 검색·릴박스 라이브러리·아바타 영상"은 reelbox.ai 자체 콘텐츠 라이브러리 기능이라 본 프로젝트에서 제외 확정 — 클립 추가 모달에는 직접 업로드 + 이미 구현된 AI 생성/BGM 라이브러리 경로만 넣는다. 나머지 1~7번은 한 라운드에 다 처리하기엔 규모가 커서 하위 그룹으로 쪼개 순차 진행하기로 함. **이번 라운드는 "2번(재생 속도·줌 슬라이더) + 3번(버튼 색상 버그)"만 진행**하고, 나머지(클립 편집 도구/속성 패널 세분화/트랙·클립 추가/합성 미리보기)는 다음 라운드 이후로 미룬다.

**타임라인 편집기 — 실제 재생 기능 (2026-07-29 요청, reelbox.ai 참조 캡처 근거)**: 지금까지 구현된 재생 관련 UI(▶ 버튼, 속도/줌 슬라이더, 재생헤드)는 전부 장식/상태 표시용이고 실제로 오디오가 나오거나 재생헤드가 시간에 따라 움직이는 기능이 없었음. 사용자가 참조 사이트의 실제 동작을 캡처로 제공하며 다음을 요청:

1. **재생 툴바 위치**: 재생 관련 툴바(◀| ▶ ▶| 속도 슬라이더, 타임코드, 줌 슬라이더, Preview Mode 안내, 스냅, 미리보기 최대화)를 하단 타임라인 바로 위에 고정 배치. 좌측 콘텐츠 영역(특히 미리보기 탭) 위에도 동일한 축소판 툴바를 중복 표시.
2. **전 탭 공통 실제 재생**: 스크립트/자막/미리보기 탭 어디서든 ▶ 누르면 자막+비디오+이미지+TTS+BGM+비디오오디오 중 존재하는 항목을 타임라인 기준으로 합쳐 재생. 재생 중에는 하단 타임라인의 빨간 재생헤드가 실시간으로 이동.
3. **자막 탭 재생 동기화**: 재생 중 TTS 오디오가 백그라운드로 나오고, 현재 재생 위치에 해당하는 자막 세그먼트로 자동 스크롤 + 파란 외곽선 하이라이트 + 타임스탬프 옆 파란색 "재생중" 텍스트 표시(§5.1에 이미 명세된 자막 세그먼트 카드 리스트 UI 자체가 아직 미구현 — 현재는 플레이스홀더).
4. **미리보기 탭 실제 합성 재생**: 재생 중 미리보기 탭으로 전환하면 이미지/비디오+TTS+자막 오버레이+BGM을 합성해 현재 타임라인 위치 기준으로 실시간 재생.

**클립 타입별 속성 패널 세분화 — 자막/비디오 (2026-07-29 요청, reelbox.ai 참조 캡처 근거)**: §1.3 "타임라인 편집기 Phase C" 4번 항목의 구체화. 사용자가 자막 클립과 비디오 클립의 속성 패널 캡처를 제공하며 다음 서브탭 구현을 요청:

- **자막 클립**: `기본 정보`(클립 ID, 트랙, 시작/끝 시간 편집) / `자막`(텍스트 편집 — 이미 구현됨, "수동 줄바꿈 적용됨" 배지, 기준 글자수 슬라이더+자막 재구성 버튼) / `스타일`(모든 자막에 적용 체크박스, 폰트/글자크기/글자색상/글자굵기, 배경색상/투명도, 위치(가로/세로 px), 테두리 두께/색상, 레이아웃 최대 줄 길이) / `효과`(참조 사이트도 미공개 — 자리만).
- **비디오 클립**: `기본 정보`(AI 영상 분석 카드 포함) / `변환`(위치/크기/회전/투명도/좌우반전) / `비디오 효과`(색보정 프리셋 20종 + 밝기/대비/채도/색온도) / `전환` / `키프레임` / `특수효과` / `비디오 옵션`(재생속도/좌우반전) / `마스크`.

**구현 전 확인된 의존성 문제 (조사 결과)**:
1. **비디오 클립 속성 패널은 현재 선택할 대상이 없음**: `syncTimeline`이 VIDEO/AUDIO 트랙은 "원본 영상 업로드 기능 없음 — 항상 빈 트랙"으로 처리(`timeline.service.ts`). §1.3 6번(트랙/클립 추가 기능)이 구현되기 전까지는 비디오 클립을 만들 방법 자체가 없어, 비디오 속성 패널을 지금 만들어도 실제로 열어볼 수 없다.
2. **자막 "스타일" 탭은 실제 렌더링에 반영되지 않음**: 현재 렌더링 파이프라인(`video.service.ts`의 `renderVideo`)은 SRT 파일을 사이드카로만 생성하고 영상에 자막을 굽지 않는다(`VideoAsset` 모델 주석: "로컬 ffmpeg 빌드에 libass가 없어 번인하지 않음" — 실제로 로컬 `ffmpeg -filters`에 `drawtext`/`subtitles` 필터가 없음을 확인). 폰트/색상/배경/위치/테두리 등을 클립별로 저장하는 UI를 만들어도, 자막 번인 기능(ffmpeg libass 재설치 + ASS 자막 생성 + 렌더 파이프라인 통합) 자체가 별도 큰 작업으로 선행되어야 최종 영상에 반영된다.

**구현 순서 제안**: (a) 오디오 재생 엔진 — TTS 세그먼트 순차 재생 + BGM 동시 재생, 재생/일시정지/처음으로/끝으로 실동작, 재생헤드 실시간 자동 진행(전 탭 공통 기반, 가장 먼저 필요) → (b) 자막 탭 세그먼트 리스트 UI 신규 구현 + 재생 동기화(자동 스크롤/하이라이트/재생중 표시) → (c) 미리보기 탭 실제 합성 렌더링(이미지 슬라이드+자막 오버레이, DOM/Canvas 방식 — 애니메이션·전환효과는 렌더링 후에만 정확, 프리뷰는 근사치) → (d) 툴바 위치 재배치. 규모가 커서 한 라운드에 다 못 끝낼 가능성이 높음 — 실제 진행 순서는 사용자 확인 후 확정.

**이번 라운드 범위 확정 (2026-07-29, 사용자 확인 "둘 다 함께")**: (a)+(b)를 함께 진행 — 오디오 재생 엔진(TTS 순차 재생 + BGM 동시 재생, ▶/⏮/⏭ 실동작, Space 단축키, 재생헤드 자동 진행)과 자막 탭 세그먼트 리스트 UI(타임스탬프 + 텍스트 편집 + 저장, 클릭 시 해당 위치로 재생헤드 이동)를 신규 구현하고, 재생 중 자동 스크롤+파란 외곽선 하이라이트+"재생중" 표시까지 이번 라운드에 끝낸다. (c) 미리보기 탭 실제 합성 재생과 (d) 툴바 위치 재배치는 다음 라운드로 미룸. **자막 탭의 전체 선택 체크박스·찾기/바꾸기 일괄 치환·↑ 순서 이동·멀티 셀렉트 삭제는 이번 라운드 제외**(§1.3 "타임라인 편집기 Phase C" 1번/7번 항목, 클립 편집 도구 그룹에서 이미 별도 라운드로 미뤄둔 것과 동일 — 지금 만드는 자막 탭에서도 이 기능들은 넣지 않는다).

**구현 완료 및 확정 사항 (2026-07-29)**: 사용자 확인에 따라 "자막 번인 기능부터 먼저 구축" + "비디오 속성 패널 UI도 지금 같이 만들기"로 진행, 다음을 구현·검증함:

1. **ffmpeg-full 도입**: 기본 ffmpeg 포뮬러에는 libass가 없어 `brew install ffmpeg-full`로 별도 설치, `FFMPEG_PATH`/`FFPROBE_PATH` 환경변수(`src/env.ts`, zod 검증)로 경로 지정. `.env.example`에 키만 기재, 실제 값은 `.env`(미커밋)에만 설정.
2. **렌더링 파이프라인을 Timeline 기준으로 전면 재구축**: 기존에는 `project.audioSegments`/`images` 원본 테이블만 읽어 렌더링해서 타임라인 편집기의 드래그/트림/분할/텍스트 수정이 **최종 영상에 전혀 반영되지 않던 것**을 발견(구현 중 확인, 사용자에게 즉시 disclosure) → TTS/IMAGE/SUBTITLE TimelineClip 기준으로 재구축. 트림/삭제로 생긴 빈 구간은 TTS 트랙은 무음 삽입(`computeAudioRenderPlan`), IMAGE 트랙은 직전 이미지 연장(`computeImageRenderSegments`)으로 채워 항상 정확한 총 길이를 보장.
3. **실제 버그 발견 및 수정**: 이미지 슬라이드쇼 생성 시 concat demuxer의 "마지막 파일 반복" 트릭이 실제로는 추가 세그먼트로 인식되어 총 길이가 부풀려지는 문제 발견(59.6초 기대 → 79.5초 산출) — `-t {totalDurationSec}`로 출력 길이를 강제해 수정. 실제 렌더링으로 검증(프레임 추출 확인).
4. **자막 번인**: ASS 자막 파일 생성(`src/lib/ass.ts`, 클립별 스타일을 override 태그로 적용) + `ffmpeg-full`의 `ass` 필터로 번인. 실제 프레임 추출로 폰트/배경 박스/위치가 정상 렌더링됨을 시각적으로 확인.
5. **알려진 단순화(디스클로저)**: ASS 포맷 한계로 "배경 박스"와 "텍스트 외곽선"을 동시에 완전히 독립적으로 표현할 수 없어, `BorderStyle=3`(배경 박스 모드)을 채택 — "테두리"는 박스의 여백 두께로 렌더링됨(순수 텍스트 외곽선 단독 표시는 미지원). 글자 드래그 선택 후 부분 색상 적용(리치 텍스트)은 미구현.
6. **클립 속성 패널**: 자막 클립(기본정보/자막/스타일/효과) + 비디오 클립(기본정보/변환/비디오효과/전환/키프레임/특수효과/비디오옵션/마스크) 서브탭 UI를 `src/components/projects/timeline/clip-properties-panel.tsx`에 신규 구현. **비디오 속성 패널은 VIDEO 트랙에 클립을 만들 방법이 아직 없어(§1.3 6번 트랙/클립 추가 미구현) 실제로는 선택할 수 없는 상태로 남아있음** — 트랙/클립 추가 기능이 구현되면 바로 연결되도록 데이터 모델(payload.transform/effects/transition/videoOptions/mask/keyframes)과 API(PATCH 클립에 필드 추가)는 이미 준비됨.
7. **자막 스타일 실제 반영 확인**: 클립별 스타일 편집(`updateClipStyle`) + "모든 자막에 적용"(`applyStyleToAllSubtitles`) API를 curl로 검증, 실제 렌더링에서 폰트 크기/색상 변경이 최종 영상에 반영됨을 프레임 추출로 확인.
8. **범위 제외 유지**: 키프레임 실제 보간/렌더링 반영, 마스크 실제 렌더링 반영, 전환효과 실제 렌더링 반영은 VIDEO 트랙 자체가 비어있어 UI 데이터 저장까지만 구현하고 렌더링 연결은 다음 라운드(트랙/클립 추가 기능 이후)로 유지.

**타임라인 편집기 — 클립 편집 도구 (2026-07-30 요청 "나머지 기능도 구현 진행")**: §1.3 1번/7번 항목 중 남은 클립 편집 도구를 진행. 5번(트랙 추가)·6번(클립 추가)은 `TimelineTrack`이 `@@unique([timelineId, type])`로 타입당 1개만 허용하는 현재 스키마를 바꿔야 하는 더 큰 구조 변경(다중 트랙 지원 시 트랙 조회/동기화/렌더링 전반의 "타입당 1개" 가정을 전부 손봐야 함)이라 이번 라운드에서는 제외하고 다음 라운드로 미룬다. 이번 라운드 범위:

1. **멀티 셀렉트**: Ctrl+클릭으로 여러 클립 선택(같은 트랙 내). 속성 패널은 마지막으로 클릭한 클립 기준으로 계속 표시.
2. **클립보드**: 복사(Ctrl+C)/잘라내기(Ctrl+X)/붙여넣기(Ctrl+V)/복제(Ctrl+D)/삭제(Delete, 멀티 셀렉트 지원). 붙여넣기·복제는 재생헤드/원본 클립 바로 옆에 삽입하되, NLE의 "리플 삽입"(뒤 클립들을 밀어내는 방식)은 이번 범위에서 제외 — 기존 드래그/트림과 동일하게 이웃 클립 경계 안으로 클램프해 배치한다(자리가 없으면 겹치지 않는 선에서 최대한 배치).
3. **가위 도구 단축키(S)**: 클립 선택 + 재생헤드가 클립 내부에 있을 때 S 키로 분할(기존 버튼과 동일 동작).
4. **트랙 갭 제거**: 클립 1개 선택 후 "전체 갭 제거" — 같은 트랙의 모든 클립을 순서대로 빈틈없이 당겨 붙인다. 멀티 셀렉트 시 "선택 클립 사이 갭만 제거".
5. **TTS 호흡구간 추가**: TTS/자막 트랙 전체에 클립 사이 지정 간격(0.1~0.5초 프리셋)을 균일하게 삽입, 이후 클립들을 뒤로 밀어낸다. TTS와 자막 클립은 동일한 순서 구조라 같은 방식으로 함께 밀어 싱크를 유지한다.
6. **목표 길이 맞추기**: 선택한 트랙 전체를 입력한 목표 길이로 비례 확대/축소(각 클립의 startMs/endMs를 원점 기준으로 스케일). **주의(디스클로저 예정)**: TTS 트랙에 적용하면 클립 길이(=재생 시간)가 바뀌는데, 현재 렌더링은 오디오를 자르거나(축소) 무음으로 채우는(확대) 방식이라 실제 음성 속도가 변하지 않는다 — 배속 처리(atempo 필터)는 이번 범위 밖.

**구현 완료 (2026-07-30)**: 1~6번 전부 구현·검증. 순수 함수(`removeGapsInClips`/`removeGapsBetweenSelectedClips`/`insertBreathingGaps`/`scaleClipsToTargetDuration`, `src/lib/timeline.ts`) + 서비스(`duplicateClip`/`pasteClips`/`bulkDeleteClips`/`removeTrackGaps`/`removeGapsBetweenClips`/`addTtsBreathingGaps`/`scaleTrackToTargetDuration`) + API 라우트 + UI(멀티 셀렉트 Ctrl+클릭, 클립보드 단축키, 편집 도구 툴바)까지 전 계층 실제 curl 검증 완료. **알려진 제약**: 복제/붙여넣기는 리플 삽입이 아니라 이웃 경계 클램프 방식이라, 동기화 직후처럼 클립이 빈틈없이 붙어있는 상태에서는 자리가 없어 실패한다(트림으로 먼저 공간을 만들어야 함) — 실제 curl 테스트로 이 동작을 확인했다. "선택 클립 사이 갭만 제거"는 선택되지 않은 클립이 사이에 끼어있으면 겹칠 수 있음(문서화된 단순화).

**타임라인 편집기 — 트랙 추가 + 클립 추가(업로드) (2026-07-30 "Proceed" 요청, §1.3 5·6번 완료)**: `TimelineTrack`의 `@@unique([timelineId, type])` 제약을 제거하고 `autoSync: Boolean` 필드를 추가해 다중 트랙을 지원하도록 스키마를 재구성(마이그레이션 `20260729234241_timeline_multi_track_and_uploads`). `TimelineTrackType`에 참조 사이트 트랙 메뉴에 있던 `SFX`(효과음)도 추가.

1. **트랙 추가**: `+ 트랙 추가` 드롭다운(비디오/이미지/TTS/BGM/효과음/자막) — 새 트랙은 항상 `autoSync=false`로 생성되어 `syncTimeline`이 절대 만들거나 지우지 않는다. BGM은 참조 사이트와 동일하게 최대 2개(자동 1개+수동 1개)로 제한.
2. **트랙 삭제**: `autoSync=false`인(사용자가 만든) 트랙만 삭제 가능 — 자동 트랙은 서비스 레이어에서 거부.
3. **클립 추가(직접 업로드)**: 각 트랙 행의 `+` 버튼으로 파일 업로드 → 새 `UploadedMedia` 모델(video/image/audio, 프로젝트 스토리지의 `uploads/` 하위에 저장)에 저장하고, 현재 재생헤드 위치에 타임라인 클립 생성(`payload.mediaId`로 참조 — 자동 파이프라인의 `sourceId`와는 별도 개념). 파일 크기 제한: 비디오 500MB/이미지 10MB/오디오 50MB(참조 UI 캡처 기준). "AI 생성"/"무료 검색"/"릴박스 라이브러리" 경로는 §1.3 6번에서 이미 제외 확정한 대로 구현하지 않음 — 직접 업로드만.
4. **실제 검증**: 실제 2초짜리 mp4를 curl로 VIDEO 트랙에 업로드 → 파일 저장·길이 자동 인식(ffprobe)·클립 생성·파일 서빙(`GET /api/projects/:id/media/:mediaId/file`)까지 확인. BGM 3번째 추가 시도 시 거부, 자동 트랙 삭제 시도 시 거부, 동기화(`syncTimeline`) 후에도 수동 트랙/클립이 그대로 보존되는 것까지 curl로 검증.
5. **범위 제외(디스클로저)**: 여러 비디오/이미지 트랙을 실제로 합성해 최종 렌더링에 반영하는 것(레이어 합성·전환·마스크 적용)은 이번 라운드에 포함하지 않았다 — 이미 "비디오 속성 패널: 데이터만 준비, 렌더링 미연결"로 남긴 것과 동일한 패턴. VIDEO 클립이 실제로 존재하게 된 지금은 §5.2 비디오 속성 패널(변환/효과/전환/키프레임/마스크)을 실제로 열어볼 수 있지만, 그 값들이 최종 영상에 반영되려면 별도의 비디오 컴포지팅 엔진(ffmpeg overlay/chromakey 등)이 필요하며 이는 후속 라운드 과제로 남긴다.

**클립 속성 패널 — 시간 편집 형식 + 자막 멀티 셀렉트 일괄 편집 (2026-07-30 사용자 피드백, 참조 캡처 근거)**: ⚠️ 이 항목은 구현 착수 후에 기재됨 — 사용자가 실제 앱 스크린샷으로 "자막 속성 탭 중 2개가 참조 사이트와 다르다"고 지적한 즉시 원인 파악 겸 수정에 들어갔고, 착수 전 기재 원칙(본 문서 상단)을 지키지 못했다. 다음부터는 이런 지적성 피드백도 먼저 기재하고 진행한다.

1. **기본 정보 탭 시간 표시/편집**: 초 단위 소수점 입력 대신 참조 사이트와 동일한 `MM:SS.mmm` 형식(예: `01:04.560`)으로 표시·편집. "지속" 값은 초 3자리+ms 병기, Alt+↑/↓로 100ms 단위 조절 지원.
2. **자막 탭 멀티 셀렉트 일괄 편집 모드**: 같은 트랙에서 2개 이상 클립이 선택되면 "N개 클립 선택됨 (일괄 편집 모드)"로 전환 — 개별 텍스트 편집(텍스트박스+저장)은 비활성화하고, "자막 재구성 (R) - N개" 버튼만 선택된 클립 각각에 개별 적용(공유 텍스트로 병합하지 않음).
3. **"수동 줄바꿈 적용됨" 경고**: 저장된 텍스트가 이미 개행을 포함하는데 현재 기준 글자수로 다시 자동 줄바꿈한 결과와 다르면 경고 배지 표시.

`formatMmSsMs`/`parseMmSsMs`를 `src/lib/timeline.ts`로 옮겨 단위 테스트 추가, `tsc`/`lint`/vitest(379/379) 확인.

**미리보기 탭 자막 스타일 미반영 버그 수정 (2026-07-30)**: 미리보기 탭의 자막 오버레이가 스타일 탭 설정과 무관하게 항상 고정 스타일(검은 배경/흰 글자/고정 크기)로 하드코딩되어 있던 버그 수정. `resolveSubtitleStyle` 결과를 그대로 써서 폰트/크기/색상/배경색·투명도/위치/테두리가 실제 렌더링(ASS 번인)과 동일하게 반영되도록 함. 미리보기 컨테이너의 실측 너비를 재서(ResizeObserver) 절대 px 값들을 화면 크기 비율로 축소.

**이미지 클립 속성 패널 (2026-07-30 요청, 참조 캡처 근거)**: ⚠️ 이 항목도 착수 전 기재 원칙을 지키지 못하고 구현 후 기재함. 사용자가 참조 사이트의 이미지 클립 속성 패널(기본정보/변환/이미지 효과/전환/키프레임/특수효과/마스크, 7개 서브탭) 캡처를 제공하며 구현 요청.

1. §5.2 문서상 "비디오 클립과 동일 구조(변환/전환/키프레임/마스크), 비디오 옵션 탭 없음, 이미지 효과에 패닝/줌(켄 번즈) 섹션 추가"에 따라, 기존 `VideoClipProperties`를 `kind: "video" | "image"` 파라미터를 받는 공용 `MediaClipProperties`로 리팩터링해 두 클립 타입이 변환/전환/키프레임/마스크 로직을 공유하도록 함.
2. `ImageClipEffects` 타입 신설(`VideoClipEffects`의 상위집합 — 색보정 필드는 그대로 두고 `panEnabled/panDirection/panSpeed/zoomEnabled/zoomType/zoomIntensity` 추가). `PersistedClipPayload.effects`를 `Partial<ImageClipEffects>`로 넓혀 비디오·이미지 둘 다 커버.
3. `ClipPropertiesPanel`이 IMAGE 트랙 클립도 서브탭 패널로 라우팅하도록 부모 컴포넌트의 분기 조건 확장.
4. 실제 이미지 클립에 PATCH로 패닝/줌 효과 + 변환(위치/스케일)을 curl로 검증 — 서로 다른 필드(effects/transform)를 순차로 PATCH해도 이전 값이 지워지지 않고 병합되는 것까지 확인.
5. **범위 제외(디스클로저)**: 비디오와 마찬가지로 이 값들은 아직 최종 렌더링(이미지 슬라이드쇼 생성)에 반영되지 않는다 — 데이터 모델/UI만 준비된 상태이며, 실제 반영은 후속 라운드(이미지 컴포지팅/켄 번즈 효과를 ffmpeg 필터로 구현)에서 진행한다.

**이미지 마스크 — 미리보기 탭 실시간 시각화 (2026-07-30 요청)**: 이미지 클립의 "마스크" 탭에서 마스크를 추가하면 설정된 크기/좌표/모양대로 미리보기 탭의 실제 이미지에도 적용되어야 한다는 요청. 미리보기 탭에 클립별 인라인 SVG `<mask>`(objectBoundingBox 좌표계 — mask.x/y/width/height가 이미 0~1 정규화 값이라 그대로 대응)를 생성해 `<img>`에 `mask-image`/`-webkit-mask-image`로 적용. 사각형/타원 모양, 위치, 크기, 회전, 둥근 모서리(rx), 반전을 지원. **알려진 단순화**: 페더링(경계 부드러움, featherPx)은 SVG 유닛 환산이 까다로워 이번 라운드에서는 반영하지 않음(항상 하드 엣지로 표시) — 실제 렌더링과 마찬가지로 미리보기 근사치이며, 브라우저 시각 확인은 세션에 브라우저 도구가 없어 실제 데이터 저장/조회만 curl로 검증함.

**이미지 마스크 — 미리보기 화면에서 직접 드래그 편집 (2026-07-30 요청, 참조 캡처 근거)**: 사용자가 참조 사이트(reelbox.ai) 캡처를 근거로, "마스크 추가"를 누르면 미리보기 화면에 즉시 점선 테두리 + 크기조절/회전 핸들이 표시되고 마우스로 바로 크기·위치·회전을 조절할 수 있어야 한다고 지적. 원인 조사 결과 두 가지 문제가 결합되어 있었음:
1. 미리보기 탭의 이미지는 재생헤드(playhead) 위치로만 결정되고 속성 패널에서 선택한 클립과는 무관했다 — 클립을 클릭해 선택해도 재생헤드가 그 클립 구간으로 이동하지 않아, 마스크를 편집 중인 클립이 화면에 아예 표시되지 않는 경우가 많았음(표시되는 이미지는 재생헤드가 위치한 다른 클립).
2. 마스크 시각화(위 항목)는 정적 렌더링만 구현되어 있었고, 드래그로 조작 가능한 핸들 UI가 없었음.

**해결 방향**: (a) 타임라인에서 클립을 단일 클릭으로 선택하면 재생헤드를 그 클립의 시작 시각으로 이동시켜, 미리보기가 항상 선택한 클립을 보여주도록 수정. (b) 이미지 속성 패널의 "마스크" 탭이 활성화되어 있고 해당 클립에 마스크가 있으면, 미리보기 화면 위에 점선 테두리 오버레이 + 8개 크기조절 핸들(모서리 4개 + 변 중앙 4개) + 회전 핸들(상단 중앙, 원형 핸들과 연결선)을 렌더링. 드래그로 위치/크기/회전을 실시간 조절하고 마우스를 뗄 때 PATCH로 저장(사각형은 반대쪽 모서리 고정 방식 리사이즈, 회전 중에는 회전 좌표계 기준으로 리사이즈 방향을 보정). **알려진 제약**: 회전 보정 계산은 정규화 좌표(가로 0~1, 세로 0~1)를 화면 픽셀 기준으로 그대로 회전시키는 방식이라, 세로/가로 비율이 다른 쇼츠(9:16) 프레임에서는 기존 마스크 렌더링과 동일하게 근사치이다(실제 렌더링 파이프라인 연결 전까지는 미리보기 근사로 유지).

**이미지 효과(색보정/패닝/줌) — 미리보기 탭 실시간 반영 (2026-07-30 요청)**: "이미지 효과" 탭에서 색보정 프리셋/밝기/대비/채도/색온도나 패닝·줌 효과를 바꿔도 미리보기 화면에 아무 변화가 없다는 지적. 지금까지 `effects` 필드는 마스크와 마찬가지로 UI/데이터만 저장되고 미리보기·렌더링 어느 쪽에도 연결되어 있지 않았음. 마스크/자막 스타일 때와 동일한 "미리보기 근사치" 패턴으로 다음을 추가한다:
1. 색보정 20종 프리셋 각각에 대응하는 CSS `filter` 레시피(brightness/contrast/saturate/grayscale/sepia/hue-rotate 조합)를 정의하고, 밝기/대비/채도/색온도 슬라이더 값을 추가 필터로 곱연산 합성해 미리보기 `<img>`에 `filter` 스타일로 적용한다(`resolveImageEffectsFilter`, `src/lib/timeline.ts`, 단위 테스트 포함).
2. 패닝/줌(켄 번즈) 효과는 재생헤드가 해당 클립 구간 내에서 진행된 비율(0~1)에 따라 `scale()`/`translate()` CSS 트랜스폼을 계산해 적용한다(`resolveImageKenBurnsTransform`). 패닝 방향이 "랜덤"이면 클립 ID를 해시해 고정된 방향을 결정(같은 클립은 항상 같은 방향 재생). 패닝 활성화 시 프레임 밖 여백이 보이지 않도록 최소 배율(bleed)을 확보한다.
3. **범위 제외**: 비디오(VIDEO) 트랙 클립은 미리보기 탭에 애초에 렌더링되는 자리가 없어(§1.3 disclosure 참조) 이번 라운드에서는 이미지 클립에만 적용한다. 실제 렌더링(ffmpeg) 파이프라인 연결은 별도 후속 과제로 유지 — CSS 필터/트랜스폼은 근사치이며 ffmpeg 필터 결과와 픽셀 단위로 일치하지 않는다.

**TTS/BGM 클립 전용 속성 패널 (2026-07-30 요청, 참조 캡처 근거)**: 사용자가 참조 사이트의 TTS 클립 속성 패널(기본정보/오디오 2개 서브탭) 캡처를 제공하고, 이어서 "BGM 클립도 동일한 속성 UI를 가짐"이라고 추가 지적. 지금까지 TTS/BGM/AUDIO(비디오 오디오)/SFX 클립은 모두 기존 플랫 패널(시작/종료초 입력 + TTS만 텍스트 편집)만 쓰고 있었음(§1.3 disclosure). 이번 라운드는 **TTS와 BGM 두 트랙 타입만** 참조 캡처 구조로 전용 패널을 구현한다(AUDIO/SFX는 범위 제외, 다음 라운드).
1. `PersistedClipPayload`에 `audioOptions?: Partial<{volume, muted, speed}>` 필드 추가(기본값 volume 1, muted false, speed 1).
2. "기본 정보" 탭은 기존 `BasicInfoTab`(클립 정보 + MM:SS.mmm 시간 편집)을 그대로 재사용.
3. "오디오" 탭: 클립 텍스트(TTS만 해당) 미리보기 카드, "모든 TTS/BGM에 설정 적용" 체크박스(같은 트랙 타입 전체에 적용), 음소거 체크박스 + 볼륨 슬라이더(0~2, 기본 1), 재생 속도 슬라이더(0.25~4x, 기본 1).
4. **재생 속도 변경 시 클립 길이 변경**: 참조 사이트 캡처의 "연결된 자막이 있습니다. 속도 변경 시 자막 클립 길이도 함께 조정됩니다" 안내를 그대로 구현 — TTS 클립의 속도를 바꾸면 (기존속도/새속도) 비율만큼 클립 길이를 재계산하고, `sourceId`가 같은(같은 스크립트 세그먼트 유래) SUBTITLE 클립이 있으면 동일 비율로 길이를 맞춘다. **알려진 단순화**: 트랙 내 다음 클립과 겹치지 않도록 그 클립 시작 시각까지만 클램프하지만, 이후 클립들을 밀어서 간격을 자동으로 유지해주지는 않는다(호흡구간 추가처럼 전체 캐스케이드하지 않음 — 필요하면 "트랙 갭 제거"를 별도로 실행). BGM은 연결된 자막이 없어 이 단계가 자연히 스킵된다.
5. 실제 오디오 재생 속도(피치/타임스트레치)는 아직 렌더링에 반영되지 않는다 — 비디오 클립의 "재생 속도" 옵션과 동일하게 데이터/클립 길이만 조정하고, ffmpeg atempo 등 실제 오디오 처리 연결은 후속 과제로 유지(§1.3에 이미 있는 disclosure와 동일 패턴).

**TTS/BGM 오디오 탭 — 페이드 인/아웃 (2026-07-30 요청)**: 위 TTS/BGM 속성 패널 캡처를 다시 확인한 사용자가 "BGM 속성에는 페이드 효과 주는 옵션이 추가로 존재한다"고 지적(오디오 탭 하단 "페이드 설정" 섹션, 페이드인/페이드아웃 ms 슬라이더). `AudioClipOptions`에 `fadeInMs`/`fadeOutMs`(기본 0)를 추가해 TTS/BGM 공용으로 구현한다. **범위 제외**: 참조 캡처의 "모든 BGM에 설정 적용 (덕킹 설정 제외)" 문구에 있는 "덕킹"은 이번 요청에 포함되지 않았고 우리 앱에 해당 기능 자체가 없으므로 구현하지 않는다 — 체크박스 안내 문구에도 존재하지 않는 기능을 언급하지 않는다. 볼륨/속도와 마찬가지로 실제 오디오 렌더링(페이드 곡선 적용)에는 아직 연결되지 않은 데이터/UI뿐이다 — BGM의 실제 브라우저 미리듣기 재생조차 이 클립별 `audioOptions`가 아니라 별도의 프로젝트 단위 `BgmSettings`(volumeDb/loop/playbackSpeed)로 제어되고 있어(§1.3 확인됨), 클립별 옵션은 애초부터 미리듣기에도 영향이 없는 순수 데이터임을 이미 알고 있다.

**실행취소(Ctrl+Z)/다시실행(Ctrl+Y) — 클립 삭제·구조 변경까지 포함하도록 확장 (2026-07-30 요청)**: 사용자가 TTS 클립 하나를 삭제해본 뒤 Ctrl+Z로 복구가 안 된다고 지적하며, "모든 트랙에서 Ctrl+Z/Ctrl+Y는 기본 제공해야 한다"고 요구. 원인: 기존 실행취소 스택(`history`/`future`)은 클립의 `startMs`/`endMs`만 기록하는 `TimingSnapshot`이었고(드래그/트림 전용으로 설계됨), 삭제·분할·붙여넣기·복제·갭 제거·호흡구간 추가·길이 스케일 같은 구조 변경 작업들은 실행 후 오히려 `setHistory([]); setFuture([])`로 스택을 **비워버리고** 있어 실행취소 자체가 불가능했다(삭제된 클립은 애초에 스냅샷에 시간만 있고 존재 여부가 없어 복원 대상도 아니었음).
1. 스냅샷을 `{id, trackId, startMs, endMs, payload}` 전체로 확장(`snapshotClips`, `timeline-editor-client.tsx`)하고, 서버에 `restoreClipsSnapshot`(`timeline.service.ts`)을 추가해 스냅샷 시점에 없던 클립은 삭제, 스냅샷에 있는데 지금 없는 클립은 원래 id 그대로 재생성(upsert create), 남아있는 클립은 시간/payload를 스냅샷 값으로 되돌리는 방식으로 "그 시점 상태"를 온전히 복원한다. 신규 엔드포인트 `PUT /api/projects/:id/timeline/clips/snapshot`.
2. 기존 `PUT /api/projects/:id/timeline/clips/bulk`(시간만 복원하던 `bulkRestoreClipTimings`)는 새 스냅샷 방식으로 완전히 대체되어 제거한다(DELETE 핸들러는 그대로 유지 — bulk 삭제 용도로 계속 사용됨).
3. 삭제/잘라내기/붙여넣기/복제/분할/트랙 갭 제거/선택 사이 갭 제거/호흡구간 추가/길이 스케일/드래그·트림(타이밍) — 클립 목록 자체를 바꾸는 모든 조작 앞에서 현재 상태를 `history`에 push하도록 통일(`pushHistory()`).
4. **범위 제외(디스클로저)**: 트랙 추가/삭제, 미디어 업로드, 그리고 속성 패널의 개별 필드 편집(자막 텍스트/스타일/마스크/이미지 효과/변환/오디오 옵션 등 `patchClip` 계열)은 이번 라운드에서 실행취소 스택에 기록하지 않는다 — 계속 지원되던 "삭제→복구 안 됨" 핵심 시나리오와 클립 목록 자체가 바뀌는 조작에 집중했다. 부작용으로, 트랙 추가/미디어 업로드처럼 기록되지 않는 조작 **이후에** 만든 스냅샷을 실행취소하면 그 사이에 추가된 클립도 함께 사라질 수 있다(스냅샷이 "그 시점의 전체 클립 목록"이기 때문). 속성 필드 실행취소는 후속 라운드 과제로 남긴다.

**편집 도구 툴바 — 아이콘+툴팁 전환, 분할/삭제를 속성 패널에서 툴바로 이동 (2026-07-30 요청, 참조 캡처 근거)**: 사용자가 참조 사이트 툴바 캡처(재생 컨트롤 옆에 실행취소/다시실행/가위/복사/붙여넣기/휴지통 + 리스트·교환·저울 모양 아이콘이 툴팁과 함께 나열된 형태)를 제공하며 3가지 요청:
1. 우리 앱의 "편집 도구" 텍스트 버튼 행(복사/잘라내기/붙여넣기/복제/삭제/트랙 갭 제거/선택 사이 갭 제거/호흡구간 추가/길이 스케일)과 상단 툴바의 실행취소/다시실행 버튼을 아이콘+마우스오버 툴팁 방식으로 전환한다. 참조 캡처의 미확인 아이콘 3개는 사용자에게 직접 확인(질문/답변으로 진행): 리스트 모양→트랙 갭 제거, 교환 화살표 모양→TTS 호흡구간 추가, 저울 모양→목표 길이 맞추기. 이 매핑대로 아이콘을 고른다(lucide-react): 실행취소 `Undo2`/다시실행 `Redo2`/분할 `SplitSquareHorizontal`/잘라내기 `Scissors`/복사 `Copy`/붙여넣기 `ClipboardPaste`/복제 `CopyPlus`/삭제 `Trash2`/트랙 갭 제거 `List`/선택 사이 갭 제거(참조에 없어 임의 선정) `FoldHorizontal`/호흡구간 추가 `ArrowLeftRight`/목표 길이 맞추기 `Scale`. 호흡구간(ms)·목표길이(초) 같은 수치 입력이 필요한 두 기능은 아이콘 버튼 옆에 기존 select/input을 축소된 형태로 그대로 유지한다(참조처럼 팝오버로 감싸는 것은 이번 라운드 범위 밖).
2. 각 클립 속성 패널의 "기본 정보" 탭(`BasicInfoTab`, 자막/비디오·이미지/TTS·BGM 공용)에 있던 "분할(재생헤드)"/"삭제" 버튼과, TTS/BGM 외 트랙(AUDIO/SFX)이 쓰는 기존 플랫 패널의 동일 버튼을 제거한다 — 이제 툴바 아이콘으로 통일. `canSplit`/`onSplit`/`onDelete` prop이 `BasicInfoTab` 경로에서만 쓰이던 것이라 관련 prop 스레딩도 함께 제거한다(`handleSplit`/`handleDeleteClip`/`canSplit` 자체는 툴바 아이콘과 S/Delete 단축키에서 계속 사용).
3. 참조 캡처에서 "잘라내기"와 "분할" 아이콘이 똑같이 보인다는 지적 — 우리 구현에서는 애초에 서로 다른 아이콘(`Scissors` vs `SplitSquareHorizontal`)을 배정해 시각적으로 구분한다.
4. 신규 의존성 `@radix-ui/react-tooltip` 설치 + shadcn 표준 패턴의 `src/components/ui/tooltip.tsx` 추가.

**편집 도구 툴바 — 후속 수정 5건 (2026-07-30 요청, 참조 캡처 근거)**: 위 아이콘 툴바를 실제로 확인한 사용자의 후속 지적 5가지.
1. **버그**: 분할 아이콘이 클립을 선택해도 활성화되지 않음 — 원인은 "마스크 미리보기" 수정(§1.3) 때 클립 선택 시 재생헤드를 클립의 **정확히 시작 시각**으로 이동시켰기 때문. `canSplit`은 재생헤드가 클립 시작보다 **엄격히 커야**(`>`) 참이 되므로, 선택 직후에는 항상 `false`였다. 재생헤드 이동 지점을 클립 시작이 아니라 **중간 지점**으로 변경해 선택 즉시 분할 가능하도록 수정(미리보기 동기화 효과는 그대로 유지됨 — 중간 지점도 해당 클립 구간 안이므로).
2. **버그**: 비활성화된 아이콘 버튼은 `disabled:pointer-events-none`(shadcn Button 기본 스타일) 때문에 마우스오버 이벤트 자체가 발생하지 않아 툴팁이 뜨지 않았음 — `TooltipTrigger`를 감싸는 `<span>`으로 하위 `Button`을 감싸 hover 이벤트가 span에서 잡히도록 수정(비활성 상태에서도 툴팁 문구는 항상 보이도록).
3. 호흡구간 추가 아이콘을 참조 캡처 기준으로 `ArrowLeftRight`(좌우 화살표 겹침) 대신 `Wind`(입김을 부는 형상에 가장 가까운 lucide 아이콘)로 교체.
4. 호흡구간 추가 아이콘 클릭 시, 참조 사이트처럼 툴바가 확장되어 인라인으로 "호흡: [드롭다운] ✓ ✗"이 나타나는 구조로 변경(항상 보이던 select+버튼 대신, 아이콘 클릭 시에만 펼쳐짐). ✓ 클릭 시 적용 후 닫힘, ✗ 클릭 시 취소하고 닫힘.
5. 목표 길이 맞추기 아이콘도 동일하게 클릭 시 확장되어 "목표: [MM:SS.mmm 입력] (최대 30:00.00) ✓ ✗"이 나타나도록 변경. 입력 형식을 기존의 "초 단위 숫자"에서 클립 속성 패널의 시간 입력과 동일한 MM:SS.mmm 텍스트 포맷(`formatMmSsMs`/`parseMmSsMs` 재사용)으로 바꾸고, 최대값은 이미 존재하던 렌더링 길이 제한(1800초 = 30:00.00, 유효성 검사 패널에 이미 안내되어 있음)과 동일하게 맞춘다.

```
POST   /api/projects                  프로젝트 생성
GET    /api/projects?q=&channel=&status=&type=&sort=   목록+검색+필터
PATCH  /api/projects/:id              제목/설명/검수상태/설정 수정
DELETE /api/projects/:id
POST   /api/projects/:id/script       대본 생성 (Anthropic API)
POST   /api/projects/:id/images       장면별 이미지 생성
POST   /api/projects/:id/tts          TTS 생성
POST   /api/projects/:id/render       FFmpeg 렌더링 (비동기)
GET    /api/projects/:id/events       SSE 진행률 스트림
POST   /api/projects/:id/upload       YouTube 업로드 (OAuth)
GET    /api/channels                  채널 목록/기본값 관리
```

---

## 2. 기능 2 — YouTube 데이터 분석 대시보드

### 2.1 현실적 범위 설정 (중요)

reelbox는 YouTube + TikTok + Instagram + Douyin + Kuaishou 멀티플랫폼 검색을 제공하지만,
**TikTok/Instagram/Douyin/Kuaishou는 공개 검색 API가 사실상 없거나 매우 제한적**이다.
비공식 스크래핑은 각 플랫폼 약관 위반 소지가 있으므로 이 프로젝트에서는:

- **1차 범위: YouTube Data API v3 기반 기능만 구현** (아래 표에서 ✅)
- 타 플랫폼은 인터페이스만 설계해 두고(어댑터 패턴), 추후 합법적 서드파티 데이터 API를 붙일 수 있게 확장 포인트만 남긴다

또한 YouTube Data API는 **일일 쿼터 10,000 units** 제한이 있으므로(search.list는 1회 100 units),
모든 API 응답을 DB에 캐싱하고 TTL(예: 인기 영상 1시간, 채널 정보 24시간)을 두는 것이 필수다.

### 2.2 메뉴 구성 및 범위

| # | 메뉴 | 설명 | 범위 |
|---|---|---|---|
| 2.1 | 홈 | 오늘의 AI 아이디어(저장된 관심 키워드 기반 Anthropic API 생성) + 요약 위젯(내 채널 지표, 최근 떡상 영상) | ✅ 1차 |
| 2.2 | 소스 발굴 | 해외 YouTube 검색 (지역 코드 US/JP/글로벌 등 지정, 키워드/카테고리) | ✅ 1차 (YouTube만) |
| 2.3 | 쇼핑 소스 발굴 | 상품 영상(언박싱·리뷰·하울) 키워드 프리셋 검색 + 영상 설명에서 상품/링크 추출 + 쇼핑 쇼츠 아이디어 AI 생성 | ⏩ 2차 (YouTube만) |![alt text](image.png)
| 2.4 | 탐색·분석 | 지금 인기 영상(mostPopular, 지역·카테고리별) + 키워드 시장성(검색량 프록시: 검색 결과 수·평균 조회수·경쟁 채널 규모 분석) | ✅ 1차 |
| 2.5 | 채널 분석 | 채널 URL/ID 입력 → TOP 영상, 전체 영상 스캔(uploads 플레이리스트 페이지네이션), 업로드 시간대별 성과(골든시간 히트맵), 썸네일 그리드 분석 | ✅ 1차 |
| 2.6 | 한·영 비교 | 동일 카테고리의 KR vs US 인기 영상 나란히 비교 → "한국화 갭"(해외에서 뜨는데 한국에 없는 소재) AI 분석 | ⏩ 2차 |
| 2.7 | 제목 변경 이력 | 즐겨찾기 채널의 영상 제목을 주기적으로(cron) 스냅샷 → 제목 변경 감지 + 변경 전후 조회수 추이 비교 | ⏩ 2차 |
| 2.8 | 영상 SEO | 영상 URL 1개 입력 → 제목/설명/태그 SEO 진단, 댓글 수집·감성 요약(AI), 유사 영상 리스트 | ✅ 1차 |
| 2.9 | 떡상 영상 ⭐ | 키워드 검색 결과에서 채널 평균 조회수 대비 N배(기본 5배) 이상 폭증한 영상 필터링 | ✅ 1차 (핵심 기능) |
| 2.10 | 영상 분석 모달 | 모든 탭의 영상 카드 클릭 → 모달: 개요/통계/자막/댓글/SEO/유사영상/썸네일/채널정보/AI 아이디어 9탭 | ✅ 1차 (자막·댓글·AI아이디어 우선) |
| 2.11 | 저장됨 | 영상/채널/아이디어 스냅샷 저장(당시 지표 보존) + 저장 항목 기반 대본 생성 버튼 → 기능 1의 새 프로젝트로 연결 | ✅ 1차 |

### 2.3 핵심 로직 상세

**홈 (2.1) "오늘의 AI 아이디어" 직접 입력 모드 — UI 수정 요청 (2026-07-24 수정, 원본 서비스 스크린샷 기준)**
- 헤더 한 줄에 모드 토글(자동/직접 입력)과 `아이디어 생성` 버튼을 함께 배치한다 (버튼을 별도 줄에 분리하지 않는다).
- 직접 입력 모드는 옅은 배경으로 구분되는 박스 안에 다음을 배치한다:
  1. 안내 문구: "직접 주제·타깃·카테고리를 지정해 5개 아이디어를 받습니다 (저장된 자동 모드 결과는 그대로 유지됨)."
  2. `주제/제품` 입력 (placeholder 예: "다이어트 / 비트코인 ETF / 스탠리 텀블러")
  3. `타깃 시청자` 입력 (선택, placeholder 예: "30대 직장인 / 자취생")
  4. `카테고리` (선택, 1개 클릭) — 자유 텍스트 입력이 아니라 `없음` + 니치 카테고리 목록(16종, lib/niche-catalog.ts와 동일) 중 단일 선택 칩
- **자동/직접 입력 모드는 서로 다른 결과를 독립적으로 저장한다** — 안내 문구가 명시하듯 직접 입력으로 생성해도 그날의 자동 모드 결과가 사라지면 안 된다. 즉 "오늘의 아이디어" 저장 키는 날짜만이 아니라 (날짜, 모드) 조합이어야 한다 (기존 구현은 date만으로 upsert해 모드 전환 시 서로 덮어썼음 — 수정 필요).
- 크레딧 표시는 계속 범위 밖(§2.1 실비용 투명 표시 정책 유지) — 버튼 라벨에 크레딧 수치를 넣지 않는다.

**홈 (2.1) "나의 니치" 재설정 기능 누락 — 수정 요청 (2026-07-26 수정)**
- 현재는 니치를 최초 1회 설정하고 나면(niches.length > 0) 니치 설정 카드가 영구히 사라져 다시 변경할 UI 경로가 없다. "나의 니치:" 칩 목록 옆에 `초기화` 버튼을 추가한다(원본 서비스 화면에서 같은 위치에 있던 크레딧 뱃지 자리를 재활용 — 크레딧 수치가 아니라 "초기화" 텍스트로 대체).
- 클릭 시 니치를 빈 배열로 저장(`PUT /api/insight/home/niches` 재사용) → 니치 설정 카드가 다시 나타나 새로 선택할 수 있다. 니치가 0개일 때는 이 버튼 자체를 표시하지 않는다.

**홈 (2.1) "오늘의 AI 아이디어"/"니치 인기 영상" 연결성 부족 — 수정 요청 (2026-07-27 수정, 원본 서비스 스크린샷 기준)**
- 오늘의 AI 아이디어 카드: 현재는 저장 버튼만 있어 아이디어가 실제 YouTube 시장 데이터와 단절돼 있다.
  1. 카드에 YouTube 검색 링크 추가 — 아이디어 키워드로 `https://www.youtube.com/results?search_query=...`를 새 탭에서 연다 (아이디어 자체는 특정 영상이 아니라 AI 생성물이므로 특정 videoId 링크가 아닌 검색 링크).
  2. `🔍 분석` 버튼 추가 — 클릭 시 탐색·분석 탭의 분석(analyze) 모드로 이동하며 아이디어의 대표 키워드가 자동 입력되어 즉시 시장성 분석이 실행된다 (`/analytics/explore?mode=analyze&keyword=...` 딥링크, 탐색·분석 탭에 URL 파라미터 처리 추가 필요 — 현재는 탭 내부 "핵심 토픽" 클릭시에만 analyze 모드로 전환되고 외부 링크로는 진입 불가).
- 니치 인기 영상 위젯: 현재 제목·조회수만 텍스트로 나열된다.
  1. 각 항목에 썸네일 이미지를 추가한다.
  2. 항목 클릭 시 `https://www.youtube.com/watch?v={영상ID}`를 새 탭에서 연다.
  3. `더보기` 링크는 탐색·분석 탭으로 이동하되, 현재 니치가 자동 선택된 상태로 이동한다 (`/analytics/explore?niche=...` 딥링크, 탐색·분석 탭에 니치 파라미터 처리 추가 필요).

**소스 발굴 (2.2) — UI 확장 요구사항 (2026-07-20 수정, 원본 서비스 스크린샷 기준)**
- 최초 구현은 컨셉 입력 + 지역 1개 선택 + 한국 콘텐츠 제외 토글만 있는 축소판이었으나,
  원본 서비스 스크린샷 확인 결과 아래 폼 요소로 확장한다 (UI_SPEC.md §7.1 "소스 발굴" 문서 내용과 일치):
  1. **지역 다중 선택** — 7그룹 칩: 영어권(미국/영국/캐나다/호주/뉴질랜드/아일랜드/싱가포르/인도),
     서유럽(독일/프랑스/네덜란드/벨기에/오스트리아/스위스/이탈리아/스페인/포르투갈),
     북유럽(스웨덴/노르웨이/덴마크/핀란드/아이슬란드),
     동유럽(폴란드/체코/헝가리/루마니아/그리스/우크라이나/러시아/튀르키예),
     아시아(일본/대만/홍콩/인도네시아/태국/베트남/말레이시아/필리핀),
     라틴아메리카(브라질/멕시코/아르헨티나/콜롬비아/칠레),
     중동·아프리카(UAE/사우디/이집트/이스라엘/남아공) — `전체 선택`/`전체 해제` 버튼 포함
  2. **언어 다중 선택 칩 15종** — 영어/일본어/중국어/스페인어/독일어/프랑스어/이탈리아어/포르투갈어/러시아어/폴란드어/튀르키예어/인도네시아어/태국어/베트남어/아랍어
  3. **길이 / 게시일 / 최소 조회수 / 정렬** 드롭다운 4종 (옵션값은 UI_SPEC.md §7.1 참고)
  4. **제목 자동 번역** 체크박스 (해외 영상 제목을 한글로 변환해 병기)
  5. **한국 콘텐츠 제외** 토글 (기본 ON, 기존 구현 유지)
- **범위 제외(변경 없음)**: TikTok·Instagram 플랫폼 검색, 크레딧 잔액/차감 UI는 계속 범위 밖 — 스크래핑 금지 방침(§2.1) 및 실비용 투명 표시 정책에 따라 YouTube Data API만 사용하고 비용은 크레딧이 아닌 실제 API 사용량으로 표시한다.
- 다중 지역·다중 언어 선택은 백엔드에서 지역별 병렬 검색 후 중복 제거·병합하는 방식으로 구현한다 (YouTube search.list는 다중 지역·언어를 한 번에 받지 않으므로).

**탐색·분석 (2.4) — UI/기능 전체 확장 요구사항 (2026-07-24 수정, 원본 서비스 화면·설명 기준)**
- 현재 구현은 browse/analyze 토글 UI만 있고 실제로는 지역 4개·카테고리 13개 단순 Select + `mostPopular` 차트 고정 조회 + 단일 키워드 시장성 점수뿐이다. UI_SPEC.md §7.1 "탐색·분석 — 절대 기준 시장조사"에 이미 상세 확정되어 있는 아래 사양대로 **전체를 다시 구현**한다.
- **browse(탐색) 모드**
  1. 국가 + 카테고리 + 기간 필터로 조회수 상위 최대 100개 조회. 기간=24h이고 검색어 없음 → YouTube 공식 인기 차트(`mostPopular`) 사용, 그 외 조합(검색어 있음 또는 기간≠24h) → `search.list` 결과를 조회수 순 정렬로 전환.
  2. 비디오폼 필터: 쇼츠 / 롱폼 / 전체.
  3. 성능 등급 5단계 필터: 폭발적 / 상승세 / 안정 성장 / 장기 영상 / 정체 (등급 산출 로직 필요 — VPH·업로드 경과시간 기반 파생 지표).
  4. 최소 조회수 4단계: 1천+ / 1만+(추천 ★) / 10만+ / 100만+, "채널당 1개만" 옵션.
  5. 한국형 서브카테고리 칩 17종(1-click 진입, 키워드 검색+제목 매칭 필터로 구현 — YouTube 공식 카테고리 매핑 부정확성 보완).
  6. 결과 카드: VPH(시간당 조회수), 추정 수익(카테고리별 평균 단가 기반), 5개 액션 버튼(분석/드릴다운/자막 복사/대본 패턴/번역), 저장 버튼. VPH·성능등급·추정수익은 ReelBox 자체 파생 지표이며 YouTube 공식 지표가 아님을 명시하는 안내 문구 포함.
- **analyze(분석) 모드**
  1. 추천 점수 = 검색량 점수 × (1 − 경쟁도) 산식으로 명시.
  2. bulk 모드: 키워드 최대 10개를 한 번에 비교.
  3. "추천 키워드" 버튼 → 같은 주제의 연관 키워드 자동 제안.
  4. 추천 점수 상위 키워드를 "떡상 영상 키워드" 모드(2.9)로 재검색하는 연동 버튼.
- **자동 키워드 확장 (browse/analyze 공통, 버튼 없이 자동 적용)**: 키워드 1개 입력 시 연관 검색어 최대 3개를 자동 생성해 병렬 검색 후 병합 — 영상 표본이 최대 200개까지 늘어나고 파생 채널 풀도 확장된다.
- **범위 제외(변경 없음)**: 크레딧 잔액/차감 UI(실비용 투명 표시 정책 유지), TikTok/Instagram 등 타 플랫폼.
- 진행 방식은 사용자 확인 결과 "전체 확장"으로 결정 (browse+analyze+자동 키워드 확장 한 번에 구현).
- **추가 수정 (2026-07-26)**: 결과 카드의 영상 썸네일도 클릭 시 `https://www.youtube.com/watch?v={영상ID}`를 새 탭에서 열어 바로 재생할 수 있게 한다 (2.10에서 관련 영상 리스트에 적용한 것과 동일한 단순 외부 링크 방식).

**탐색·분석 (2.4) — analyze(분석) 모드 결과 고도화 (2026-07-27 수정, 원본 서비스 스크린샷 기준)**
- 현재 analyze 모드는 점수·통계 요약만 보여주고, 분석에 사용된 실제 영상 목록은 노출하지 않는다. 아래와 같이 보강한다.
- **KPI 3카드 병합**: 기존 `searchVolumeScore`(검색량)·`competitionRatio`(경쟁도, 0~1 비율 → 0~100 점수로 환산해 표시)·`recommendScore`(추천 점수)를 상단 3개 카드로 병합 표시한다. 각 카드에 보조 지표를 함께 표기: 검색량 카드에 `stats.medianViewCount`, 경쟁도 카드에 `stats.medianChannelSubscriberCount`, 추천 점수 카드에 `stats.videoCount`(표본 수). 기존 4항목(조회수/최신성/참여율/경쟁도) 세부 breakdown 막대는 하단에 "세부 분석"으로 유지한다(제거하지 않고 병행 표시).
- **종합 기회 점수(신규)**: 인기도(=검색량 점수) + 진입 난이도(=100−경쟁도 점수) + 신생 채널 비중(표본 중 구독자 10만 미만 채널 비율) + 최신성(게시 7일 이내=100점, 1년 이상=0점, 선형 보간)의 가중합. 기본 가중치는 각 25%. 가중치는 클라이언트에서 즉시 조정 가능(재계산은 서버 재호출 없이 이미 받은 breakdown으로 프런트에서 처리). "이 점수는 YouTube 공식 데이터가 아니며 자체 산출한 분석" 각주 포함.
- **상위 영상 미리보기(신규)**: 분석 표본(최대 50개, 화면엔 상위 25개) 각 영상을 리스트로 표시 — 썸네일(클릭 시 YouTube 새 탭), 제목, 채널명, 성능 등급+VPH 뱃지(탐색 모드와 동일 로직 재사용), 추정 수익 뱃지, 조회수, `자막`·`대본 패턴` 버튼(영상 분석 모달 재사용). 성능 등급 필터 칩(전체/폭발적/상승세/안정 성장/장기 영상/정체, 건수 표시)과 최소 조회수 필터(전체/1천+/1만+★/10만+/100만+)를 이미 받아온 목록에 클라이언트 필터로 적용한다(추가 API 호출 없음).
- **추천 키워드/태그(신규)**: 상위 20개 영상의 제목+태그 빈도를 분석해 관련 키워드·태그를 추천한다(탐색 모드의 "핵심 토픽"과 동일한 TF-IDF 근사 로직 재사용, AI 호출 아님 — 기존 "추천 키워드" 버튼은 Anthropic 기반 연관어 제안으로 별개 기능이며 그대로 유지).
- **복수 키워드(bulk) 비교 테이블**: 종합 기회 점수 컬럼 추가.
- 범위 제외: "키워드 저장"/"결과 저장" 버튼은 이번 항목에서 다루지 않는다(SavedItem 타입 확장이 필요한 별도 작업 — 요청 시 후속 진행).

**떡상 영상 (2.9) — 핵심 알고리즘**
```
1. 키워드로 search.list → 영상 목록 (최근 N일 필터)
2. videos.list로 각 영상 조회수 조회
3. 각 영상의 채널에 대해 channels.list → 채널 통계 + uploads 최근 50개 영상 median 조회수 계산 (캐싱)
4. ratio = 영상 조회수 / 채널 최근 median 조회수
5. ratio >= 임계값(기본 2배) 영상만 표시, 배수 순 정렬
6. 결과 캐싱 (동일 조건 1시간, 채널 median은 24시간)
```
(2026-07-26 수정: "최근 30개 평균" → "최근 50개 median", 기본 임계값 5배 → 2배로 UI_SPEC.md §7.1 확정 내용에 맞춰 정정. 코드는 이미 median 방식으로 구현돼 있었음 — 이 문서 문구만 구식이었음.)

**떡상 영상 (2.9) — 전체 확장 요구사항 (2026-07-26 수정, 원본 서비스 스크린샷·매뉴얼 문서 기준)**
- 현재 구현은 UI_SPEC.md §7.1 "떡상 영상 — 상대 기준, 재현 공식 발굴"에 이미 상세 확정된 내용 중 "영상 단위(키워드)" 모드 하나와 노이즈 필터 3종만 반영돼 있다. 아래 사양대로 **전체를 확장**한다.
- **모드 3종**:
  1. `영상 단위(키워드)` ⭐ 기본값(기존 구현 확장) — 키워드로 영상 검색 → 각 영상을 자기 채널 최근 업로드 50개 median과 비교. 자동 키워드 확장(연관어 3개) 적용.
  2. `채널 단위(카테고리)` (신규) — 국가+카테고리+선택적 시드 키워드로 상위 50개 채널 자동 선정(시드 없으면 공식 인기 차트 기반) → 각 채널의 기간 내 떡상 영상을 통합 정렬.
  3. `채널 ID` (신규) — UCxxx 채널 ID 직접 입력 → 그 채널의 최근 영상 중 떡상만 표시. 지역/카테고리/숨겨진 보석 옵션 없음.
- **💎 숨겨진 보석 모드** (키워드·카테고리 모드만 지원, 채널ID 모드 미지원): 체크박스 ON + 구독자 상한(1만↓/5만↓/10만↓ 기본/50만↓/100만↓) → 상한 초과 채널 및 구독자 비공개 채널의 영상을 서버에서 선필터링. 결과 중 구독자 1만 미만 채널의 영상에는 "신생 강자" 배지를 별도 표시(보석 모드 여부와 무관).
- **필터 확장**: 카테고리(기존 탐색·분석의 8종 재사용), 비디오폼(쇼츠/롱폼/전체, 기존 재사용), 기간 9단계(1시간/6시간/12시간/24시간/7일/30일/90일/1년/전체 기간), threshold 4단계(1.5/2/3/5배, 기본값 2배로 정정).
- **`[패턴 분석]` 버튼**: 결과 상위 떡상 영상들의 공통 훅·업로드 시간대·길이·주제를 Anthropic API로 자동 추출해 표시. 개별 카드의 `[대본 패턴]`과는 구분되는 결과-집합 단위 분석.
- **캐싱 신규 추가**: 채널 median(baseline)은 채널ID 단위로 24시간 캐시(모드·검색어가 달라져도 재사용되어 쿼터 절감), 최종 검색 결과는 조건 조합 단위로 1시간 캐시.
- 지역 선택지를 기존 4개(KR/US/JP/GB)에서 스크린샷 기준 15개국(대한민국/미국/일본/영국/독일/프랑스/인도/브라질/멕시코/베트남/인도네시아/태국/대만/스페인/러시아)으로 확장한다 — 탐색·분석 탭의 지역 선택도 동일 목록을 공유해 함께 확장된다.

**키워드 시장성 (2.4)**
- 검색 결과 상위 50개의 조회수 분포(중앙값/상위10%), 업로드 최신성, 참여율(좋아요/조회수), 경쟁 채널 구독자 분포를 종합해 0~100 스코어 산출
- 스코어 공식은 초기엔 단순 가중합, 대시보드에 근거 데이터 함께 표시

**영상 분석 모달 (2.10)**
- 자막: YouTube 자막 트랙 조회(가능한 경우) → 없으면 "자막 없음" 표시 (음성 STT는 범위 외)
- 댓글: commentThreads.list 상위 100개 → Anthropic API로 반응 요약/감성 분류
- AI 아이디어: 제목+자막+댓글 요약을 컨텍스트로 "이 소재로 만들 쇼츠 아이디어 5개" 생성 → 저장됨(2.11)으로 보관 가능

**영상 분석 모달 (2.10) — 댓글/SEO/종합분석 탭 고도화 요구사항 (2026-07-25 수정, 원본 서비스 "소스 영상 상세" 스크린샷 기준)**
- 현재 구현은 UI_SPEC.md §7.1 "영상 분석 모달 — 9탭 완전 확정"에 이미 상세 확정된 내용 중 일부만 반영되어 있다. 스크린샷 대비 격차를 다음과 같이 보강한다.
- **댓글 탭 파이프라인 재설계**: `listCommentThreads` 원본 응답에서 지금은 텍스트만 남기고 버리는 `authorDisplayName`·`likeCount`·(가능하면) 답글 수를 보존한다. 이를 바탕으로:
  1. KPI 4종: 분석 댓글 수 / 유니크 작성자 수 / 댓글 총 좋아요 합계 / 긍정 비율
  2. 댓글 의도 분류를 기존 3분류(긍정/중립/부정)에서 9종(전체/공감/놀람/수요/질문/요청/칭찬/비판/기타) 건수 체계로 확장 (Anthropic 스키마 재설계)
  3. 🔥 핵심 인사이트: 각 분류 카테고리의 좋아요 1위 댓글 + 활용 제안 문구
  4. 인기 댓글 리스트(좋아요 순): 분류 뱃지 + 원문 + 작성자 + 좋아요 수 + 답글 수
  5. 기존에 계산되지만 미표시 중인 `frequentQuestions`(자주 나오는 질문)도 함께 노출
- **SEO 탭 — 이미 계산된 데이터 연결**: `video-seo.service.ts`/`seo-score.ts`가 이미 산출하는 `tags`, `bestPractices`(5종 체크리스트), `suggestions`(AI 개선 제안)를 모달에 렌더링한다. "전체 태그 복사" 버튼(클립보드) 신규 추가.
- **종합 분석 탭 신설**: 기존 "통계" 탭과 별개로 새 탭 추가. `performance-tier.ts`(이번 세션에 탐색·분석용으로 만든 VPH/성과등급/예상수익 계산 함수)를 영상 분석 파이프라인에 연결해 KPI 카드 4종(조회수/VPH/성과등급/예상 수익)을 표시하고, 쇼츠 여부 뱃지, 챕터 파싱(설명란 타임스탬프 패턴), "같은 채널 인기 영상" 리스트와 "유사 컨셉 영상" 리스트를 채널 기준으로 분리해 표시한다.
- 범위: 이번 항목에서는 스크린샷에 나온 자막/댓글/SEO/종합분석 4탭 중 댓글·SEO·종합분석만 다룬다(자막 탭은 기존 안내형 구현 유지, 모멘텀 탭은 스크린샷에 없어 범위 밖).
- **추가 수정 (2026-07-26)**: 종합 분석 탭의 "같은 채널 인기 영상"·"유사 컨셉 영상" 리스트 각 항목에 링크를 건다. 클릭 시 `https://www.youtube.com/watch?v={영상ID}`를 새 탭에서 연다 (참조 서비스와 동일하게, 모달 내 재분석 방식이 아닌 단순 외부 링크).
- **추가 수정 (2026-07-26)**: "유사영상" 탭(개요/통계 옆의 기존 9탭 중 하나, 종합 분석 탭과는 별개)의 리스트도 동일하게 각 항목에 `https://www.youtube.com/watch?v={영상ID}` 링크를 걸어 새 탭에서 바로 재생되게 한다.

**저장됨 → 프로젝트 연결 (2.11, 두 기능의 연결 고리)**
- 저장된 아이디어/영상에서 `대본 생성` 클릭 → 기능 1의 새 프로젝트가 자동 생성되고 스크립트 설정 탭에 컨텍스트가 주입된 상태로 이동

### 2.4 데이터 모델 (분석용, 초안)

```
CachedVideo / CachedChannel   : API 응답 캐시 (TTL 필드 포함)
FavoriteChannel               : 즐겨찾기 채널 (제목 이력 추적 대상)
TitleSnapshot                 : videoId, title, viewCount, capturedAt
SavedItem                     : type(VIDEO|CHANNEL|IDEA), snapshotJson, note
KeywordAnalysis               : keyword, score, evidenceJson, analyzedAt
```

---

## 3. 개발 로드맵

### Phase 1 — 뼈대 (1~2주)
- [ ] Next.js + Prisma + SQLite 프로젝트 셋업, 레이아웃/네비게이션
- [ ] 기능 1: Channel/Project CRUD + 목록(검색·필터·정렬·검수상태) 완성
- [ ] YouTube Data API 클라이언트 + 캐싱 레이어

### Phase 2 — 파이프라인 (2~3주)
- [ ] 스크립트 생성(Anthropic API) → TTS → 이미지 → FFmpeg 렌더링 순서로 1단계씩
- [ ] SSE 진행률 스트리밍
- [ ] YouTube OAuth + 업로드

### Phase 3 — 분석 대시보드 (2~3주)
- [ ] 탐색·분석 / 채널 분석 / 떡상 영상 / 영상 SEO
- [ ] 영상 분석 모달 (자막·댓글·AI 아이디어)
- [ ] 저장됨 + 프로젝트 연결

### Phase 4 — 고도화
- [ ] 예약 업로드, 썸네일 편집기, 한·영 비교, 제목 변경 이력(cron), 쇼핑 소스
- [ ] PostgreSQL 전환, 배포

---

## 4. 비기능 요구사항

- **쿼터 관리**: YouTube API 호출량을 DB에 기록, 일일 사용량 대시보드 표시, 80% 도달 시 경고
- **비용 관리**: Anthropic/TTS/이미지 API 호출 건수·예상 비용 로깅
- **파일 저장**: 생성 산출물은 `./storage/{projectId}/` 하위에 저장, DB에는 경로만
- **에러 처리**: 파이프라인 각 단계 실패 시 status=FAILED + 에러 메시지 저장, 해당 단계만 재시도 가능
- **약관 준수**: 스크래핑 금지. 공식 API만 사용. YouTube 콘텐츠 정책(중복 콘텐츠) 관련 안내 문구를 업로드 화면에 표시
